# Historical benchmark data

`results/latest.json` and `benchmark-results.schema.json` remain here because
the website consumes them. These measurements describe the former compiler.

The runner, example programs, and original documentation are preserved under
`Development/DeferredCompiler/Benchmarks/Speed`. They depend on the retired
bootstrap and are deferred until the C compiler produces native programs.
