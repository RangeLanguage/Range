macro cyclic(let origin: Value, let period: Value): @member -> Value {
  let value: #environment.target.Application.value

  if period <= 0 {
    @diagnostic(
      "Cyclic period must be greater than zero"
    )
  }

  origin + ((((value - origin) % period) + period) % period)
}
