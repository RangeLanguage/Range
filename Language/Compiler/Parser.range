function syntaxNodeStoreCreate(let capacity: Int): SyntaxNodeStore {
    return SyntaxNodeStore(
        ids: @many(capacity: capacity),
        firstTokenIDs: @many(capacity: capacity),
        afterLastTokenIDs: @many(capacity: capacity)
    )
}

function syntaxNodeStoreAppend(
    let store: SyntaxNodeStore,
    let id: Int,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state row: Int(0)
    while row < store.ids.count {
        if store.ids.element(index: row) == id {
            if firstTokenID < store.firstTokenIDs.element(index: row) {
                if store.firstTokenIDs.update(
                    element: firstTokenID,
                    index: row
                ) != 0 { return 0 - 1 }
            }
            if afterLastTokenID > store.afterLastTokenIDs.element(index: row) {
                if store.afterLastTokenIDs.update(
                    element: afterLastTokenID,
                    index: row
                ) != 0 { return 0 - 1 }
            }
            return 0
        }
        row: row + 1
    }
    if store.ids.append(element: id) != 0 { return 0 - 1 }
    if store.firstTokenIDs.append(element: firstTokenID) != 0 { return 0 - 1 }
    return store.afterLastTokenIDs.append(element: afterLastTokenID)
}

function syntaxNodeStoreDestroy(let store: SyntaxNodeStore): Int {
    state status: Int(0)
    if store.ids.destroy() != 0 { status: 0 - 1 }
    if store.firstTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.afterLastTokenIDs.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBDeclarationFacetStoreCreate(
    let capacity: Int
): CompilerBDeclarationFacetStore {
    return CompilerBDeclarationFacetStore(
        generics: compilerBMemberFacetStoreCreate(capacity: capacity),
        syntaxIDs: @many(capacity: capacity),
        identityTokenIDs: @many(capacity: capacity),
        bodySyntaxIDs: @many(capacity: capacity)
    )
}

function compilerBDeclarationFacetStoreAppend(
    let store: CompilerBDeclarationFacetStore,
    let syntaxID: Int,
    let identityTokenID: Int,
    let bodySyntaxID: Int
): Int {
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.identityTokenIDs.append(element: identityTokenID) != 0 { return 0 - 1 }
    return store.bodySyntaxIDs.append(element: bodySyntaxID)
}

function compilerBDeclarationFacetStoreDestroy(
    let store: CompilerBDeclarationFacetStore
): Int {
    state status: Int(0)
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.bodySyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if compilerBMemberFacetStoreDestroy(store: store.generics) != 0 { status: 0 - 1 }
    return status
}

function compilerBBlockFacetStoreCreate(let capacity: Int): CompilerBBlockFacetStore {
    return CompilerBBlockFacetStore(
        syntaxIDs: @many(capacity: capacity),
        parentSyntaxIDs: @many(capacity: capacity)
    )
}

function compilerBBlockFacetStoreAppend(
    let store: CompilerBBlockFacetStore,
    let syntaxID: Int,
    let parentSyntaxID: Int
): Int {
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    return store.parentSyntaxIDs.append(element: parentSyntaxID)
}

function compilerBBlockFacetStoreDestroy(let store: CompilerBBlockFacetStore): Int {
    state status: Int(0)
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.parentSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBEnumCaseFacetStoreCreate(let capacity: Int): CompilerBEnumCaseFacetStore {
    return CompilerBEnumCaseFacetStore(
        syntaxIDs: @many(capacity: capacity),
        ownerSyntaxIDs: @many(capacity: capacity),
        identityTokenIDs: @many(capacity: capacity),
        ordinals: @many(capacity: capacity)
    )
}

function compilerBEnumCaseFacetStoreAppend(
    let store: CompilerBEnumCaseFacetStore,
    let syntaxID: Int,
    let ownerSyntaxID: Int,
    let identityTokenID: Int,
    let ordinal: Int
): Int {
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 { return 0 - 1 }
    if store.identityTokenIDs.append(element: identityTokenID) != 0 { return 0 - 1 }
    return store.ordinals.append(element: ordinal)
}

function compilerBEnumCaseFacetStoreDestroy(let store: CompilerBEnumCaseFacetStore): Int {
    state status: Int(0)
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.ordinals.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBMacroApplicationStoreCreate(let capacity: Int): CompilerBMacroApplicationStore {
    return CompilerBMacroApplicationStore(
        syntaxIDs: @many(capacity: capacity),
        identityTokenIDs: @many(capacity: capacity),
        macroDeclarationSyntaxIDs: @many(capacity: capacity),
        firstArgumentTokenIDs: @many(capacity: capacity),
        afterLastArgumentTokenIDs: @many(capacity: capacity),
        targetSyntaxIDs: @many(capacity: capacity),
        targetIdentityTokenIDs: @many(capacity: capacity),
        ordinals: @many(capacity: capacity),
        argumentApplicationSyntaxIDs: @many(capacity: capacity),
        argumentLabelTokenIDs: @many(capacity: capacity),
        argumentValueExpressionSyntaxIDs: @many(capacity: capacity),
        argumentOrdinals: @many(capacity: capacity),
        environmentApplicationSyntaxIDs: @many(capacity: capacity),
        environmentSyntaxIDs: @many(capacity: capacity),
        environmentOrdinals: @many(capacity: capacity)
    )
}

function compilerBMacroApplicationStoreDestroy(let store: CompilerBMacroApplicationStore): Int {
    state status: Int(0)
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.macroDeclarationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.firstArgumentTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.afterLastArgumentTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.targetSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.targetIdentityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.ordinals.destroy() != 0 { status: 0 - 1 }
    if store.argumentApplicationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.argumentLabelTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.argumentValueExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.argumentOrdinals.destroy() != 0 { status: 0 - 1 }
    if store.environmentApplicationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.environmentSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.environmentOrdinals.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBMacroApplicationStoreAppend(
    let store: CompilerBMacroApplicationStore,
    let syntaxID: Int,
    let identityTokenID: Int,
    let firstArgumentTokenID: Int,
    let afterLastArgumentTokenID: Int,
    let targetSyntaxID: Int,
    let targetIdentityTokenID: Int,
    let ordinal: Int
): Int {
    state existingRow: Int(0)
    while existingRow < store.syntaxIDs.count {
        if store.syntaxIDs.element(index: existingRow) == syntaxID { return 0 }
        existingRow: existingRow + 1
    }
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.identityTokenIDs.append(element: identityTokenID) != 0 { return 0 - 1 }
    if store.macroDeclarationSyntaxIDs.append(element: 0 - 1) != 0 { return 0 - 1 }
    if store.firstArgumentTokenIDs.append(element: firstArgumentTokenID) != 0 { return 0 - 1 }
    if store.afterLastArgumentTokenIDs.append(element: afterLastArgumentTokenID) != 0 { return 0 - 1 }
    if store.targetSyntaxIDs.append(element: targetSyntaxID) != 0 { return 0 - 1 }
    if store.targetIdentityTokenIDs.append(element: targetIdentityTokenID) != 0 { return 0 - 1 }
    return store.ordinals.append(element: ordinal)
}

function compilerBMacroParameterFacetStoreAppend(
    let store: CompilerBMacroSignatureFacetStore,
    let macroSyntaxID: Int,
    let keywordTokenID: Int,
    let identityTokenID: Int,
    let typeTokenID: Int,
    let cardinality: Bool,
    let optional: Bool,
    let defaultFirstTokenID: Int,
    let defaultAfterLastTokenID: Int,
    let ordinal: Int
): Int {
    if store.parameterMacroSyntaxIDs.append(element: macroSyntaxID) != 0 { return 0 - 1 }
    if store.parameterKeywordTokenIDs.append(element: keywordTokenID) != 0 { return 0 - 1 }
    if store.parameterIdentityTokenIDs.append(element: identityTokenID) != 0 { return 0 - 1 }
    if store.parameterTypeTokenIDs.append(element: typeTokenID) != 0 { return 0 - 1 }
    state cardinalityValue: Int(0)
    if cardinality { cardinalityValue: 1 }
    if store.parameterCardinalityValues.append(element: cardinalityValue) != 0 {
        return 0 - 1
    }
    state optionalValue: Int(0)
    if optional { optionalValue: 1 }
    if store.parameterOptionalValues.append(element: optionalValue) != 0 {
        return 0 - 1
    }
    if store.parameterDefaultFirstTokenIDs.append(
        element: defaultFirstTokenID
    ) != 0 { return 0 - 1 }
    if store.parameterDefaultAfterLastTokenIDs.append(
        element: defaultAfterLastTokenID
    ) != 0 { return 0 - 1 }
    return store.parameterOrdinals.append(element: ordinal)
}

function compilerBMacroArgumentFacetStoreAppend(
    let store: CompilerBMacroApplicationStore,
    let applicationSyntaxID: Int,
    let labelTokenID: Int,
    let valueExpressionSyntaxID: Int,
    let ordinal: Int
): Int {
    state existingRow: Int(0)
    while existingRow < store.argumentApplicationSyntaxIDs.count {
        if store.argumentApplicationSyntaxIDs.element(index: existingRow)
            == applicationSyntaxID
            && store.argumentLabelTokenIDs.element(index: existingRow) == labelTokenID
            && store.argumentValueExpressionSyntaxIDs.element(index: existingRow)
                == valueExpressionSyntaxID { return 0 }
        existingRow: existingRow + 1
    }
    if store.argumentApplicationSyntaxIDs.append(element: applicationSyntaxID) != 0 {
        return 0 - 1
    }
    if store.argumentLabelTokenIDs.append(element: labelTokenID) != 0 { return 0 - 1 }
    if store.argumentValueExpressionSyntaxIDs.append(element: valueExpressionSyntaxID) != 0 {
        return 0 - 1
    }
    return store.argumentOrdinals.append(element: ordinal)
}

function compilerBSwitchCaseRowContainingEnvironment(
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let environmentSyntaxID: Int
): Int {
    let environmentNodeRow: Int(nodes.ids.firstIndex(value: environmentSyntaxID))
    if environmentNodeRow < 0 { return 0 - 1 }
    let firstTokenID: Int(nodes.firstTokenIDs.element(index: environmentNodeRow))
    state result: Int(0 - 1)
    state row: Int(0)
    while row < process.switchCases.syntaxIDs.count {
        if firstTokenID >= process.switchCases.firstBodyTokenIDs.element(index: row)
            && firstTokenID < process.switchCases.afterLastBodyTokenIDs.element(index: row) {
            result: row
        }
        row: row + 1
    }
    return result
}

function compilerBEvaluateCompileTimeIntegerExpression(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBMacroApplicationStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let process: CompilerBProcessGraph,
    let applicationRow: Int,
    let macroSyntaxID: Int,
    let expressionSyntaxID: Int
): CompilerBCompileTimeInteger {
    let subjectRow: Int(process.expressions.syntaxIDs.firstIndex(value: expressionSyntaxID))
    if subjectRow < 0 {
        return CompilerBCompileTimeInteger(found: false, hasValue: false, value: 0)
    }
    if process.expressions.kindCodes.element(index: subjectRow)
        == compilerBParserExpressionKindIntegerCode() {
        return CompilerBCompileTimeInteger(
            found: true,
            hasValue: true,
            value: process.expressions.integerValues.element(index: subjectRow)
        )
    }
    let subjectFirstTokenID: Int(process.expressions.firstTokenIDs.element(index: subjectRow))
    if process.expressions.afterLastTokenIDs.element(index: subjectRow)
        != subjectFirstTokenID + 1 {
        return CompilerBCompileTimeInteger(found: false, hasValue: false, value: 0)
    }
    let subject: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: subjectFirstTokenID)
    ))
    state parameterRow: Int(0 - 1)
    state row: Int(0)
    while row < signatures.parameterMacroSyntaxIDs.count {
        if signatures.parameterMacroSyntaxIDs.element(index: row) == macroSyntaxID {
            let parameter: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: signatures.parameterIdentityTokenIDs.element(index: row))
            ))
            if compilerBTokenBytesEqual(source: source, left: subject, right: parameter) {
                parameterRow: row
            }
        }
        row: row + 1
    }
    if parameterRow < 0 {
        return CompilerBCompileTimeInteger(found: false, hasValue: false, value: 0)
    }
    let applicationSyntaxID: Int(applications.syntaxIDs.element(index: applicationRow))
    state argumentRow: Int(0 - 1)
    row: 0
    while row < applications.argumentApplicationSyntaxIDs.count {
        if applications.argumentApplicationSyntaxIDs.element(index: row)
            == applicationSyntaxID {
            let labelTokenID: Int(applications.argumentLabelTokenIDs.element(index: row))
            state matches: Bool(false)
            if labelTokenID < 0 {
                matches: applications.argumentOrdinals.element(index: row)
                    == signatures.parameterOrdinals.element(index: parameterRow)
            } else {
                let label: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: labelTokenID)
                ))
                let parameter: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: signatures.parameterIdentityTokenIDs.element(
                        index: parameterRow
                    ))
                ))
                matches: compilerBTokenBytesEqual(
                    source: source,
                    left: label,
                    right: parameter
                )
            }
            if matches { argumentRow: row }
        }
        row: row + 1
    }
    if argumentRow >= 0 {
        let expressionRow: Int(process.expressions.syntaxIDs.firstIndex(value: applications.argumentValueExpressionSyntaxIDs.element(index: argumentRow)))
        if expressionRow >= 0
            && process.expressions.kindCodes.element(index: expressionRow)
                == compilerBParserExpressionKindIntegerCode() {
            return CompilerBCompileTimeInteger(
                found: true,
                hasValue: true,
                value: process.expressions.integerValues.element(index: expressionRow)
            )
        }
        return CompilerBCompileTimeInteger(found: false, hasValue: false, value: 0)
    }
    let defaultFirstTokenID: Int(
        signatures.parameterDefaultFirstTokenIDs.element(index: parameterRow)
    )
    let defaultAfterLastTokenID: Int(
        signatures.parameterDefaultAfterLastTokenIDs.element(index: parameterRow)
    )
    if defaultFirstTokenID >= 0 && defaultAfterLastTokenID == defaultFirstTokenID + 1 {
        let defaultToken: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: defaultFirstTokenID)
        ))
        if defaultToken.kind == .integer {
            return CompilerBCompileTimeInteger(
                found: true,
                hasValue: true,
                value: compilerBParserDecimalTokenValue(source: source, token: defaultToken)
            )
        }
    }
    if signatures.parameterOptionalValues.element(index: parameterRow) != 0 {
        return CompilerBCompileTimeInteger(found: true, hasValue: false, value: 0)
    }
    return CompilerBCompileTimeInteger(found: false, hasValue: false, value: 0)
}

// RANGE SUBSTRATE — APM (Arithmetic Pattern Matching): A switch case may match
// absence or be a partial arithmetic expression. Parsing supplies the switch
// subject as the implicit left operand, so every non-default case becomes an
// ordinary comparison expression in the canonical process graph.
function compilerBEvaluateCompileTimeBooleanExpression(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBMacroApplicationStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let process: CompilerBProcessGraph,
    let applicationRow: Int,
    let macroSyntaxID: Int,
    let expressionSyntaxID: Int
): Bool {
    let comparisonRow: Int(process.comparisons.syntaxIDs.firstIndex(value: expressionSyntaxID))
    if comparisonRow < 0 { return false }
    let left: CompilerBCompileTimeInteger(compilerBEvaluateCompileTimeIntegerExpression(
        source: source,
        tokens: tokens,
        applications: applications,
        signatures: signatures,
        process: process,
        applicationRow: applicationRow,
        macroSyntaxID: macroSyntaxID,
        expressionSyntaxID: process.comparisons.leftExpressionSyntaxIDs.element(
            index: comparisonRow
        )
    ))
    if !left.found { return false }
    let operatorCode: Int(process.comparisons.operatorCodes.element(index: comparisonRow))
    if operatorCode == compilerBComparisonOperatorNoneCode() {
        return !left.hasValue
    }
    if !left.hasValue { return false }
    let right: CompilerBCompileTimeInteger(compilerBEvaluateCompileTimeIntegerExpression(
        source: source,
        tokens: tokens,
        applications: applications,
        signatures: signatures,
        process: process,
        applicationRow: applicationRow,
        macroSyntaxID: macroSyntaxID,
        expressionSyntaxID: process.comparisons.rightExpressionSyntaxIDs.element(
            index: comparisonRow
        )
    ))
    if !right.found || !right.hasValue { return false }
    if operatorCode == compilerBComparisonOperatorEqualCode() {
        return left.value == right.value
    }
    if operatorCode == compilerBComparisonOperatorNotEqualCode() {
        return left.value != right.value
    }
    if operatorCode == compilerBComparisonOperatorLessCode() {
        return left.value < right.value
    }
    if operatorCode == compilerBComparisonOperatorLessOrEqualCode() {
        return left.value <= right.value
    }
    if operatorCode == compilerBComparisonOperatorGreaterCode() {
        return left.value > right.value
    }
    if operatorCode == compilerBComparisonOperatorGreaterOrEqualCode() {
        return left.value >= right.value
    }
    return false
}

function compilerBMacroSwitchCaseIsSelected(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBMacroApplicationStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let process: CompilerBProcessGraph,
    let applicationRow: Int,
    let macroSyntaxID: Int,
    let caseRow: Int
): Bool {
    if caseRow < 0 || caseRow >= process.switchCases.syntaxIDs.count { return false }
    let switchSyntaxID: Int(process.switchCases.switchSyntaxIDs.element(index: caseRow))
    let switchRow: Int(process.switches.syntaxIDs.firstIndex(value: switchSyntaxID))
    if switchRow < 0 { return false }
    let conditionExpressionSyntaxID: Int(
        process.switchCases.conditionExpressionSyntaxIDs.element(index: caseRow)
    )
    if conditionExpressionSyntaxID >= 0 {
        return compilerBEvaluateCompileTimeBooleanExpression(
            source: source,
            tokens: tokens,
            applications: applications,
            signatures: signatures,
            process: process,
            applicationRow: applicationRow,
            macroSyntaxID: macroSyntaxID,
            expressionSyntaxID: conditionExpressionSyntaxID
        )
    }
    state row: Int(0)
    while row < process.switchCases.syntaxIDs.count {
        if process.switchCases.switchSyntaxIDs.element(index: row) == switchSyntaxID {
            let candidateConditionExpressionSyntaxID: Int(
                process.switchCases.conditionExpressionSyntaxIDs.element(index: row)
            )
            if candidateConditionExpressionSyntaxID >= 0 {
                if compilerBEvaluateCompileTimeBooleanExpression(
                    source: source,
                    tokens: tokens,
                    applications: applications,
                    signatures: signatures,
                    process: process,
                    applicationRow: applicationRow,
                    macroSyntaxID: macroSyntaxID,
                    expressionSyntaxID: candidateConditionExpressionSyntaxID
                ) { return false }
            }
        }
        row: row + 1
    }
    return true
}

function compilerBMacroEnvironmentIsSelected(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let applications: CompilerBMacroApplicationStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let process: CompilerBProcessGraph,
    let applicationRow: Int,
    let macroSyntaxID: Int,
    let environmentSyntaxID: Int
): Bool {
    let caseRow: Int(compilerBSwitchCaseRowContainingEnvironment(
        nodes: nodes,
        process: process,
        environmentSyntaxID: environmentSyntaxID
    ))
    if caseRow < 0 { return true }
    return compilerBMacroSwitchCaseIsSelected(
        source: source,
        tokens: tokens,
        applications: applications,
        signatures: signatures,
        process: process,
        applicationRow: applicationRow,
        macroSyntaxID: macroSyntaxID,
        caseRow: caseRow
    )
}

function compilerBMacroApplicationArgumentsMatchSignature(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBMacroApplicationStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let members: CompilerBMemberFacetStore,
    let process: CompilerBProcessGraph,
    let applicationRow: Int,
    let macroSyntaxID: Int
): Bool {
    let applicationSyntaxID: Int(applications.syntaxIDs.element(index: applicationRow))
    state parameterCount: Int(0)
    state requiredParameterCount: Int(0)
    state parameterRow: Int(0)
    while parameterRow < signatures.parameterMacroSyntaxIDs.count {
        if signatures.parameterMacroSyntaxIDs.element(index: parameterRow)
            == macroSyntaxID {
            parameterCount: parameterCount + 1
            if signatures.parameterOptionalValues.element(index: parameterRow) == 0
                && signatures.parameterDefaultFirstTokenIDs.element(index: parameterRow) < 0 {
                requiredParameterCount: requiredParameterCount + 1
            }
            if compilerBMacroParameterBindingStatus(
                source: source,
                tokens: tokens,
                members: members,
                process: process,
                applications: applications,
                applicationSyntaxID: applicationSyntaxID,
                parameterIdentityTokenID: signatures.parameterIdentityTokenIDs.element(
                    index: parameterRow
                ),
                parameterOrdinal: signatures.parameterOrdinals.element(index: parameterRow),
                parameterTypeTokenID: signatures.parameterTypeTokenIDs.element(
                    index: parameterRow
                ),
                parameterCardinality: signatures.parameterCardinalityValues.element(
                    index: parameterRow
                ) != 0,
                parameterOptional: signatures.parameterOptionalValues.element(
                    index: parameterRow
                ) != 0,
                parameterDefaultFirstTokenID: signatures.parameterDefaultFirstTokenIDs.element(
                    index: parameterRow
                )
            ) != 0 { return false }
        }
        parameterRow: parameterRow + 1
    }
    state argumentCount: Int(0)
    state argumentRow: Int(0)
    while argumentRow < applications.argumentApplicationSyntaxIDs.count {
        if applications.argumentApplicationSyntaxIDs.element(index: argumentRow)
            == applicationSyntaxID { argumentCount: argumentCount + 1 }
        argumentRow: argumentRow + 1
    }
    return argumentCount >= requiredParameterCount && argumentCount <= parameterCount
}

function compilerBResolveMacroApplications(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBMacroApplicationStore,
    let macros: CompilerBDeclarationFacetStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let members: CompilerBMemberFacetStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph
): Int {
    state applicationRow: Int(0)
    while applicationRow < applications.syntaxIDs.count {
        let applicationIdentity: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: applications.identityTokenIDs.element(index: applicationRow))
        ))
        state macroRow: Int(0)
        state namedCount: Int(0)
        state resolvedMacroSyntaxID: Int(0 - 1)
        while macroRow < macros.syntaxIDs.count {
            let macroIdentity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: macros.identityTokenIDs.element(index: macroRow))
            ))
            if compilerBTokenBytesEqual(
                source: source,
                left: applicationIdentity,
                right: macroIdentity
            ) {
                namedCount: namedCount + 1
                resolvedMacroSyntaxID: macros.syntaxIDs.element(index: macroRow)
            }
            macroRow: macroRow + 1
        }
        if namedCount > 1 {
            resolvedMacroSyntaxID: 0 - 1
            state matchedCount: Int(0)
            macroRow: 0
            while macroRow < macros.syntaxIDs.count {
                let macroIdentity: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: macros.identityTokenIDs.element(index: macroRow))
                ))
                let candidateSyntaxID: Int(macros.syntaxIDs.element(index: macroRow))
                if compilerBTokenBytesEqual(
                    source: source,
                    left: applicationIdentity,
                    right: macroIdentity
                ) && compilerBMacroApplicationArgumentsMatchSignature(
                    source: source,
                    tokens: tokens,
                    applications: applications,
                    signatures: signatures,
                    members: members,
                    process: process,
                    applicationRow: applicationRow,
                    macroSyntaxID: candidateSyntaxID
                ) {
                    matchedCount: matchedCount + 1
                    resolvedMacroSyntaxID: candidateSyntaxID
                }
                macroRow: macroRow + 1
            }
            if matchedCount == 0 { return 0 - 3 }
            if matchedCount != 1 { return 0 - 2 }
        }
        if resolvedMacroSyntaxID >= 0 {
            if applications.macroDeclarationSyntaxIDs.update(
                element: resolvedMacroSyntaxID,
                index: applicationRow
            ) != 0 { return 0 - 1 }
            state environmentRow: Int(0)
            state environmentOrdinal: Int(0)
            while environmentRow < process.environments.syntaxIDs.count {
                if process.environments.ownerDeclarationSyntaxIDs.element(
                    index: environmentRow
                ) == resolvedMacroSyntaxID && compilerBMacroEnvironmentIsSelected(
                    source: source,
                    tokens: tokens,
                    nodes: nodes,
                    applications: applications,
                    signatures: signatures,
                    process: process,
                    applicationRow: applicationRow,
                    macroSyntaxID: resolvedMacroSyntaxID,
                    environmentSyntaxID: process.environments.syntaxIDs.element(
                        index: environmentRow
                    )
                ) {
                    if applications.environmentApplicationSyntaxIDs.append(
                        element: applications.syntaxIDs.element(index: applicationRow)
                    ) != 0 { return 0 - 1 }
                    if applications.environmentSyntaxIDs.append(
                        element: process.environments.syntaxIDs.element(index: environmentRow)
                    ) != 0 { return 0 - 1 }
                    if applications.environmentOrdinals.append(
                        element: environmentOrdinal
                    ) != 0 { return 0 - 1 }
                    environmentOrdinal: environmentOrdinal + 1
                }
                environmentRow: environmentRow + 1
            }
        }
        applicationRow: applicationRow + 1
    }
    return 0
}

function compilerBResolveApplicationsAgainstDeclarations(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBApplicationFacetStore,
    let declarations: CompilerBDeclarationFacetStore
): Int {
    state applicationRow: Int(0)
    while applicationRow < applications.syntaxIDs.count {
        let identityTokenID: Int(applications.identityTokenIDs.element(
            index: applicationRow
        ))
        if identityTokenID >= 0 {
            let applicationIdentity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: identityTokenID)
            ))
            state declarationRow: Int(0)
            while declarationRow < declarations.syntaxIDs.count {
                let declarationIdentity: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: declarations.identityTokenIDs.element(
                        index: declarationRow
                    ))
                ))
                if compilerBTokenBytesEqual(
                    source: source,
                    left: applicationIdentity,
                    right: declarationIdentity
                ) {
                    let resolved: Int(applications.declarationSyntaxIDs.element(
                        index: applicationRow
                    ))
                    let candidate: Int(declarations.syntaxIDs.element(index: declarationRow))
                    if resolved >= 0 && resolved != candidate { return 0 - 2 }
                    if applications.declarationSyntaxIDs.update(
                        element: candidate,
                        index: applicationRow
                    ) != 0 { return 0 - 1 }
                }
                declarationRow: declarationRow + 1
            }
        }
        applicationRow: applicationRow + 1
    }
    return 0
}

function compilerBResolveApplications(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applications: CompilerBApplicationFacetStore,
    let constructs: CompilerBDeclarationFacetStore,
    let functions: CompilerBDeclarationFacetStore,
    let macros: CompilerBDeclarationFacetStore
): Int {
    let constructStatus: Int(compilerBResolveApplicationsAgainstDeclarations(
        source: source,
        tokens: tokens,
        applications: applications,
        declarations: constructs
    ))
    if constructStatus != 0 { return constructStatus }
    let functionStatus: Int(compilerBResolveApplicationsAgainstDeclarations(
        source: source,
        tokens: tokens,
        applications: applications,
        declarations: functions
    ))
    if functionStatus != 0 { return functionStatus }
    return compilerBResolveApplicationsAgainstDeclarations(
        source: source,
        tokens: tokens,
        applications: applications,
        declarations: macros
    )
}

function compilerBMemberFacetStoreCreate(let capacity: Int): CompilerBMemberFacetStore {
    return CompilerBMemberFacetStore(
        syntaxIDs: @many(capacity: capacity),
        ownerSyntaxIDs: @many(capacity: capacity),
        kindCodes: @many(capacity: capacity),
        keywordTokenIDs: @many(capacity: capacity),
        identityTokenIDs: @many(capacity: capacity),
        typeTokenIDs: @many(capacity: capacity),
        firstInitializerTokenIDs: @many(capacity: capacity),
        afterLastInitializerTokenIDs: @many(capacity: capacity),
        initializerExpressionSyntaxIDs: @many(capacity: capacity),
        rhsSyntaxIDs: @many(capacity: capacity),
        bodySyntaxIDs: @many(capacity: capacity),
        initializerKindCodes: @many(capacity: capacity),
        initializerValues: @many(capacity: capacity)
    )
}

function compilerBMemberFacetStoreDestroy(let store: CompilerBMemberFacetStore): Int {
    state status: Int(0)
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.kindCodes.destroy() != 0 { status: 0 - 1 }
    if store.keywordTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.typeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.firstInitializerTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.afterLastInitializerTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.initializerExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.rhsSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.bodySyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.initializerKindCodes.destroy() != 0 { status: 0 - 1 }
    if store.initializerValues.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBMemberFacetStoreAppend(
    let store: CompilerBMemberFacetStore,
    let syntaxID: Int,
    let ownerSyntaxID: Int,
    let kindCode: Int,
    let keywordTokenID: Int,
    let identityTokenID: Int,
    let typeTokenID: Int,
    let firstInitializerTokenID: Int,
    let afterLastInitializerTokenID: Int,
    let initializerExpressionSyntaxID: Int,
    let bodySyntaxID: Int,
    let initializerKindCode: Int,
    let initializerValue: Int
): Int {
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 { return 0 - 1 }
    if store.kindCodes.append(element: kindCode) != 0 { return 0 - 1 }
    if store.keywordTokenIDs.append(element: keywordTokenID) != 0 { return 0 - 1 }
    if store.identityTokenIDs.append(element: identityTokenID) != 0 { return 0 - 1 }
    if store.typeTokenIDs.append(element: typeTokenID) != 0 { return 0 - 1 }
    if store.firstInitializerTokenIDs.append(element: firstInitializerTokenID) != 0 {
        return 0 - 1
    }
    if store.afterLastInitializerTokenIDs.append(element: afterLastInitializerTokenID) != 0 {
        return 0 - 1
    }
    if store.initializerExpressionSyntaxIDs.append(
        element: initializerExpressionSyntaxID
    ) != 0 { return 0 - 1 }
    if store.bodySyntaxIDs.append(element: bodySyntaxID) != 0 { return 0 - 1 }
    if store.rhsSyntaxIDs.append(element: 0 - 1) != 0 { return 0 - 1 }
    if store.initializerKindCodes.append(element: initializerKindCode) != 0 { return 0 - 1 }
    return store.initializerValues.append(element: initializerValue)
}

function compilerBFunctionSignatureFacetStoreCreate(let capacity: Int): CompilerBFunctionSignatureFacetStore {
    return CompilerBFunctionSignatureFacetStore(
        functionSyntaxIDs: @many(capacity: capacity),
        syntaxIDs: @many(capacity: capacity),
        returnTypeTokenIDs: @many(capacity: capacity),
        inputCardinalityTokenIDs: @many(capacity: capacity),
        outputCardinalityTokenIDs: @many(capacity: capacity)
    )
}

function compilerBFunctionSignatureFacetStoreDestroy(let store: CompilerBFunctionSignatureFacetStore): Int {
    state status: Int(0)
    if store.functionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.returnTypeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.inputCardinalityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.outputCardinalityTokenIDs.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBFunctionParameterFacetStoreCreate(
    let capacity: Int
): CompilerBFunctionParameterFacetStore {
    return CompilerBFunctionParameterFacetStore(
        functionSyntaxIDs: @many(capacity: capacity),
        keywordTokenIDs: @many(capacity: capacity),
        identityTokenIDs: @many(capacity: capacity),
        typeTokenIDs: @many(capacity: capacity),
        ordinals: @many(capacity: capacity)
    )
}

function compilerBFunctionParameterFacetStoreDestroy(
    let store: CompilerBFunctionParameterFacetStore
): Int {
    state status: Int(0)
    if store.functionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.keywordTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.typeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.ordinals.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBFunctionSignatureFacetStoreAppend(
    let store: CompilerBFunctionSignatureFacetStore,
    let functionSyntaxID: Int,
    let syntaxID: Int,
    let returnTypeTokenID: Int,
    let inputCardinalityTokenID: Int,
    let outputCardinalityTokenID: Int
): Int {
    if store.functionSyntaxIDs.append(element: functionSyntaxID) != 0 { return 0 - 1 }
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.returnTypeTokenIDs.append(element: returnTypeTokenID) != 0 { return 0 - 1 }
    if store.inputCardinalityTokenIDs.append(element: inputCardinalityTokenID) != 0 {
        return 0 - 1
    }
    return store.outputCardinalityTokenIDs.append(element: outputCardinalityTokenID)
}

function compilerBMacroSignatureFacetStoreCreate(
    let capacity: Int
): CompilerBMacroSignatureFacetStore {
    return CompilerBMacroSignatureFacetStore(
        macroSyntaxIDs: @many(capacity: capacity),
        syntaxIDs: @many(capacity: capacity),
        targetTypeTokenIDs: @many(capacity: capacity),
        afterLastTargetTypeTokenIDs: @many(capacity: capacity),
        resultTypeTokenIDs: @many(capacity: capacity),
        inputCardinalityTokenIDs: @many(capacity: capacity),
        outputCardinalityTokenIDs: @many(capacity: capacity),
        parameterMacroSyntaxIDs: @many(capacity: capacity),
        parameterKeywordTokenIDs: @many(capacity: capacity),
        parameterIdentityTokenIDs: @many(capacity: capacity),
        parameterTypeTokenIDs: @many(capacity: capacity),
        parameterCardinalityValues: @many(capacity: capacity),
        parameterOptionalValues: @many(capacity: capacity),
        parameterDefaultFirstTokenIDs: @many(capacity: capacity),
        parameterDefaultAfterLastTokenIDs: @many(capacity: capacity),
        parameterOrdinals: @many(capacity: capacity)
    )
}

function compilerBMacroSignatureFacetStoreDestroy(
    let store: CompilerBMacroSignatureFacetStore
): Int {
    state status: Int(0)
    if store.macroSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.targetTypeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.afterLastTargetTypeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.resultTypeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.inputCardinalityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.outputCardinalityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterMacroSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterKeywordTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterIdentityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterTypeTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterCardinalityValues.destroy() != 0 { status: 0 - 1 }
    if store.parameterOptionalValues.destroy() != 0 { status: 0 - 1 }
    if store.parameterDefaultFirstTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterDefaultAfterLastTokenIDs.destroy() != 0 { status: 0 - 1 }
    if store.parameterOrdinals.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBMacroSignatureFacetStoreAppend(
    let store: CompilerBMacroSignatureFacetStore,
    let macroSyntaxID: Int,
    let syntaxID: Int,
    let targetTypeTokenID: Int,
    let afterLastTargetTypeTokenID: Int,
    let resultTypeTokenID: Int,
    let inputCardinalityTokenID: Int,
    let outputCardinalityTokenID: Int
): Int {
    if store.macroSyntaxIDs.append(element: macroSyntaxID) != 0 { return 0 - 1 }
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.targetTypeTokenIDs.append(element: targetTypeTokenID) != 0 { return 0 - 1 }
    if store.afterLastTargetTypeTokenIDs.append(
        element: afterLastTargetTypeTokenID
    ) != 0 { return 0 - 1 }
    if store.resultTypeTokenIDs.append(element: resultTypeTokenID) != 0 { return 0 - 1 }
    if store.inputCardinalityTokenIDs.append(
        element: inputCardinalityTokenID
    ) != 0 { return 0 - 1 }
    return store.outputCardinalityTokenIDs.append(element: outputCardinalityTokenID)
}

function compilerBProcessGraphCreate(let capacity: Int): CompilerBProcessGraph {
    return CompilerBProcessGraph(
        processes: CompilerBProcessFacetStore(
            ownerSyntaxIDs: @many(capacity: capacity),
            bodySyntaxIDs: @many(capacity: capacity),
            resultExpressionSyntaxIDs: @many(capacity: capacity)
        ),
        expressions: CompilerBExpressionFacetStore(
            syntaxIDs: @many(capacity: capacity),
            kindCodes: @many(capacity: capacity),
            firstTokenIDs: @many(capacity: capacity),
            afterLastTokenIDs: @many(capacity: capacity),
            integerValues: @many(capacity: capacity)
        ),
        sourceChains: CompilerBSourceChainFacetStore(
            syntaxIDs: @many(capacity: capacity),
            firstTokenIDs: @many(capacity: capacity),
            afterLastTokenIDs: @many(capacity: capacity),
            occurrenceStarts: @many(capacity: capacity),
            occurrenceCounts: @many(capacity: capacity)
        ),
        sourceOccurrences: CompilerBSourceOccurrenceFacetStore(
            chainSyntaxIDs: @many(capacity: capacity),
            tokenIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        applications: CompilerBApplicationFacetStore(
            syntaxIDs: @many(capacity: capacity),
            predecessorSyntaxIDs: @many(capacity: capacity),
            declarationSyntaxIDs: @many(capacity: capacity),
            identityTokenIDs: @many(capacity: capacity),
            firstTokenIDs: @many(capacity: capacity),
            afterLastTokenIDs: @many(capacity: capacity)
        ),
        arguments: CompilerBApplicationArgumentFacetStore(
            applicationSyntaxIDs: @many(capacity: capacity),
            labelTokenIDs: @many(capacity: capacity),
            valueExpressionSyntaxIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        genericArguments: CompilerBApplicationArgumentFacetStore(
            applicationSyntaxIDs: @many(capacity: capacity),
            labelTokenIDs: @many(capacity: capacity),
            valueExpressionSyntaxIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        components: CompilerBApplicationComponentFacetStore(
            ownerApplicationSyntaxIDs: @many(capacity: capacity),
            labelTokenIDs: @many(capacity: capacity),
            firstValueTokenIDs: @many(capacity: capacity),
            afterLastValueTokenIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        projections: CompilerBProjectionSegmentStore(
            expressionSyntaxIDs: @many(capacity: capacity),
            tokenIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        locals: CompilerBLocalBindingFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            kindCodes: @many(capacity: capacity),
            identityTokenIDs: @many(capacity: capacity),
            valueExpressionSyntaxIDs: @many(capacity: capacity),
            firstValueTokenIDs: @many(capacity: capacity),
            afterLastValueTokenIDs: @many(capacity: capacity)
        ),
        assignments: CompilerBAssignmentFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            identityTokenIDs: @many(capacity: capacity),
            valueExpressionSyntaxIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        returns: CompilerBReturnFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            valueExpressionSyntaxIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        noValueFallbacks: CompilerBNoValueFallbackFacetStore(
            expressionSyntaxIDs: @many(capacity: capacity),
            questionTokenIDs: @many(capacity: capacity),
            firstTargetTokenIDs: @many(capacity: capacity),
            afterLastTargetTokenIDs: @many(capacity: capacity),
            firstRHSTokenIDs: @many(capacity: capacity),
            afterLastRHSTokenIDs: @many(capacity: capacity)
        ),
        conditionals: CompilerBConditionalFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            firstConditionTokenIDs: @many(capacity: capacity),
            afterLastConditionTokenIDs: @many(capacity: capacity),
            conditionExpressionSyntaxIDs: @many(capacity: capacity),
            bodySyntaxIDs: @many(capacity: capacity),
            alternativeSyntaxIDs: @many(capacity: capacity),
            alternativeKindCodes: @many(capacity: capacity)
        ),
        switches: CompilerBSwitchFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            subjectExpressionSyntaxIDs: @many(capacity: capacity)
        ),
        switchCases: CompilerBSwitchCaseFacetStore(
            syntaxIDs: @many(capacity: capacity),
            switchSyntaxIDs: @many(capacity: capacity),
            conditionExpressionSyntaxIDs: @many(capacity: capacity),
            firstBodyTokenIDs: @many(capacity: capacity),
            afterLastBodyTokenIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        comparisons: CompilerBComparisonFacetStore(
            syntaxIDs: @many(capacity: capacity),
            leftExpressionSyntaxIDs: @many(capacity: capacity),
            rightExpressionSyntaxIDs: @many(capacity: capacity),
            operatorCodes: @many(capacity: capacity)
        ),
        logicals: CompilerBLogicalFacetStore(
            syntaxIDs: @many(capacity: capacity),
            leftExpressionSyntaxIDs: @many(capacity: capacity),
            rightExpressionSyntaxIDs: @many(capacity: capacity),
            operatorCodes: @many(capacity: capacity)
        ),
        applicationProvisions: CompilerBApplicationProvisionFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            environmentSyntaxIDs: @many(capacity: capacity),
            extensionSyntaxIDs: @many(capacity: capacity),
            targetApplicationSyntaxIDs: @many(capacity: capacity),
            sourceSyntaxIDs: @many(capacity: capacity)
        ),
        environments: CompilerBEnvironmentFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerDeclarationSyntaxIDs: @many(capacity: capacity)
        ),
        environmentNodes: CompilerBEnvironmentNodeFacetStore(
            environmentSyntaxIDs: @many(capacity: capacity),
            syntaxIDs: @many(capacity: capacity),
            parentSyntaxIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        emittedMacroBlocks: CompilerBEmittedMacroBlockFacetStore(
            syntaxIDs: @many(capacity: capacity),
            identityTokenIDs: @many(capacity: capacity),
            bodySyntaxIDs: @many(capacity: capacity),
            openingTokenIDs: @many(capacity: capacity),
            closingTokenIDs: @many(capacity: capacity)
        ),
        extensions: CompilerBExtensionFacetStore(
            syntaxIDs: @many(capacity: capacity),
            targetApplicationSyntaxIDs: @many(capacity: capacity)
        ),
        collections: CompilerBCollectionFacetStore(
            syntaxIDs: @many(capacity: capacity),
            selectorIdentityTokenIDs: @many(capacity: capacity),
            selectorMacroSyntaxIDs: @many(capacity: capacity)
        ),
        collectionProductions: CompilerBCollectionProductionFacetStore(
            syntaxIDs: @many(capacity: capacity),
            sourceIdentityTokenIDs: @many(capacity: capacity),
            sourceCollectionSyntaxIDs: @many(capacity: capacity),
            outputIdentityTokenIDs: @many(capacity: capacity),
            outputDeclarationSyntaxIDs: @many(capacity: capacity)
        ),
        executions: CompilerBExecutionFacetStore(
            syntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity),
            expressionSyntaxIDs: @many(capacity: capacity),
            ordinals: @many(capacity: capacity)
        ),
        functionOwners: CompilerBFunctionOwnerFacetStore(
            functionSyntaxIDs: @many(capacity: capacity),
            ownerSyntaxIDs: @many(capacity: capacity)
        )
    )
}

function compilerBProcessGraphDestroy(let graph: CompilerBProcessGraph): Int {
    state status: Int(0)
    if graph.processes.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.processes.bodySyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.processes.resultExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.expressions.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.expressions.kindCodes.destroy() != 0 { status: 0 - 1 }
    if graph.expressions.firstTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.expressions.afterLastTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.expressions.integerValues.destroy() != 0 { status: 0 - 1 }
    if graph.sourceChains.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.sourceChains.firstTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.sourceChains.afterLastTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.sourceChains.occurrenceStarts.destroy() != 0 { status: 0 - 1 }
    if graph.sourceChains.occurrenceCounts.destroy() != 0 { status: 0 - 1 }
    if graph.sourceOccurrences.chainSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.sourceOccurrences.tokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.sourceOccurrences.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.applications.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applications.predecessorSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applications.declarationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applications.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applications.firstTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applications.afterLastTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.arguments.applicationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.arguments.labelTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.arguments.valueExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.arguments.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.genericArguments.applicationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.genericArguments.labelTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.genericArguments.valueExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.genericArguments.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.components.ownerApplicationSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.components.labelTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.components.firstValueTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.components.afterLastValueTokenIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.components.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.projections.expressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.projections.tokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.projections.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.locals.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.locals.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.locals.kindCodes.destroy() != 0 { status: 0 - 1 }
    if graph.locals.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.locals.valueExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.locals.firstValueTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.locals.afterLastValueTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.assignments.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.assignments.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.assignments.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.assignments.valueExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.assignments.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.returns.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.returns.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.returns.valueExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.returns.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.noValueFallbacks.expressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.noValueFallbacks.questionTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.noValueFallbacks.firstTargetTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.noValueFallbacks.afterLastTargetTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.noValueFallbacks.firstRHSTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.noValueFallbacks.afterLastRHSTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.firstConditionTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.afterLastConditionTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.conditionExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.bodySyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.alternativeSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.conditionals.alternativeKindCodes.destroy() != 0 { status: 0 - 1 }
    if graph.switches.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switches.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switches.subjectExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switchCases.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switchCases.switchSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switchCases.conditionExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switchCases.firstBodyTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switchCases.afterLastBodyTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.switchCases.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.comparisons.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.comparisons.leftExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.comparisons.rightExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.comparisons.operatorCodes.destroy() != 0 { status: 0 - 1 }
    if graph.logicals.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.logicals.leftExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.logicals.rightExpressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.logicals.operatorCodes.destroy() != 0 { status: 0 - 1 }
    if graph.applicationProvisions.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applicationProvisions.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.applicationProvisions.environmentSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.applicationProvisions.extensionSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.applicationProvisions.targetApplicationSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.applicationProvisions.sourceSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.environments.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.environments.ownerDeclarationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.environmentNodes.environmentSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.environmentNodes.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.environmentNodes.parentSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.environmentNodes.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.emittedMacroBlocks.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.emittedMacroBlocks.identityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.emittedMacroBlocks.bodySyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.emittedMacroBlocks.openingTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.emittedMacroBlocks.closingTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.extensions.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.extensions.targetApplicationSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.collections.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.collections.selectorIdentityTokenIDs.destroy() != 0 { status: 0 - 1 }
    if graph.collections.selectorMacroSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.collectionProductions.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.collectionProductions.sourceIdentityTokenIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.collectionProductions.sourceCollectionSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.collectionProductions.outputIdentityTokenIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.collectionProductions.outputDeclarationSyntaxIDs.destroy() != 0 {
        status: 0 - 1
    }
    if graph.executions.syntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.executions.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.executions.expressionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.executions.ordinals.destroy() != 0 { status: 0 - 1 }
    if graph.functionOwners.functionSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    if graph.functionOwners.ownerSyntaxIDs.destroy() != 0 { status: 0 - 1 }
    return status
}

function compilerBParserExpressionKindUnknownCode(): Int { return 0 }
function compilerBParserExpressionKindIntegerCode(): Int { return 1 }
function compilerBParserExpressionKindBooleanCode(): Int { return 2 }
function compilerBParserExpressionKindProjectionCode(): Int { return 3 }
function compilerBParserExpressionKindCallCode(): Int { return 4 }
function compilerBParserExpressionKindStringCode(): Int { return 5 }
function compilerBParserExpressionKindNoValueFallbackCode(): Int { return 6 }
function compilerBParserExpressionKindComparisonCode(): Int { return 7 }
function compilerBParserExpressionKindLogicalCode(): Int { return 8 }

function compilerBComparisonOperatorEqualCode(): Int { return 1 }
function compilerBComparisonOperatorLessCode(): Int { return 2 }
function compilerBComparisonOperatorLessOrEqualCode(): Int { return 3 }
function compilerBComparisonOperatorGreaterCode(): Int { return 4 }
function compilerBComparisonOperatorGreaterOrEqualCode(): Int { return 5 }
function compilerBComparisonOperatorNoneCode(): Int { return 6 }
function compilerBComparisonOperatorNotEqualCode(): Int { return 7 }

function compilerBLogicalOperatorAndCode(): Int { return 1 }
function compilerBLogicalOperatorOrCode(): Int { return 2 }
function compilerBLogicalOperatorNotCode(): Int { return 3 }

function compilerBLogicalOperatorName(let operatorCode: Int): String {
    if operatorCode == compilerBLogicalOperatorAndCode() { return "and" }
    if operatorCode == compilerBLogicalOperatorOrCode() { return "or" }
    if operatorCode == compilerBLogicalOperatorNotCode() { return "not" }
    return "unknown"
}

function compilerBComparisonOperatorName(let operatorCode: Int): String {
    if operatorCode == compilerBComparisonOperatorEqualCode() { return "equal" }
    if operatorCode == compilerBComparisonOperatorLessCode() { return "less" }
    if operatorCode == compilerBComparisonOperatorLessOrEqualCode() {
        return "lessOrEqual"
    }
    if operatorCode == compilerBComparisonOperatorGreaterCode() { return "greater" }
    if operatorCode == compilerBComparisonOperatorGreaterOrEqualCode() {
        return "greaterOrEqual"
    }
    if operatorCode == compilerBComparisonOperatorNoneCode() { return "none" }
    if operatorCode == compilerBComparisonOperatorNotEqualCode() { return "notEqual" }
    return "unknown"
}

function compilerBExpressionFacetStoreAppend(
    let store: CompilerBExpressionFacetStore,
    let syntaxID: Int,
    let kindCode: Int,
    let firstTokenID: Int,
    let afterLastTokenID: Int,
    let integerValue: Int
): Int {
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.kindCodes.append(element: kindCode) != 0 { return 0 - 1 }
    if store.firstTokenIDs.append(element: firstTokenID) != 0 { return 0 - 1 }
    if store.afterLastTokenIDs.append(element: afterLastTokenID) != 0 { return 0 - 1 }
    return store.integerValues.append(element: integerValue)
}

function compilerBParserScalarKindNoneCode(): Int { return 0 }
function compilerBParserScalarKindIntegerCode(): Int { return 1 }
function compilerBParserScalarKindBooleanCode(): Int { return 2 }
function compilerBParserMemberKindLetCode(): Int { return 1 }
function compilerBParserMemberKindStateCode(): Int { return 2 }
function compilerBParserMemberKindDerivedCode(): Int { return 3 }
function compilerBParserMemberKindBindingCode(): Int { return 4 }
function compilerBParserMemberKindConcreteCode(): Int { return 0 }

function compilerBParserDecimalTokenValue(let source: String, let token: CompilerBToken): Int {
    state cursor: Int(token.start)
    state value: Int(0)
    while cursor < token.end {
        value: value * 10 + source.byte(index: cursor) - 48
        cursor: cursor + 1
    }
    return value
}

function compilerBParserTokenByte(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let tokenID: Int
): Int {
    return source.byte(index: compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: tokenID)
    ).start)
}

// Named generic arguments distinguish <...> from an ordinary comparison.
function compilerBParserStartsGenericArguments(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let openingTokenID: Int
): Bool {
    if openingTokenID < 1 || openingTokenID + 2 >= tokens.kinds.count { return false }
    if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: openingTokenID) != 60 {
        return false
    }
    return compilerBRetainedToken(store: tokens, id: TokenID(value: openingTokenID - 1)).kind == .identity
        && compilerBRetainedToken(store: tokens, id: TokenID(value: openingTokenID + 1)).kind == .identity
        && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: openingTokenID + 2) == 58
}

function compilerBParserMatchingRightAngle(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let openingTokenID: Int,
    let limitTokenID: Int
): Int {
    state cursor: Int(openingTokenID + 1)
    state depth: Int(1)
    state nested: Int(0)
    while cursor < limitTokenID {
        let byte: Int(compilerBParserTokenByte(source: source, tokens: tokens, tokenID: cursor))
        if byte == 40 || byte == 91 || byte == 123 { nested: nested + 1 }
        else if byte == 41 || byte == 93 || byte == 125 {
            if nested == 0 { return 0 - 1 }
            nested: nested - 1
        } else if nested == 0 {
            if byte == 60 && compilerBParserStartsGenericArguments(
                source: source, tokens: tokens, openingTokenID: cursor
            ) { depth: depth + 1 }
            if byte == 62 {
                depth: depth - 1
                if depth == 0 { return cursor }
            }
        }
        cursor: cursor + 1
    }
    return 0 - 1
}

// Locate a comma outside nested applications, groups, and specializations.
function compilerBParserArgumentAfterLast(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let firstTokenID: Int,
    let limitTokenID: Int
): Int {
    state cursor: Int(firstTokenID)
    state depth: Int(0)
    while cursor < limitTokenID {
        let byte: Int(compilerBParserTokenByte(source: source, tokens: tokens, tokenID: cursor))
        if compilerBParserStartsGenericArguments(source: source, tokens: tokens, openingTokenID: cursor) {
            cursor: compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: cursor, limitTokenID: limitTokenID
            )
            if cursor < 0 { return 0 - 1 }
        } else {
            if byte == 40 || byte == 91 || byte == 123 { depth: depth + 1 }
            if byte == 41 || byte == 93 || byte == 125 { depth: depth - 1 }
            if depth < 0 { return 0 - 1 }
            if byte == 44 && depth == 0 { return cursor }
            if byte == 58 && depth == 0 { return 0 - 1 }
        }
        cursor: cursor + 1
    }
    if depth != 0 { return 0 - 1 }
    return cursor
}

function compilerBParseGenericMemberFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let closingTokenID: Int
): Int {
    state cursor: Int(openingTokenID + 1)
    if cursor >= closingTokenID { return 0 - 18 }
    while cursor < closingTokenID {
        if cursor + 3 >= closingTokenID { return 0 - 18 }
        let keyword: CompilerBToken(compilerBRetainedToken(store: tokens, id: TokenID(value: cursor)))
        let identity: CompilerBToken(compilerBRetainedToken(store: tokens, id: TokenID(value: cursor + 1)))
        if !compilerBTokenMatchesBytes(source: source, token: keyword, expected: "let")
            || identity.kind != .identity
            || compilerBTokenMatchesBytes(source: source, token: identity, expected: "_")
            || compilerBParserTokenByte(source: source, tokens: tokens, tokenID: cursor + 2) != 58 {
            return 0 - 18
        }
        let firstValue: Int(cursor + 3)
        let end: Int(compilerBParserArgumentAfterLast(
            source: source, tokens: tokens, firstTokenID: firstValue, limitTokenID: closingTokenID
        ))
        if end <= firstValue { return 0 - 18 }
        let value: Int(compilerBParseExpressionFacet(
            source: source, tokens: tokens, nodes: nodes, process: process,
            firstTokenID: firstValue, afterLastTokenID: end
        ))
        if value < 0 { return 0 - 18 }
        if syntaxNodeStoreAppend(store: nodes, id: keyword.start,
            firstTokenID: cursor, afterLastTokenID: end) != 0 { return 0 - 18 }
        if compilerBMemberFacetStoreAppend(
            store: members, syntaxID: keyword.start, ownerSyntaxID: ownerSyntaxID,
            kindCode: compilerBParserMemberKindLetCode(), keywordTokenID: cursor,
            identityTokenID: cursor + 1, typeTokenID: firstValue,
            firstInitializerTokenID: firstValue, afterLastInitializerTokenID: end,
            initializerExpressionSyntaxID: value, bodySyntaxID: 0 - 1,
            initializerKindCode: compilerBParserScalarKindNoneCode(), initializerValue: 0
        ) != 0 { return 0 - 18 }
        if members.rhsSyntaxIDs.update(element: value, index: members.syntaxIDs.count - 1) != 0 {
            return 0 - 18
        }
        cursor: end + 1
    }
    return 0
}

function compilerBParseArgumentRange(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let arguments: CompilerBApplicationArgumentFacetStore,
    let applicationSyntaxID: Int,
    let firstTokenID: Int,
    let afterLastTokenID: Int,
    let requireLabels: Bool
): Int {
    state cursor: Int(firstTokenID)
    state ordinal: Int(0)
    if requireLabels && cursor >= afterLastTokenID { return 0 - 18 }
    while cursor < afterLastTokenID {
        state label: Int(0 - 1)
        state firstValue: Int(cursor)
        if cursor + 1 < afterLastTokenID && compilerBParserTokenByte(
            source: source, tokens: tokens, tokenID: cursor + 1
        ) == 58 {
            let identity: CompilerBToken(compilerBRetainedToken(store: tokens, id: TokenID(value: cursor)))
            if identity.kind != .identity || compilerBTokenMatchesBytes(
                source: source, token: identity, expected: "_"
            ) { return 0 - 18 }
            label: cursor
            firstValue: cursor + 2
        }
        if requireLabels && label < 0 { return 0 - 18 }
        let end: Int(compilerBParserArgumentAfterLast(
            source: source, tokens: tokens, firstTokenID: firstValue, limitTokenID: afterLastTokenID
        ))
        if end <= firstValue { return 0 - 18 }
        let value: Int(compilerBParseExpressionFacet(
            source: source, tokens: tokens, nodes: nodes, process: process,
            firstTokenID: firstValue, afterLastTokenID: end
        ))
        if value < 0 { return value }
        if compilerBApplicationArgumentFacetStoreAppend(
            store: arguments, applicationSyntaxID: applicationSyntaxID,
            labelTokenID: label, valueExpressionSyntaxID: value, ordinal: ordinal
        ) != 0 { return 0 - 18 }
        ordinal: ordinal + 1
        cursor: end + 1
    }
    return 0
}

function compilerBParseSpecializedExpressionFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    let closing: Int(compilerBParserMatchingRightAngle(
        source: source, tokens: tokens, openingTokenID: firstTokenID + 1,
        limitTokenID: afterLastTokenID
    ))
    if closing < 0 { return 0 - 18 }
    let identity: CompilerBToken(compilerBRetainedToken(store: tokens, id: TokenID(value: firstTokenID)))
    state kind: Int(compilerBParserExpressionKindProjectionCode())
    if closing + 1 < afterLastTokenID {
        if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: closing + 1) != 40 {
            return 0 - 18
        }
        if compilerBParserMatchingRightParenthesis(
            source: source, tokens: tokens, openingTokenID: closing + 1
        ) != afterLastTokenID - 1 { return 0 - 18 }
        kind: compilerBParserExpressionKindCallCode()
    }
    if compilerBParseArgumentRange(
        source: source, tokens: tokens, nodes: nodes, process: process,
        arguments: process.genericArguments, applicationSyntaxID: identity.start,
        firstTokenID: firstTokenID + 2, afterLastTokenID: closing, requireLabels: true
    ) != 0 { return 0 - 18 }
    if kind == compilerBParserExpressionKindCallCode() {
        if compilerBParseArgumentRange(
            source: source, tokens: tokens, nodes: nodes, process: process,
            arguments: process.arguments, applicationSyntaxID: identity.start,
            firstTokenID: closing + 2, afterLastTokenID: afterLastTokenID - 1, requireLabels: false
        ) != 0 { return 0 - 18 }
    }
    if syntaxNodeStoreAppend(store: nodes, id: identity.start,
        firstTokenID: firstTokenID, afterLastTokenID: afterLastTokenID) != 0 { return 0 - 18 }
    if compilerBExpressionFacetStoreAppend(
        store: process.expressions, syntaxID: identity.start, kindCode: kind,
        firstTokenID: firstTokenID, afterLastTokenID: afterLastTokenID, integerValue: 0
    ) != 0 { return 0 - 18 }
    if compilerBApplicationFacetStoreAppend(
        store: process.applications, syntaxID: identity.start, predecessorSyntaxID: 0 - 1,
        identityTokenID: firstTokenID, firstTokenID: firstTokenID, afterLastTokenID: afterLastTokenID
    ) != 0 { return 0 - 18 }
    return identity.start
}

function compilerBParserMatchingRightParenthesis(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let openingTokenID: Int
): Int {
    state depth: Int(1)
    state tokenID: Int(openingTokenID + 1)
    while tokenID < tokens.kinds.count && depth != 0 {
        let byte: Int(compilerBParserTokenByte(source: source, tokens: tokens, tokenID: tokenID))
        if byte == 40 { depth: depth + 1 }
        if byte == 41 { depth: depth - 1 }
        tokenID: tokenID + 1
    }
    if depth != 0 { return 0 - 1 }
    return tokenID - 1
}

function compilerBParserMacroApplicationAfterLast(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let applicationTokenID: Int
): Int {
    if applicationTokenID + 1 >= tokens.kinds.count { return 0 - 1 }
    if applicationTokenID + 2 < tokens.kinds.count
        && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: applicationTokenID + 2) == 40 {
        let closing: Int(compilerBParserMatchingRightParenthesis(
            source: source,
            tokens: tokens,
            openingTokenID: applicationTokenID + 2
        ))
        if closing < 0 { return 0 - 1 }
        return closing + 1
    }
    if applicationTokenID + 2 < tokens.kinds.count
        && compilerBTokenMatchesBytes(
            source: source,
            token: compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: applicationTokenID + 1)
            ),
            expected: "syntax"
        )
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: applicationTokenID + 2
        ) == 123 {
        let closing: Int(compilerBParserMatchingRightBrace(
            source: source,
            tokens: tokens,
            openingTokenID: applicationTokenID + 2,
            limitTokenID: tokens.kinds.count
        ))
        if closing < 0 { return 0 - 1 }
        return closing + 1
    }
    return applicationTokenID + 2
}

function compilerBParseMacroApplicationChain(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let applications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let chainStart: Int
): Int {
    state targetTokenID: Int(chainStart)
    while targetTokenID < tokens.kinds.count
        && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: targetTokenID) == 64 {
        targetTokenID: compilerBParserMacroApplicationAfterLast(
            source: source,
            tokens: tokens,
            applicationTokenID: targetTokenID
        )
        if targetTokenID < 0 { return 0 - 2 }
    }
    if targetTokenID + 1 >= tokens.kinds.count { return 0 - 7 }
    let target: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: targetTokenID)
    ))
    if target.kind != .identity
        || (!compilerBTokenMatchesBytes(source: source, token: target, expected: "construct")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "enum")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "function")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "macro")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "let")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "state")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "derived")
            && !compilerBTokenMatchesBytes(source: source, token: target, expected: "binding")) {
        return 0 - 7
    }

    state applicationTokenID: Int(chainStart)
    state ordinal: Int(0)
    while applicationTokenID < targetTokenID {
        let application: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: applicationTokenID)
        ))
        let afterLast: Int(compilerBParserMacroApplicationAfterLast(
            source: source,
            tokens: tokens,
            applicationTokenID: applicationTokenID
        ))
        state firstArgumentTokenID: Int(applicationTokenID + 2)
        state afterLastArgumentTokenID: Int(applicationTokenID + 2)
        if applicationTokenID + 2 < tokens.kinds.count
            && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: applicationTokenID + 2) == 40 {
            firstArgumentTokenID: applicationTokenID + 3
            afterLastArgumentTokenID: afterLast - 1
        }
        state hasTemplateBody: Bool(false)
        if applicationTokenID + 2 < tokens.kinds.count
            && compilerBTokenMatchesBytes(
                source: source,
                token: compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: applicationTokenID + 1)
                ),
                expected: "syntax"
            )
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: applicationTokenID + 2
            ) == 123 {
            firstArgumentTokenID: applicationTokenID + 3
            afterLastArgumentTokenID: afterLast - 1
            hasTemplateBody: true
        }
        if syntaxNodeStoreAppend(
            store: nodes,
            id: application.start,
            firstTokenID: applicationTokenID,
            afterLastTokenID: afterLast
        ) != 0 { return 0 - 3 }
        if compilerBMacroApplicationStoreAppend(
            store: applications,
            syntaxID: application.start,
            identityTokenID: applicationTokenID + 1,
            firstArgumentTokenID: firstArgumentTokenID,
            afterLastArgumentTokenID: afterLastArgumentTokenID,
            targetSyntaxID: target.start,
            targetIdentityTokenID: targetTokenID + 1,
            ordinal: ordinal
        ) != 0 { return 0 - 3 }
        if firstArgumentTokenID < afterLastArgumentTokenID && !hasTemplateBody {
            if compilerBParseMacroArgumentFacets(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                arguments: applications,
                applicationSyntaxID: application.start,
                firstTokenID: firstArgumentTokenID,
                afterLastTokenID: afterLastArgumentTokenID
            ) != 0 { return 0 - 3 }
        }
        applicationTokenID: afterLast
        ordinal: ordinal + 1
    }
    return targetTokenID
}

function compilerBParserIsMemberKeyword(
    let source: String,
    let token: CompilerBToken
): Bool {
    if token.kind != .identity { return false }
    if compilerBTokenMatchesBytes(source: source, token: token, expected: "let")
        || compilerBTokenMatchesBytes(source: source, token: token, expected: "state")
        || compilerBTokenMatchesBytes(source: source, token: token, expected: "derived")
        || compilerBTokenMatchesBytes(source: source, token: token, expected: "binding") {
        return true
    }
    state cursor: Int(token.end)
    while cursor < source.count
        && compilerBIsWhitespace(byte: source.byte(index: cursor)) {
        cursor: cursor + 1
    }
    if cursor >= source.count
        || !compilerBIsLetter(byte: source.byte(index: cursor)) {
        return false
    }
    cursor: cursor + 1
    while cursor < source.count
        && (compilerBIsLetter(byte: source.byte(index: cursor))
            || compilerBIsDigit(byte: source.byte(index: cursor))) {
        cursor: cursor + 1
    }
    while cursor < source.count
        && compilerBIsWhitespace(byte: source.byte(index: cursor)) {
        cursor: cursor + 1
    }
    return cursor < source.count && source.byte(index: cursor) == 58
}

function compilerBParseDerivedBodyExecutions(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let closingTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    while tokenID < closingTokenID {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        if token.kind == .identity && compilerBParserCallStartsAt(
            source: source,
            tokens: tokens,
            firstTokenID: tokenID,
            limitTokenID: closingTokenID
        ) {
            let afterExecutionTokenID: Int(compilerBParseExecutionProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                firstTokenID: tokenID,
                limitTokenID: closingTokenID
            ))
            if afterExecutionTokenID < 0 { return 0 - 1 }
            tokenID: afterExecutionTokenID
        } else {
            tokenID: tokenID + 1
        }
    }
    return 0
}

function compilerBParseDerivedBodyMemberFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let memberIndex: Int
): Int {
    let keyword: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: memberIndex)
    ))
    let firstTypeTokenID: Int(memberIndex + 3)
    state typeTokenID: Int(firstTypeTokenID)
    state selectorIdentityTokenID: Int(0 - 1)
    if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: typeTokenID
    ) == 64 {
        selectorIdentityTokenID: typeTokenID + 1
        typeTokenID: typeTokenID + 1
    }
    let typeToken: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: typeTokenID)
    ))
    if typeToken.kind != .identity { return 0 - 1 }
    let openingTokenID: Int(typeTokenID + 1)
    if openingTokenID >= tokens.kinds.count
        || compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: openingTokenID
        ) != 123 { return 0 - 1 }
    let closingTokenID: Int(compilerBParserMatchingRightBrace(
        source: source,
        tokens: tokens,
        openingTokenID: openingTokenID,
        limitTokenID: tokens.kinds.count
    ))
    if closingTokenID < 0 { return 0 - 2 }
    let opening: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: openingTokenID)
    ))
    if syntaxNodeStoreAppend(
        store: nodes,
        id: keyword.start,
        firstTokenID: memberIndex,
        afterLastTokenID: closingTokenID + 1
    ) != 0 { return 0 - 3 }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: opening.start,
        firstTokenID: openingTokenID,
        afterLastTokenID: closingTokenID + 1
    ) != 0 { return 0 - 3 }
    if compilerBMemberFacetStoreAppend(
        store: members,
        syntaxID: keyword.start,
        ownerSyntaxID: ownerSyntaxID,
        kindCode: compilerBParserMemberKindDerivedCode(),
        keywordTokenID: memberIndex,
        identityTokenID: memberIndex + 1,
        typeTokenID: typeTokenID,
        firstInitializerTokenID: openingTokenID + 1,
        afterLastInitializerTokenID: closingTokenID,
        initializerExpressionSyntaxID: 0 - 1,
        bodySyntaxID: opening.start,
        initializerKindCode: compilerBParserScalarKindNoneCode(),
        initializerValue: 0
    ) != 0 { return 0 - 3 }
    if process.processes.ownerSyntaxIDs.append(element: keyword.start) != 0 {
        return 0 - 3
    }
    if process.processes.bodySyntaxIDs.append(element: opening.start) != 0 {
        return 0 - 3
    }
    if process.processes.resultExpressionSyntaxIDs.append(element: 0 - 1) != 0 {
        return 0 - 3
    }
    if selectorIdentityTokenID >= 0 {
        if process.collections.syntaxIDs.append(element: keyword.start) != 0 {
            return 0 - 3
        }
        if process.collections.selectorIdentityTokenIDs.append(
            element: selectorIdentityTokenID
        ) != 0 { return 0 - 3 }
        if process.collections.selectorMacroSyntaxIDs.append(element: 0 - 1) != 0 {
            return 0 - 3
        }
    }
    if compilerBParseDerivedBodyExecutions(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        ownerSyntaxID: keyword.start,
        openingTokenID: openingTokenID,
        closingTokenID: closingTokenID
    ) != 0 { return 0 - 3 }
    return closingTokenID + 1
}

function compilerBParseMemberFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let memberIndex: Int
): Int {
    if memberIndex + 3 >= tokens.kinds.count { return 0 - 1 }
    let keyword: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: memberIndex)
    ))
    let identityTokenID: Int(memberIndex + 1)
    let colonTokenID: Int(memberIndex + 2)
    let identity: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: identityTokenID)
    ))
    if !compilerBParserIsMemberKeyword(source: source, token: keyword)
        || identity.kind != .identity { return 0 - 1 }
    if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: colonTokenID) != 58 {
        return 0 - 1
    }
    let isDerived: Bool(compilerBTokenMatchesBytes(
        source: source,
        token: keyword,
        expected: "derived"
    ))
    if isDerived {
        state isCollectionProduction: Bool(false)
        if memberIndex + 6 < tokens.kinds.count {
            if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: memberIndex + 4
            ) == 45 {
                if compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: memberIndex + 5
                ) == 62 {
                    isCollectionProduction: true
                }
            }
        }
        if isCollectionProduction {
            let sourceIdentity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: memberIndex + 3)
            ))
            let outputIdentity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: memberIndex + 6)
            ))
            if sourceIdentity.kind != .identity { return 0 - 1 }
            if outputIdentity.kind != .identity { return 0 - 1 }
            if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: memberIndex + 4
            ) != 45 { return 0 - 1 }
            if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: memberIndex + 5
            ) != 62 { return 0 - 1 }
            if syntaxNodeStoreAppend(
                store: nodes,
                id: keyword.start,
                firstTokenID: memberIndex,
                afterLastTokenID: memberIndex + 7
            ) != 0 { return 0 - 3 }
            if compilerBMemberFacetStoreAppend(
                store: members,
                syntaxID: keyword.start,
                ownerSyntaxID: ownerSyntaxID,
                kindCode: compilerBParserMemberKindDerivedCode(),
                keywordTokenID: memberIndex,
                identityTokenID: memberIndex + 1,
                typeTokenID: memberIndex + 6,
                firstInitializerTokenID: memberIndex + 3,
                afterLastInitializerTokenID: memberIndex + 7,
                initializerExpressionSyntaxID: 0 - 1,
                bodySyntaxID: 0 - 1,
                initializerKindCode: compilerBParserScalarKindNoneCode(),
                initializerValue: 0
            ) != 0 { return 0 - 3 }
            if process.collectionProductions.syntaxIDs.append(element: keyword.start) != 0 {
                return 0 - 3
            }
            if process.collectionProductions.sourceIdentityTokenIDs.append(
                element: memberIndex + 3
            ) != 0 { return 0 - 3 }
            if process.collectionProductions.sourceCollectionSyntaxIDs.append(
                element: 0 - 1
            ) != 0 { return 0 - 3 }
            if process.collectionProductions.outputIdentityTokenIDs.append(
                element: memberIndex + 6
            ) != 0 { return 0 - 3 }
            if process.collectionProductions.outputDeclarationSyntaxIDs.append(
                element: 0 - 1
            ) != 0 { return 0 - 3 }
            return memberIndex + 7
        }
    }

    if isDerived && memberIndex + 4 < tokens.kinds.count {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: memberIndex + 4
        ) == 123 {
            return compilerBParseDerivedBodyMemberFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                members: members,
                ownerSyntaxID: ownerSyntaxID,
                memberIndex: memberIndex
            )
        }
    }

    let firstTypeTokenID: Int(colonTokenID + 1)
    if firstTypeTokenID >= tokens.kinds.count { return 0 - 1 }
    let firstTypeToken: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: firstTypeTokenID)
    ))
    if firstTypeToken.kind == .rightDelimiter {
        state emptyKindCode: Int(compilerBParserMemberKindConcreteCode())
        if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "let") {
            emptyKindCode: compilerBParserMemberKindLetCode()
        }
        if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "state") {
            emptyKindCode: compilerBParserMemberKindStateCode()
        }
        if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "binding") {
            emptyKindCode: compilerBParserMemberKindBindingCode()
        }
        if syntaxNodeStoreAppend(
            store: nodes,
            id: keyword.start,
            firstTokenID: memberIndex,
            afterLastTokenID: firstTypeTokenID
        ) != 0 { return 0 - 3 }
        if compilerBMemberFacetStoreAppend(
            store: members,
            syntaxID: keyword.start,
            ownerSyntaxID: ownerSyntaxID,
            kindCode: emptyKindCode,
            keywordTokenID: memberIndex,
            identityTokenID: identityTokenID,
            typeTokenID: 0 - 1,
            firstInitializerTokenID: firstTypeTokenID,
            afterLastInitializerTokenID: firstTypeTokenID,
            initializerExpressionSyntaxID: 0 - 1,
            bodySyntaxID: 0 - 1,
            initializerKindCode: compilerBParserScalarKindNoneCode(),
            initializerValue: 0
        ) != 0 { return 0 - 3 }
        return firstTypeTokenID
    }
    state typeTokenID: Int(firstTypeTokenID)
    state afterLastTypeTokenID: Int(firstTypeTokenID + 1)
    state selectorIdentityTokenID: Int(0 - 1)
    if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: typeTokenID
    ) == 40 {
        let closingParameterTokenID: Int(compilerBParserMatchingRightParenthesis(
            source: source,
            tokens: tokens,
            openingTokenID: typeTokenID
        ))
        if closingParameterTokenID < 0 { return 0 - 1 }
        if closingParameterTokenID + 3 >= tokens.kinds.count { return 0 - 1 }
        if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: closingParameterTokenID + 1
            ) != 45 { return 0 - 1 }
        if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: closingParameterTokenID + 2
            ) != 62 { return 0 - 1 }
        typeTokenID: closingParameterTokenID + 3
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: typeTokenID
        ) == 64 {
            typeTokenID: typeTokenID + 1
        }
        afterLastTypeTokenID: typeTokenID + 1
    } else if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: typeTokenID
    ) == 64 {
        selectorIdentityTokenID: typeTokenID + 1
        typeTokenID: typeTokenID + 1
        afterLastTypeTokenID: typeTokenID + 1
    }
    let typeToken: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: typeTokenID)
    ))
    let directValue: Bool(
        typeToken.kind == .string || typeToken.kind == .integer
    )
    if typeToken.kind != .identity && !directValue { return 0 - 1 }
    if typeTokenID + 1 < tokens.kinds.count {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: typeTokenID + 1
        ) == 60 {
            let closing: Int(compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: typeTokenID + 1,
                limitTokenID: tokens.kinds.count
            ))
            if closing < 0 { return 0 - 18 }
            afterLastTypeTokenID: closing + 1
        }
    }

    state firstInitializerTokenID: Int(afterLastTypeTokenID)
    state afterLastInitializerTokenID: Int(afterLastTypeTokenID)
    state afterLastMemberTokenID: Int(afterLastTypeTokenID)
    state constructionBodySyntaxID: Int(0 - 1)
    if directValue {
        firstInitializerTokenID: typeTokenID
        afterLastInitializerTokenID: typeTokenID + 1
        afterLastMemberTokenID: typeTokenID + 1
    } else if afterLastTypeTokenID < tokens.kinds.count {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: afterLastTypeTokenID
        ) == 40 {
            let closing: Int(compilerBParserMatchingRightParenthesis(
                source: source,
                tokens: tokens,
                openingTokenID: afterLastTypeTokenID
            ))
            if closing < 0 { return 0 - 2 }
            firstInitializerTokenID: afterLastTypeTokenID + 1
            afterLastInitializerTokenID: closing
            afterLastMemberTokenID: closing + 1
        }
    }
    if afterLastMemberTokenID < tokens.kinds.count {
        if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: afterLastMemberTokenID
            ) == 123 {
            let constructionBodyClosingTokenID: Int(
                compilerBParserMatchingRightBrace(
                    source: source,
                    tokens: tokens,
                    openingTokenID: afterLastMemberTokenID,
                    limitTokenID: tokens.kinds.count
                )
            )
            if constructionBodyClosingTokenID < 0 { return 0 - 2 }
            constructionBodySyntaxID: compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: afterLastMemberTokenID)
            ).start
            afterLastMemberTokenID: constructionBodyClosingTokenID + 1
        }
    }
    state initializerKindCode: Int(compilerBParserScalarKindNoneCode())
    state initializerValue: Int(0)
    if afterLastInitializerTokenID == firstInitializerTokenID + 1 {
        let initializer: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: firstInitializerTokenID)
        ))
        if initializer.kind == .integer {
            initializerKindCode: compilerBParserScalarKindIntegerCode()
            initializerValue: compilerBParserDecimalTokenValue(
                source: source,
                token: initializer
            )
        } else if initializer.kind == .identity
            && compilerBTokenMatchesBytes(source: source, token: initializer, expected: "true") {
            initializerKindCode: compilerBParserScalarKindBooleanCode()
            initializerValue: 1
        } else if initializer.kind == .identity
            && compilerBTokenMatchesBytes(source: source, token: initializer, expected: "false") {
            initializerKindCode: compilerBParserScalarKindBooleanCode()
        }
    }
    state initializerExpressionSyntaxID: Int(0 - 1)
    if firstInitializerTokenID < afterLastInitializerTokenID
        || afterLastMemberTokenID > afterLastTypeTokenID && constructionBodySyntaxID < 0 {
        state expressionFirstTokenID: Int(firstInitializerTokenID)
        state expressionAfterLastTokenID: Int(afterLastInitializerTokenID)
        if afterLastTypeTokenID < tokens.kinds.count {
            if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: afterLastTypeTokenID
            ) == 40 {
                expressionFirstTokenID: typeTokenID
                expressionAfterLastTokenID: afterLastMemberTokenID
            }
        }
        initializerExpressionSyntaxID: compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: expressionFirstTokenID,
            afterLastTokenID: expressionAfterLastTokenID
        )
        if initializerExpressionSyntaxID < 0 { return 0 - 3 }
    }
    state kindCode: Int(compilerBParserMemberKindConcreteCode())
    if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "let") {
        kindCode: compilerBParserMemberKindLetCode()
    }
    if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "state") {
        kindCode: compilerBParserMemberKindStateCode()
    }
    if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "derived") {
        kindCode: compilerBParserMemberKindDerivedCode()
    }
    if compilerBTokenMatchesBytes(source: source, token: keyword, expected: "binding") {
        kindCode: compilerBParserMemberKindBindingCode()
    }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: keyword.start,
        firstTokenID: memberIndex,
        afterLastTokenID: afterLastMemberTokenID
    ) != 0 { return 0 - 3 }
    if compilerBMemberFacetStoreAppend(
        store: members,
        syntaxID: keyword.start,
        ownerSyntaxID: ownerSyntaxID,
        kindCode: kindCode,
        keywordTokenID: memberIndex,
        identityTokenID: identityTokenID,
        typeTokenID: typeTokenID,
        firstInitializerTokenID: firstInitializerTokenID,
        afterLastInitializerTokenID: afterLastInitializerTokenID,
        initializerExpressionSyntaxID: initializerExpressionSyntaxID,
        bodySyntaxID: constructionBodySyntaxID,
        initializerKindCode: initializerKindCode,
        initializerValue: initializerValue
    ) != 0 { return 0 - 3 }
    // Keep the complete RHS independently of the legacy initializer/type split.
    // Bare names remain unresolved references; even empty calls retain () here.
    if firstTypeTokenID == typeTokenID && afterLastMemberTokenID == afterLastTypeTokenID
        && typeToken.kind == .identity {
        // The same name expression represents a construct or a value member.
        // Resolution, rather than this parser, determines which it references.
        if compilerBParseExpressionFacet(
            source: source, tokens: tokens, nodes: nodes, process: process,
            firstTokenID: firstTypeTokenID, afterLastTokenID: afterLastMemberTokenID
        ) < 0 { return 0 - 3 }
    }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: firstTypeToken.start,
        firstTokenID: firstTypeTokenID,
        afterLastTokenID: afterLastMemberTokenID
    ) != 0 { return 0 - 3 }
    if members.rhsSyntaxIDs.update(
        element: firstTypeToken.start,
        index: members.syntaxIDs.count - 1
    ) != 0 { return 0 - 3 }
    if selectorIdentityTokenID >= 0 {
        if process.collections.syntaxIDs.append(element: keyword.start) != 0 {
            return 0 - 3
        }
        if process.collections.selectorIdentityTokenIDs.append(
            element: selectorIdentityTokenID
        ) != 0 { return 0 - 3 }
        if process.collections.selectorMacroSyntaxIDs.append(element: 0 - 1) != 0 {
            return 0 - 3
        }
    }
    return afterLastMemberTokenID
}

function compilerBParseOwnedMemberFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let applications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state braceDepth: Int(0)
    state tokenID: Int(openingTokenID + 1)
    while tokenID < afterLastTokenID - 1 {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        let byte: Int(compilerBParserTokenByte(source: source, tokens: tokens, tokenID: tokenID))
        if braceDepth == 0 && byte == 64 {
            let afterApplicationTokenID: Int(compilerBParserMacroApplicationAfterLast(
                source: source,
                tokens: tokens,
                applicationTokenID: tokenID
            ))
            if afterApplicationTokenID < 0 { return afterApplicationTokenID }
            state hasMemberTarget: Bool(false)
            if afterApplicationTokenID < afterLastTokenID - 1 {
                let possibleTarget: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: afterApplicationTokenID)
                ))
                hasMemberTarget: compilerBParserIsMemberKeyword(
                    source: source,
                    token: possibleTarget
                )
            }
            if hasMemberTarget {
                let targetTokenID: Int(compilerBParseMacroApplicationChain(
                    source: source,
                    tokens: tokens,
                    nodes: nodes,
                    applications: applications,
                    process: process,
                    chainStart: tokenID
                ))
                if targetTokenID < 0 { return targetTokenID }
                let afterMember: Int(compilerBParseMemberFacet(
                        source: source,
                        tokens: tokens,
                        nodes: nodes,
                        process: process,
                        members: members,
                        ownerSyntaxID: ownerSyntaxID,
                        memberIndex: targetTokenID
                    ))
                if afterMember < 0 { return afterMember }
                tokenID: afterMember
            } else {
                let application: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: tokenID)
                ))
                state firstArgumentTokenID: Int(tokenID + 2)
                state afterLastArgumentTokenID: Int(tokenID + 2)
                if tokenID + 2 < tokens.kinds.count {
                    if compilerBParserTokenByte(
                        source: source,
                        tokens: tokens,
                        tokenID: tokenID + 2
                    ) == 40 {
                        firstArgumentTokenID: tokenID + 3
                        afterLastArgumentTokenID: afterApplicationTokenID - 1
                    }
                }
                if syntaxNodeStoreAppend(
                    store: nodes,
                    id: application.start,
                    firstTokenID: tokenID,
                    afterLastTokenID: afterApplicationTokenID
                ) != 0 { return 0 - 3 }
                if compilerBMacroApplicationStoreAppend(
                    store: applications,
                    syntaxID: application.start,
                    identityTokenID: tokenID + 1,
                    firstArgumentTokenID: firstArgumentTokenID,
                    afterLastArgumentTokenID: afterLastArgumentTokenID,
                    targetSyntaxID: ownerSyntaxID,
                    targetIdentityTokenID: 0 - 1,
                    ordinal: 0
                ) != 0 { return 0 - 3 }
                if firstArgumentTokenID < afterLastArgumentTokenID {
                    if compilerBParseMacroArgumentFacets(
                        source: source,
                        tokens: tokens,
                        nodes: nodes,
                        process: process,
                        arguments: applications,
                        applicationSyntaxID: application.start,
                        firstTokenID: firstArgumentTokenID,
                        afterLastTokenID: afterLastArgumentTokenID
                    ) != 0 { return 0 - 3 }
                }
                tokenID: afterApplicationTokenID
            }
        } else if braceDepth == 0 && compilerBParserIsMemberKeyword(source: source, token: token) {
            let afterMember: Int(compilerBParseMemberFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                members: members,
                ownerSyntaxID: ownerSyntaxID,
                memberIndex: tokenID
            ))
            if afterMember < 0 { return afterMember }
            tokenID: afterMember
        } else {
            if byte == 123 { braceDepth: braceDepth + 1 }
            if byte == 125 { braceDepth: braceDepth - 1 }
            tokenID: tokenID + 1
        }
    }
    return 0
}

function compilerBDeclarationSyntaxIDMatchingIdentity(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let declarations: CompilerBDeclarationFacetStore,
    let identityTokenID: Int
): Int {
    let identity: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: identityTokenID)
    ))
    state result: Int(0 - 1)
    state count: Int(0)
    state row: Int(0)
    while row < declarations.syntaxIDs.count {
        let candidate: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: declarations.identityTokenIDs.element(index: row))
        ))
        if compilerBTokenBytesEqual(source: source, left: identity, right: candidate) {
            result: declarations.syntaxIDs.element(index: row)
            count: count + 1
        }
        row: row + 1
    }
    if count != 1 { return 0 - count - 1 }
    return result
}

function compilerBResolveCollectionProductions(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let macros: CompilerBDeclarationFacetStore,
    let constructs: CompilerBDeclarationFacetStore
): Int {
    state collectionRow: Int(0)
    while collectionRow < process.collections.syntaxIDs.count {
        let macroSyntaxID: Int(compilerBDeclarationSyntaxIDMatchingIdentity(
            source: source,
            tokens: tokens,
            declarations: macros,
            identityTokenID: process.collections.selectorIdentityTokenIDs.element(
                index: collectionRow
            )
        ))
        if macroSyntaxID < 0 { return macroSyntaxID }
        if process.collections.selectorMacroSyntaxIDs.update(
            element: macroSyntaxID,
            index: collectionRow
        ) != 0 { return 0 - 1 }
        collectionRow: collectionRow + 1
    }

    state productionRow: Int(0)
    while productionRow < process.collectionProductions.syntaxIDs.count {
        let sourceIdentity: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: process.collectionProductions.sourceIdentityTokenIDs.element(
                index: productionRow
            ))
        ))
        state sourceSyntaxID: Int(0 - 1)
        state sourceCount: Int(0)
        collectionRow: 0
        while collectionRow < process.collections.syntaxIDs.count {
            let memberRow: Int(members.syntaxIDs.firstIndex(value: process.collections.syntaxIDs.element(index: collectionRow)))
            if memberRow >= 0 {
                let candidate: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: members.identityTokenIDs.element(index: memberRow))
                ))
                if compilerBTokenBytesEqual(
                    source: source,
                    left: sourceIdentity,
                    right: candidate
                ) {
                    sourceSyntaxID: process.collections.syntaxIDs.element(index: collectionRow)
                    sourceCount: sourceCount + 1
                }
            }
            collectionRow: collectionRow + 1
        }
        if sourceCount != 1 { return 0 - sourceCount - 1 }
        let sourceCollectionRow: Int(process.collections.syntaxIDs.firstIndex(value: sourceSyntaxID))
        let selectorMacroSyntaxID: Int(
            process.collections.selectorMacroSyntaxIDs.element(index: sourceCollectionRow)
        )
        let outputIdentity: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: process.collectionProductions.outputIdentityTokenIDs.element(
                index: productionRow
            ))
        ))
        state emissionCount: Int(0)
        state environmentRow: Int(0)
        while environmentRow < process.environments.syntaxIDs.count {
            if process.environments.ownerDeclarationSyntaxIDs.element(index: environmentRow)
                == selectorMacroSyntaxID {
                let environmentSyntaxID: Int(
                    process.environments.syntaxIDs.element(index: environmentRow)
                )
                state nodeRow: Int(0)
                while nodeRow < process.environmentNodes.syntaxIDs.count {
                    if process.environmentNodes.environmentSyntaxIDs.element(index: nodeRow)
                        == environmentSyntaxID {
                        let containedSyntaxID: Int(
                            process.environmentNodes.syntaxIDs.element(index: nodeRow)
                        )
                let emittedApplicationRow: Int(process.applications.syntaxIDs.firstIndex(value: containedSyntaxID))
                if emittedApplicationRow >= 0 {
                    let emittedIdentity: CompilerBToken(compilerBRetainedToken(
                        store: tokens,
                        id: TokenID(value: process.applications.identityTokenIDs.element(
                            index: emittedApplicationRow
                        ))
                    ))
                    if compilerBTokenBytesEqual(
                        source: source,
                        left: emittedIdentity,
                        right: outputIdentity
                    ) { emissionCount: emissionCount + 1 }
                }
                    }
                    nodeRow: nodeRow + 1
                }
            }
            environmentRow: environmentRow + 1
        }
        if emissionCount == 0 { return 0 - 1 }
        let outputSyntaxID: Int(compilerBDeclarationSyntaxIDMatchingIdentity(
            source: source,
            tokens: tokens,
            declarations: constructs,
            identityTokenID: process.collectionProductions.outputIdentityTokenIDs.element(
                index: productionRow
            )
        ))
        if outputSyntaxID < 0 { return outputSyntaxID }
        if process.collectionProductions.sourceCollectionSyntaxIDs.update(
            element: sourceSyntaxID,
            index: productionRow
        ) != 0 { return 0 - 1 }
        if process.collectionProductions.outputDeclarationSyntaxIDs.update(
            element: outputSyntaxID,
            index: productionRow
        ) != 0 { return 0 - 1 }
        productionRow: productionRow + 1
    }
    return 0
}

function syntaxNodeRowForID(let store: SyntaxNodeStore, let syntaxID: Int): Int {
    return store.ids.firstIndex(value: syntaxID)
}

function compilerBParseLeafExpressionFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    if firstTokenID < 0 || firstTokenID >= afterLastTokenID { return 0 - 1 }
    let first: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: firstTokenID)
    ))
    let syntaxID: Int(first.start)
    state kindCode: Int(compilerBParserExpressionKindUnknownCode())
    state integerValue: Int(0)
    if first.kind == .integer && afterLastTokenID == firstTokenID + 1 {
        kindCode: compilerBParserExpressionKindIntegerCode()
        integerValue: compilerBParserDecimalTokenValue(source: source, token: first)
    } else if afterLastTokenID == firstTokenID + 2
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: firstTokenID
        ) == 45 {
        let magnitude: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: firstTokenID + 1)
        ))
        if magnitude.kind == .integer {
            kindCode: compilerBParserExpressionKindIntegerCode()
            integerValue: 0 - compilerBParserDecimalTokenValue(
                source: source,
                token: magnitude
            )
        }
    } else if first.kind == .identity && afterLastTokenID == firstTokenID + 1
        && (compilerBTokenMatchesBytes(source: source, token: first, expected: "true")
            || compilerBTokenMatchesBytes(source: source, token: first, expected: "false")) {
        kindCode: compilerBParserExpressionKindBooleanCode()
        if compilerBTokenMatchesBytes(source: source, token: first, expected: "true") {
            integerValue: 1
        }
    } else if first.kind == .string && afterLastTokenID == firstTokenID + 1 {
        kindCode: compilerBParserExpressionKindStringCode()
    } else if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: firstTokenID
    ) == 35 {
        kindCode: compilerBParserExpressionKindProjectionCode()
    }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: syntaxID,
        firstTokenID: firstTokenID,
        afterLastTokenID: afterLastTokenID
    ) != 0 { return 0 - 61 }
    if compilerBExpressionFacetStoreAppend(
        store: process.expressions,
        syntaxID: syntaxID,
        kindCode: kindCode,
        firstTokenID: firstTokenID,
        afterLastTokenID: afterLastTokenID,
        integerValue: integerValue
    ) != 0 { return 0 - 62 }

    if kindCode == compilerBParserExpressionKindProjectionCode() {
        state ordinal: Int(0)
        state cursor: Int(firstTokenID + 1)
        state identityTokenID: Int(0 - 1)
        while cursor < afterLastTokenID {
            let segment: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: cursor)
            ))
            if segment.kind == .identity {
                if process.projections.expressionSyntaxIDs.append(element: syntaxID) != 0 {
                    return 0 - 1
                }
                if process.projections.tokenIDs.append(element: cursor) != 0 { return 0 - 1 }
                if process.projections.ordinals.append(element: ordinal) != 0 { return 0 - 1 }
                identityTokenID: cursor
                ordinal: ordinal + 1
            }
            cursor: cursor + 1
        }
        if compilerBApplicationFacetStoreAppend(
            store: process.applications,
            syntaxID: syntaxID,
            predecessorSyntaxID: 0 - 1,
            identityTokenID: identityTokenID,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        ) != 0 { return 0 - 1 }
    }
    return syntaxID
}

function compilerBParseMacroArgumentFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let arguments: CompilerBMacroApplicationStore,
    let applicationSyntaxID: Int,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state ordinal: Int(0)
    state cursor: Int(firstTokenID)
    while cursor < afterLastTokenID {
        let candidate: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: cursor)
        ))
        state labelTokenID: Int(0 - 1)
        state valueFirstTokenID: Int(cursor)
        if candidate.kind == .identity && cursor + 2 < afterLastTokenID
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: cursor + 1
            ) == 58 {
            labelTokenID: cursor
            valueFirstTokenID: cursor + 2
        }
        state valueAfterLastTokenID: Int(valueFirstTokenID)
        state depth: Int(0)
        state foundSeparator: Bool(false)
        while valueAfterLastTokenID < afterLastTokenID && !foundSeparator {
            let byte: Int(compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: valueAfterLastTokenID
            ))
            if byte == 40 { depth: depth + 1 }
            if byte == 41 { depth: depth - 1 }
            if byte == 44 && depth == 0 {
                foundSeparator: true
            } else {
                valueAfterLastTokenID: valueAfterLastTokenID + 1
            }
        }
        let valueSyntaxID: Int(compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: valueFirstTokenID,
            afterLastTokenID: valueAfterLastTokenID
        ))
        if valueSyntaxID < 0 { return valueSyntaxID }
        if compilerBMacroArgumentFacetStoreAppend(
            store: arguments,
            applicationSyntaxID: applicationSyntaxID,
            labelTokenID: labelTokenID,
            valueExpressionSyntaxID: valueSyntaxID,
            ordinal: ordinal
        ) != 0 { return 0 - 1 }
        ordinal: ordinal + 1
        cursor: valueAfterLastTokenID + 1
    }
    return 0
}

function compilerBApplicationFacetStoreAppend(
    let store: CompilerBApplicationFacetStore,
    let syntaxID: Int,
    let predecessorSyntaxID: Int,
    let identityTokenID: Int,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state row: Int(0)
    while row < store.syntaxIDs.count {
        if store.syntaxIDs.element(index: row) == syntaxID { return 0 }
        row: row + 1
    }
    if store.syntaxIDs.append(element: syntaxID) != 0 { return 0 - 1 }
    if store.predecessorSyntaxIDs.append(element: predecessorSyntaxID) != 0 {
        return 0 - 1
    }
    if store.declarationSyntaxIDs.append(element: 0 - 1) != 0 { return 0 - 1 }
    if store.identityTokenIDs.append(element: identityTokenID) != 0 { return 0 - 1 }
    if store.firstTokenIDs.append(element: firstTokenID) != 0 { return 0 - 1 }
    return store.afterLastTokenIDs.append(element: afterLastTokenID)
}

function compilerBApplicationArgumentFacetStoreAppend(
    let store: CompilerBApplicationArgumentFacetStore,
    let applicationSyntaxID: Int,
    let labelTokenID: Int,
    let valueExpressionSyntaxID: Int,
    let ordinal: Int
): Int {
    state row: Int(0)
    while row < store.applicationSyntaxIDs.count {
        if store.applicationSyntaxIDs.element(index: row) == applicationSyntaxID
            && store.ordinals.element(index: row) == ordinal {
            if store.labelTokenIDs.element(index: row) != labelTokenID
                || store.valueExpressionSyntaxIDs.element(index: row)
                    != valueExpressionSyntaxID {
                return 0 - 1
            }
            return 0
        }
        row: row + 1
    }
    if store.applicationSyntaxIDs.append(element: applicationSyntaxID) != 0 {
        return 0 - 1
    }
    if store.labelTokenIDs.append(element: labelTokenID) != 0 { return 0 - 1 }
    if store.valueExpressionSyntaxIDs.append(element: valueExpressionSyntaxID) != 0 {
        return 0 - 1
    }
    return store.ordinals.append(element: ordinal)
}

function compilerBParseStructuredExpressionFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    if firstTokenID < 0 || firstTokenID >= afterLastTokenID { return 0 - 1 }
    if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: firstTokenID
    ) == 40 {
        let closing: Int(compilerBParserMatchingRightParenthesis(
            source: source,
            tokens: tokens,
            openingTokenID: firstTokenID
        ))
        if closing == afterLastTokenID - 1 {
            return compilerBParseExpressionFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                firstTokenID: firstTokenID + 1,
                afterLastTokenID: closing
            )
        }
    }
    state operatorTokenID: Int(0 - 1)
    state operatorCode: Int(0)
    state depth: Int(0)
    state tokenID: Int(firstTokenID)
    while tokenID + 1 < afterLastTokenID && operatorTokenID < 0 {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: tokenID
        ))
        if compilerBParserStartsGenericArguments(source: source, tokens: tokens, openingTokenID: tokenID) {
            tokenID: compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: tokenID, limitTokenID: afterLastTokenID
            )
            if tokenID < 0 { return 0 - 18 }
        } else {
            if byte == 40 || byte == 91 || byte == 123 { depth: depth + 1 }
            if byte == 41 || byte == 93 || byte == 125 { depth: depth - 1 }
            if depth == 0 {
                let nextByte: Int(compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: tokenID + 1
                ))
                if byte == 124 && nextByte == 124 {
                    operatorTokenID: tokenID
                    operatorCode: compilerBLogicalOperatorOrCode()
                }
            }
        }
        tokenID: tokenID + 1
    }
    if operatorTokenID < 0 {
        depth: 0
        tokenID: firstTokenID
        while tokenID + 1 < afterLastTokenID && operatorTokenID < 0 {
            let byte: Int(compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: tokenID
            ))
            if compilerBParserStartsGenericArguments(source: source, tokens: tokens, openingTokenID: tokenID) {
                tokenID: compilerBParserMatchingRightAngle(
                    source: source, tokens: tokens, openingTokenID: tokenID, limitTokenID: afterLastTokenID
                )
                if tokenID < 0 { return 0 - 18 }
            } else {
                if byte == 40 || byte == 91 || byte == 123 { depth: depth + 1 }
                if byte == 41 || byte == 93 || byte == 125 { depth: depth - 1 }
                if depth == 0 {
                    let nextByte: Int(compilerBParserTokenByte(
                        source: source,
                        tokens: tokens,
                        tokenID: tokenID + 1
                    ))
                    if byte == 38 && nextByte == 38 {
                        operatorTokenID: tokenID
                        operatorCode: compilerBLogicalOperatorAndCode()
                    }
                }
            }
            tokenID: tokenID + 1
        }
    }
    if operatorTokenID >= 0 {
        let leftSyntaxID: Int(compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID,
            afterLastTokenID: operatorTokenID
        ))
        let rightSyntaxID: Int(compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: operatorTokenID + 2,
            afterLastTokenID: afterLastTokenID
        ))
        if leftSyntaxID < 0 || rightSyntaxID < 0 { return 0 - 1 }
        let operatorToken: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: operatorTokenID)
        ))
        let syntaxID: Int(operatorToken.start)
        if syntaxNodeStoreAppend(
            store: nodes,
            id: syntaxID,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        ) != 0 || compilerBExpressionFacetStoreAppend(
            store: process.expressions,
            syntaxID: syntaxID,
            kindCode: compilerBParserExpressionKindLogicalCode(),
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID,
            integerValue: 0
        ) != 0 { return 0 - 1 }
        if process.logicals.syntaxIDs.append(element: syntaxID) != 0 {
            return 0 - 1
        }
        if process.logicals.leftExpressionSyntaxIDs.append(
            element: leftSyntaxID
        ) != 0 || process.logicals.rightExpressionSyntaxIDs.append(
            element: rightSyntaxID
        ) != 0 || process.logicals.operatorCodes.append(
            element: operatorCode
        ) != 0 { return 0 - 1 }
        return syntaxID
    }
    depth: 0
    tokenID: firstTokenID
    state comparisonTokenID: Int(0 - 1)
    state comparisonAfterOperatorTokenID: Int(0 - 1)
    state comparisonCode: Int(0)
    while tokenID < afterLastTokenID && comparisonTokenID < 0 {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: tokenID
        ))
        if compilerBParserStartsGenericArguments(source: source, tokens: tokens, openingTokenID: tokenID) {
            tokenID: compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: tokenID, limitTokenID: afterLastTokenID
            )
            if tokenID < 0 { return 0 - 18 }
        } else {
            if byte == 40 || byte == 91 || byte == 123 { depth: depth + 1 }
            if byte == 41 || byte == 93 || byte == 125 { depth: depth - 1 }
            if depth == 0 && (byte == 33 || byte == 60 || byte == 61 || byte == 62) {
                state nextByte: Int(0 - 1)
                if tokenID + 1 < afterLastTokenID {
                    nextByte: compilerBParserTokenByte(
                        source: source,
                        tokens: tokens,
                        tokenID: tokenID + 1
                    )
                }
                if byte == 61 && nextByte == 61 {
                    comparisonCode: compilerBComparisonOperatorEqualCode()
                } else if byte == 33 && nextByte == 61 {
                    comparisonCode: compilerBComparisonOperatorNotEqualCode()
                } else if byte == 60 && nextByte == 61 {
                    comparisonCode: compilerBComparisonOperatorLessOrEqualCode()
                } else if byte == 62 && nextByte == 61 {
                    comparisonCode: compilerBComparisonOperatorGreaterOrEqualCode()
                } else if byte == 60 {
                    comparisonCode: compilerBComparisonOperatorLessCode()
                } else if byte == 62 {
                    comparisonCode: compilerBComparisonOperatorGreaterCode()
                }
                if comparisonCode != 0 {
                    comparisonTokenID: tokenID
                    comparisonAfterOperatorTokenID: tokenID + 1
                    if nextByte == 61 { comparisonAfterOperatorTokenID: tokenID + 2 }
                }
            }
        }
        tokenID: tokenID + 1
    }
    if comparisonTokenID >= 0 {
        let leftSyntaxID: Int(compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID,
            afterLastTokenID: comparisonTokenID
        ))
        let rightSyntaxID: Int(compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: comparisonAfterOperatorTokenID,
            afterLastTokenID: afterLastTokenID
        ))
        if leftSyntaxID < 0 || rightSyntaxID < 0 { return 0 - 1 }
        let operatorToken: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: comparisonTokenID)
        ))
        let syntaxID: Int(operatorToken.start)
        if syntaxNodeStoreAppend(
            store: nodes,
            id: syntaxID,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        ) != 0 || compilerBExpressionFacetStoreAppend(
            store: process.expressions,
            syntaxID: syntaxID,
            kindCode: compilerBParserExpressionKindComparisonCode(),
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID,
            integerValue: 0
        ) != 0 { return 0 - 1 }
        if process.comparisons.syntaxIDs.append(element: syntaxID) != 0 {
            return 0 - 1
        }
        if process.comparisons.leftExpressionSyntaxIDs.append(
            element: leftSyntaxID
        ) != 0 || process.comparisons.rightExpressionSyntaxIDs.append(
            element: rightSyntaxID
        ) != 0 || process.comparisons.operatorCodes.append(
            element: comparisonCode
        ) != 0 { return 0 - 1 }
        return syntaxID
    }
    if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: firstTokenID
    ) == 33 {
        let operandSyntaxID: Int(compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID + 1,
            afterLastTokenID: afterLastTokenID
        ))
        if operandSyntaxID < 0 { return 0 - 1 }
        let operatorToken: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: firstTokenID)
        ))
        let syntaxID: Int(operatorToken.start)
        if syntaxNodeStoreAppend(
            store: nodes,
            id: syntaxID,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        ) != 0 || compilerBExpressionFacetStoreAppend(
            store: process.expressions,
            syntaxID: syntaxID,
            kindCode: compilerBParserExpressionKindLogicalCode(),
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID,
            integerValue: 0
        ) != 0 { return 0 - 1 }
        if process.logicals.syntaxIDs.append(element: syntaxID) != 0 {
            return 0 - 1
        }
        if process.logicals.leftExpressionSyntaxIDs.append(
            element: operandSyntaxID
        ) != 0 || process.logicals.rightExpressionSyntaxIDs.append(
            element: 0 - 1
        ) != 0 || process.logicals.operatorCodes.append(
            element: compilerBLogicalOperatorNotCode()
        ) != 0 { return 0 - 1 }
        return syntaxID
    }
    return 0 - 2
}

function compilerBParserApplicationCallAfterLast(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state openingTokenID: Int(firstTokenID)
    state searching: Bool(true)
    while openingTokenID < afterLastTokenID && searching {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: openingTokenID
        ) == 40 {
            searching: false
        } else {
            openingTokenID: openingTokenID + 1
        }
    }
    if openingTokenID <= firstTokenID || openingTokenID >= afterLastTokenID {
        return afterLastTokenID
    }
    let closingTokenID: Int(compilerBParserMatchingRightParenthesis(
        source: source,
        tokens: tokens,
        openingTokenID: openingTokenID
    ))
    if closingTokenID < 0 || closingTokenID + 1 >= afterLastTokenID {
        return afterLastTokenID
    }
    if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: closingTokenID + 1
        ) != 123 {
        return afterLastTokenID
    }
    if compilerBParserMatchingRightBrace(
        source: source,
        tokens: tokens,
        openingTokenID: closingTokenID + 1,
        limitTokenID: afterLastTokenID
    ) != afterLastTokenID - 1 {
        return afterLastTokenID
    }
    return closingTokenID + 1
}

function compilerBParseApplicationComponentFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let process: CompilerBProcessGraph,
    let ownerApplicationSyntaxID: Int,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state ordinal: Int(0)
    state cursor: Int(firstTokenID)
    while cursor < afterLastTokenID {
        state labelTokenID: Int(0 - 1)
        state firstValueTokenID: Int(cursor)
        if cursor + 2 < afterLastTokenID {
            if compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: cursor)
            ).kind == .identity
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: cursor + 1
                ) == 58 {
                labelTokenID: cursor
                firstValueTokenID: cursor + 2
            }
        }
        state valueAfterLastTokenID: Int(firstValueTokenID)
        state delimiterDepth: Int(0)
        state foundSeparator: Bool(false)
        while valueAfterLastTokenID < afterLastTokenID && !foundSeparator {
            let byte: Int(compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: valueAfterLastTokenID
            ))
            if byte == 40 || byte == 91 || byte == 123 {
                delimiterDepth: delimiterDepth + 1
            }
            if byte == 41 || byte == 93 || byte == 125 {
                delimiterDepth: delimiterDepth - 1
            }
            if byte == 44 && delimiterDepth == 0 {
                foundSeparator: true
            } else {
                valueAfterLastTokenID: valueAfterLastTokenID + 1
            }
        }
        if firstValueTokenID >= valueAfterLastTokenID { return 0 - 1 }
        if process.components.ownerApplicationSyntaxIDs.append(
            element: ownerApplicationSyntaxID
        ) != 0 { return 0 - 1 }
        if process.components.labelTokenIDs.append(
            element: labelTokenID
        ) != 0 { return 0 - 1 }
        if process.components.firstValueTokenIDs.append(
            element: firstValueTokenID
        ) != 0 { return 0 - 1 }
        if process.components.afterLastValueTokenIDs.append(
            element: valueAfterLastTokenID
        ) != 0 { return 0 - 1 }
        if process.components.ordinals.append(element: ordinal) != 0 {
            return 0 - 1
        }
        ordinal: ordinal + 1
        cursor: valueAfterLastTokenID + 1
    }
    return 0
}

function compilerBParseExpressionFacetCore(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    let structured: Int(compilerBParseStructuredExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: firstTokenID,
        afterLastTokenID: afterLastTokenID
    ))
    if structured != 0 - 2 { return structured }
    if compilerBParserStartsGenericArguments(
        source: source, tokens: tokens, openingTokenID: firstTokenID + 1
    ) {
        return compilerBParseSpecializedExpressionFacet(
            source: source, tokens: tokens, nodes: nodes, process: process,
            firstTokenID: firstTokenID, afterLastTokenID: afterLastTokenID
        )
    }

    state openingTokenID: Int(0 - 1)
    state cursor: Int(firstTokenID)
    while cursor < afterLastTokenID && openingTokenID < 0 {
        if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: cursor) == 40 {
            openingTokenID: cursor
        }
        cursor: cursor + 1
    }
    if openingTokenID < 0 {
        return compilerBParseLeafExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        )
    }
    if openingTokenID <= firstTokenID || compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: openingTokenID - 1)
    ).kind != .identity {
        return compilerBParseLeafExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        )
    }
    state shapeClosingTokenID: Int(compilerBParserMatchingRightParenthesis(
        source: source,
        tokens: tokens,
        openingTokenID: openingTokenID
    ))
    if shapeClosingTokenID < 0 || shapeClosingTokenID >= afterLastTokenID {
        return 0 - 1
    }
    state shapeCursor: Int(shapeClosingTokenID + 1)
    state applicationShape: Bool(true)
    while shapeCursor < afterLastTokenID && applicationShape {
        if shapeCursor + 1 >= afterLastTokenID
            || compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: shapeCursor
            ) != 46 {
            applicationShape: false
        } else {
            let member: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: shapeCursor + 1)
            ))
            if member.kind != .identity {
                applicationShape: false
            } else {
                state memberIsApplication: Bool(false)
                if shapeCursor + 2 < afterLastTokenID {
                    memberIsApplication: compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: shapeCursor + 2
                    ) == 40
                }
                if memberIsApplication {
                    shapeClosingTokenID: compilerBParserMatchingRightParenthesis(
                        source: source,
                        tokens: tokens,
                        openingTokenID: shapeCursor + 2
                    )
                    if shapeClosingTokenID < 0
                        || shapeClosingTokenID >= afterLastTokenID {
                        return 0 - 1
                    }
                    shapeCursor: shapeClosingTokenID + 1
                } else {
                    shapeCursor: shapeCursor + 2
                }
            }
        }
    }
    if !applicationShape {
        return compilerBParseLeafExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID,
            afterLastTokenID: afterLastTokenID
        )
    }
    state predecessorSyntaxID: Int(0 - 1)
    state applicationFirstTokenID: Int(firstTokenID)
    state functionIdentityTokenID: Int(openingTokenID - 1)
    state hasPredecessor: Bool(false)
    if functionIdentityTokenID - 1 >= firstTokenID {
        let attachmentTokenID: Int(functionIdentityTokenID - 1)
        let hasAttachment: Bool(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: attachmentTokenID
        ) == 46)
        if hasAttachment {
            if attachmentTokenID > firstTokenID {
                hasPredecessor: true
            }
        }
    }
    if hasPredecessor {
        let predecessorAfterLastTokenID: Int(functionIdentityTokenID - 1)
        let predecessor: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: firstTokenID)
        ))
        predecessorSyntaxID: predecessor.start
        if compilerBParseLeafExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstTokenID,
            afterLastTokenID: predecessorAfterLastTokenID
        ) < 0 { return 0 - 1 }
        state predecessorIdentityTokenID: Int(predecessorAfterLastTokenID - 1)
        while predecessorIdentityTokenID >= firstTokenID
            && compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: predecessorIdentityTokenID)
            ).kind != .identity {
            predecessorIdentityTokenID: predecessorIdentityTokenID - 1
        }
        if compilerBApplicationFacetStoreAppend(
            store: process.applications,
            syntaxID: predecessorSyntaxID,
            predecessorSyntaxID: 0 - 1,
            identityTokenID: predecessorIdentityTokenID,
            firstTokenID: firstTokenID,
            afterLastTokenID: predecessorAfterLastTokenID
        ) != 0 { return 0 - 1 }
    }

    state finalApplicationSyntaxID: Int(0 - 1)
    state parsingApplications: Bool(true)
    while parsingApplications {
        let closingTokenID: Int(compilerBParserMatchingRightParenthesis(
            source: source,
            tokens: tokens,
            openingTokenID: openingTokenID
        ))
        if closingTokenID < 0 || closingTokenID >= afterLastTokenID {
            return 0 - 1
        }
        let functionIdentity: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: functionIdentityTokenID)
        ))
        if functionIdentity.kind != .identity { return 0 - 1 }
        let applicationSyntaxID: Int(functionIdentity.start)
        if syntaxNodeStoreAppend(
            store: nodes,
            id: applicationSyntaxID,
            firstTokenID: applicationFirstTokenID,
            afterLastTokenID: closingTokenID + 1
        ) != 0 { return 0 - 1 }
        if compilerBExpressionFacetStoreAppend(
            store: process.expressions,
            syntaxID: applicationSyntaxID,
            kindCode: compilerBParserExpressionKindCallCode(),
            firstTokenID: applicationFirstTokenID,
            afterLastTokenID: closingTokenID + 1,
            integerValue: 0
        ) != 0 { return 0 - 1 }
        if compilerBApplicationFacetStoreAppend(
            store: process.applications,
            syntaxID: applicationSyntaxID,
            predecessorSyntaxID: predecessorSyntaxID,
            identityTokenID: functionIdentityTokenID,
            firstTokenID: applicationFirstTokenID,
            afterLastTokenID: closingTokenID + 1
        ) != 0 { return 0 - 1 }

        if compilerBParseArgumentRange(
            source: source, tokens: tokens, nodes: nodes, process: process,
            arguments: process.arguments, applicationSyntaxID: applicationSyntaxID,
            firstTokenID: openingTokenID + 1, afterLastTokenID: closingTokenID,
            requireLabels: false
        ) != 0 { return 0 - 18 }
        finalApplicationSyntaxID: applicationSyntaxID
        state hasMemberAccess: Bool(false)
        if closingTokenID + 2 < afterLastTokenID {
            hasMemberAccess: compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: closingTokenID + 1
            ) == 46
        }
        state hasNextApplication: Bool(false)
        if hasMemberAccess && closingTokenID + 3 < afterLastTokenID {
            hasNextApplication: compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: closingTokenID + 3
                ) == 40
        }
        if hasNextApplication {
            predecessorSyntaxID: applicationSyntaxID
            functionIdentityTokenID: closingTokenID + 2
            openingTokenID: closingTokenID + 3
        } else if hasMemberAccess && closingTokenID + 3 == afterLastTokenID {
            let projectionIdentityTokenID: Int(closingTokenID + 2)
            let projectionIdentity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: projectionIdentityTokenID)
            ))
            let projectionSyntaxID: Int(projectionIdentity.start)
            if projectionIdentity.kind != .identity { return 0 - 1 }
            if syntaxNodeStoreAppend(
                store: nodes,
                id: projectionSyntaxID,
                firstTokenID: applicationFirstTokenID,
                afterLastTokenID: afterLastTokenID
            ) != 0 { return 0 - 1 }
            if compilerBExpressionFacetStoreAppend(
                store: process.expressions,
                syntaxID: projectionSyntaxID,
                kindCode: compilerBParserExpressionKindProjectionCode(),
                firstTokenID: applicationFirstTokenID,
                afterLastTokenID: afterLastTokenID,
                integerValue: 0
            ) != 0 { return 0 - 1 }
            if compilerBApplicationFacetStoreAppend(
                store: process.applications,
                syntaxID: projectionSyntaxID,
                predecessorSyntaxID: applicationSyntaxID,
                identityTokenID: projectionIdentityTokenID,
                firstTokenID: applicationFirstTokenID,
                afterLastTokenID: afterLastTokenID
            ) != 0 { return 0 - 1 }
            finalApplicationSyntaxID: projectionSyntaxID
            parsingApplications: false
        } else {
            if closingTokenID != afterLastTokenID - 1 { return 0 - 1 }
            parsingApplications: false
        }
    }
    return finalApplicationSyntaxID
}

function compilerBParseExpressionFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let firstTokenID: Int,
    let afterLastTokenID: Int
): Int {
    let callAfterLastTokenID: Int(compilerBParserApplicationCallAfterLast(
        source: source,
        tokens: tokens,
        firstTokenID: firstTokenID,
        afterLastTokenID: afterLastTokenID
    ))
    let syntaxID: Int(compilerBParseExpressionFacetCore(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: firstTokenID,
        afterLastTokenID: callAfterLastTokenID
    ))
    if syntaxID < 0 || callAfterLastTokenID == afterLastTokenID {
        return syntaxID
    }
    if compilerBParseApplicationComponentFacets(
        source: source,
        tokens: tokens,
        process: process,
        ownerApplicationSyntaxID: syntaxID,
        firstTokenID: callAfterLastTokenID + 1,
        afterLastTokenID: afterLastTokenID - 1
    ) != 0 { return 0 - 1 }
    return syntaxID
}

function compilerBParserHasLineBreak(
    let source: String,
    let left: CompilerBToken,
    let right: CompilerBToken
): Bool {
    state cursor: Int(left.end)
    while cursor < right.start {
        if source.byte(index: cursor) == 10 { return true }
        cursor: cursor + 1
    }
    return false
}

function compilerBParserLineAfterLastTokenID(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let firstTokenID: Int,
    let limitTokenID: Int
): Int {
    state tokenID: Int(firstTokenID + 1)
    state delimiterDepth: Int(0)
    state previous: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: firstTokenID)
    ))
    while tokenID < limitTokenID {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        if delimiterDepth == 0
            && compilerBParserHasLineBreak(source: source, left: previous, right: token) {
            return tokenID
        }
        let byte: Int(source.byte(index: token.start))
        if byte == 40 || byte == 91 || byte == 123 { delimiterDepth: delimiterDepth + 1 }
        if byte == 41 || byte == 93 || byte == 125 { delimiterDepth: delimiterDepth - 1 }
        if byte == 60 && (compilerBParserStartsGenericArguments(
            source: source, tokens: tokens, openingTokenID: tokenID
        ) || tokenID + 1 < limitTokenID && compilerBTokenMatchesBytes(
            source: source,
            token: compilerBRetainedToken(store: tokens, id: TokenID(value: tokenID + 1)),
            expected: "let"
        )) {
            let closing: Int(compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: tokenID, limitTokenID: limitTokenID
            ))
            if closing < 0 { return limitTokenID }
            tokenID: closing
            previous: compilerBRetainedToken(store: tokens, id: TokenID(value: closing))
        } else {
            previous: token
        }
        tokenID: tokenID + 1
    }
    return limitTokenID
}

function compilerBParserMatchingRightBrace(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let openingTokenID: Int,
    let limitTokenID: Int
): Int {
    state depth: Int(1)
    state tokenID: Int(openingTokenID + 1)
    while tokenID < limitTokenID && depth != 0 {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: tokenID
        ))
        if byte == 123 { depth: depth + 1 }
        if byte == 125 { depth: depth - 1 }
        tokenID: tokenID + 1
    }
    if depth != 0 { return 0 - 1 }
    return tokenID - 1
}

function compilerBParseLocalBindingProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let memberTokenID: Int,
    let limitTokenID: Int
): Int {
    if memberTokenID + 3 >= limitTokenID { return 0 - 1 }
    let keyword: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: memberTokenID)
    ))
    let identity: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: memberTokenID + 1)
    ))
    let isLet: Bool(compilerBTokenMatchesBytes(
        source: source,
        token: keyword,
        expected: "let"
    ))
    let isState: Bool(compilerBTokenMatchesBytes(
        source: source,
        token: keyword,
        expected: "state"
    ))
    if (!isLet && !isState) || identity.kind != .identity
        || compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: memberTokenID + 2
        ) != 58 { return 0 - 1 }
    let firstValueTokenID: Int(memberTokenID + 3)
    let afterLastValueTokenID: Int(compilerBParserLineAfterLastTokenID(
        source: source,
        tokens: tokens,
        firstTokenID: firstValueTokenID,
        limitTokenID: limitTokenID
    ))
    state questionTokenID: Int(0 - 1)
    state depth: Int(0)
    state cursor: Int(firstValueTokenID)
    while cursor < afterLastValueTokenID && questionTokenID < 0 {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: cursor
        ))
        if byte == 40 { depth: depth + 1 }
        if byte == 41 { depth: depth - 1 }
        if byte == 63 && depth == 0 { questionTokenID: cursor }
        cursor: cursor + 1
    }
    state expressionAfterLastTokenID: Int(afterLastValueTokenID)
    if questionTokenID >= 0 { expressionAfterLastTokenID: questionTokenID }
    let valueExpressionSyntaxID: Int(compilerBParseExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: firstValueTokenID,
        afterLastTokenID: expressionAfterLastTokenID
    ))
    if valueExpressionSyntaxID < 0 { return valueExpressionSyntaxID }
    state expressionKind: Int(compilerBParserExpressionKindUnknownCode())
    if questionTokenID >= 0 {
        expressionKind: compilerBParserExpressionKindNoValueFallbackCode()
    }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: keyword.start,
        firstTokenID: memberTokenID,
        afterLastTokenID: afterLastValueTokenID
    ) != 0 { return 0 - 1 }
    let expressionRow: Int(process.expressions.syntaxIDs.firstIndex(value: valueExpressionSyntaxID))
    if expressionRow < 0 { return 0 - 1 }
    if questionTokenID >= 0 {
        if process.expressions.kindCodes.update(
            element: expressionKind,
            index: expressionRow
        ) != 0 { return 0 - 1 }
    }
    if process.locals.syntaxIDs.append(element: keyword.start) != 0 { return 0 - 1 }
    if process.locals.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 { return 0 - 1 }
    state localKindCode: Int(compilerBParserMemberKindLetCode())
    if isState { localKindCode: compilerBParserMemberKindStateCode() }
    if process.locals.kindCodes.append(element: localKindCode) != 0 { return 0 - 1 }
    if process.locals.identityTokenIDs.append(element: memberTokenID + 1) != 0 {
        return 0 - 1
    }
    if process.locals.valueExpressionSyntaxIDs.append(element: valueExpressionSyntaxID) != 0 {
        return 0 - 1
    }
    if process.locals.firstValueTokenIDs.append(element: firstValueTokenID) != 0 {
        return 0 - 1
    }
    if process.locals.afterLastValueTokenIDs.append(element: afterLastValueTokenID) != 0 {
        return 0 - 1
    }
    if questionTokenID >= 0 {
        if process.noValueFallbacks.expressionSyntaxIDs.append(
            element: valueExpressionSyntaxID
        ) != 0 {
            return 0 - 1
        }
        if process.noValueFallbacks.questionTokenIDs.append(element: questionTokenID) != 0 {
            return 0 - 1
        }
        if process.noValueFallbacks.firstTargetTokenIDs.append(element: firstValueTokenID) != 0 {
            return 0 - 1
        }
        if process.noValueFallbacks.afterLastTargetTokenIDs.append(element: questionTokenID) != 0 {
            return 0 - 1
        }
        if process.noValueFallbacks.firstRHSTokenIDs.append(element: questionTokenID + 1) != 0 {
            return 0 - 1
        }
        if process.noValueFallbacks.afterLastRHSTokenIDs.append(
            element: afterLastValueTokenID
        ) != 0 {
            return 0 - 1
        }
    }
    return afterLastValueTokenID
}

function compilerBParseAssignmentProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let identityTokenID: Int,
    let limitTokenID: Int
): Int {
    if identityTokenID + 2 >= limitTokenID
        || compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: identityTokenID + 1
        ) != 58 { return 0 - 1 }
    let identity: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: identityTokenID)
    ))
    let firstValueTokenID: Int(identityTokenID + 2)
    let afterLastValueTokenID: Int(compilerBParserLineAfterLastTokenID(
        source: source,
        tokens: tokens,
        firstTokenID: firstValueTokenID,
        limitTokenID: limitTokenID
    ))
    let valueExpressionSyntaxID: Int(compilerBParseExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: firstValueTokenID,
        afterLastTokenID: afterLastValueTokenID
    ))
    if valueExpressionSyntaxID < 0 { return valueExpressionSyntaxID }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: identity.start,
        firstTokenID: identityTokenID,
        afterLastTokenID: afterLastValueTokenID
    ) != 0 { return 0 - 1 }
    state ordinal: Int(0)
    state row: Int(0)
    while row < process.assignments.ownerSyntaxIDs.count {
        if process.assignments.ownerSyntaxIDs.element(index: row) == ownerSyntaxID {
            ordinal: ordinal + 1
        }
        row: row + 1
    }
    if process.assignments.syntaxIDs.append(element: identity.start) != 0 {
        return 0 - 1
    }
    if process.assignments.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
        return 0 - 1
    }
    if process.assignments.identityTokenIDs.append(element: identityTokenID) != 0 {
        return 0 - 1
    }
    if process.assignments.valueExpressionSyntaxIDs.append(
        element: valueExpressionSyntaxID
    ) != 0 { return 0 - 1 }
    if process.assignments.ordinals.append(element: ordinal) != 0 { return 0 - 1 }
    return afterLastValueTokenID
}

function compilerBParseReturnProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let returnTokenID: Int,
    let limitTokenID: Int
): Int {
    if returnTokenID + 1 >= limitTokenID { return 0 - 1 }
    let keyword: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: returnTokenID)
    ))
    let firstValueTokenID: Int(returnTokenID + 1)
    let afterLastValueTokenID: Int(compilerBParserLineAfterLastTokenID(
        source: source,
        tokens: tokens,
        firstTokenID: firstValueTokenID,
        limitTokenID: limitTokenID
    ))
    let valueExpressionSyntaxID: Int(compilerBParseExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: firstValueTokenID,
        afterLastTokenID: afterLastValueTokenID
    ))
    if valueExpressionSyntaxID < 0 { return valueExpressionSyntaxID }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: keyword.start,
        firstTokenID: returnTokenID,
        afterLastTokenID: afterLastValueTokenID
    ) != 0 { return 0 - 1 }
    state ordinal: Int(0)
    state row: Int(0)
    while row < process.returns.ownerSyntaxIDs.count {
        if process.returns.ownerSyntaxIDs.element(index: row) == ownerSyntaxID {
            ordinal: ordinal + 1
        }
        row: row + 1
    }
    if process.returns.syntaxIDs.append(element: keyword.start) != 0 { return 0 - 1 }
    if process.returns.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
        return 0 - 1
    }
    if process.returns.valueExpressionSyntaxIDs.append(
        element: valueExpressionSyntaxID
    ) != 0 { return 0 - 1 }
    if process.returns.ordinals.append(element: ordinal) != 0 { return 0 - 1 }
    return afterLastValueTokenID
}

function compilerBParseLocalMacroApplication(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let applications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let applicationTokenID: Int,
    let limitTokenID: Int
): Int {
    let afterApplicationTokenID: Int(compilerBParserMacroApplicationAfterLast(
        source: source,
        tokens: tokens,
        applicationTokenID: applicationTokenID
    ))
    if afterApplicationTokenID < 0 || afterApplicationTokenID > limitTokenID {
        return 0 - 1
    }
    state targetSyntaxID: Int(0 - 1)
    state targetIdentityTokenID: Int(0 - 1)
    state hasTarget: Bool(false)
    if afterApplicationTokenID < limitTokenID {
        let target: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: afterApplicationTokenID)
        ))
        if compilerBTokenMatchesBytes(source: source, token: target, expected: "let") {
            if afterApplicationTokenID + 1 >= limitTokenID { return 0 - 1 }
            hasTarget: true
            targetSyntaxID: target.start
            targetIdentityTokenID: afterApplicationTokenID + 1
        }
    }
    let application: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: applicationTokenID)
    ))
    let existingRow: Int(applications.syntaxIDs.firstIndex(value: application.start))
    if existingRow < 0 {
        state firstArgumentTokenID: Int(applicationTokenID + 2)
        state afterLastArgumentTokenID: Int(applicationTokenID + 2)
        if applicationTokenID + 2 < tokens.kinds.count
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: applicationTokenID + 2
            ) == 40 {
            firstArgumentTokenID: applicationTokenID + 3
            afterLastArgumentTokenID: afterApplicationTokenID - 1
        }
        if syntaxNodeStoreAppend(
            store: nodes,
            id: application.start,
            firstTokenID: applicationTokenID,
            afterLastTokenID: afterApplicationTokenID
        ) != 0 { return 0 - 1 }
        if compilerBMacroApplicationStoreAppend(
            store: applications,
            syntaxID: application.start,
            identityTokenID: applicationTokenID + 1,
            firstArgumentTokenID: firstArgumentTokenID,
            afterLastArgumentTokenID: afterLastArgumentTokenID,
            targetSyntaxID: targetSyntaxID,
            targetIdentityTokenID: targetIdentityTokenID,
            ordinal: 0
        ) != 0 { return 0 - 1 }
        if firstArgumentTokenID < afterLastArgumentTokenID
            && compilerBParseMacroArgumentFacets(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                arguments: applications,
                applicationSyntaxID: application.start,
                firstTokenID: firstArgumentTokenID,
                afterLastTokenID: afterLastArgumentTokenID
            ) != 0 { return 0 - 1 }
        if !hasTarget {
            if compilerBExpressionFacetStoreAppend(
                store: process.expressions,
                syntaxID: application.start,
                kindCode: compilerBParserExpressionKindCallCode(),
                firstTokenID: applicationTokenID,
                afterLastTokenID: afterApplicationTokenID,
                integerValue: 0
            ) != 0 { return 0 - 1 }
            state executionOrdinal: Int(0)
            state executionRow: Int(0)
            while executionRow < process.executions.ownerSyntaxIDs.count {
                if process.executions.ownerSyntaxIDs.element(index: executionRow)
                    == ownerSyntaxID {
                    executionOrdinal: executionOrdinal + 1
                }
                executionRow: executionRow + 1
            }
            if process.executions.syntaxIDs.append(element: application.start) != 0 {
                return 0 - 1
            }
            if process.executions.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
                return 0 - 1
            }
            if process.executions.expressionSyntaxIDs.append(element: application.start) != 0 {
                return 0 - 1
            }
            if process.executions.ordinals.append(element: executionOrdinal) != 0 {
                return 0 - 1
            }
        }
    }
    return afterApplicationTokenID
}

function compilerBLocalSyntaxIDForIdentity(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let identityTokenID: Int
): Int {
    let identity: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: identityTokenID)
    ))
    state row: Int(0)
    while row < process.locals.syntaxIDs.count {
        if process.locals.ownerSyntaxIDs.element(index: row) == ownerSyntaxID {
            let candidate: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: process.locals.identityTokenIDs.element(index: row))
            ))
            if compilerBTokenBytesEqual(
                source: source,
                left: identity,
                right: candidate
            ) {
                return process.locals.syntaxIDs.element(index: row)
            }
        }
        row: row + 1
    }
    return 0 - 1
}

function compilerBCollectMacroEnvironmentFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    while tokenID + 2 < afterLastTokenID - 1 {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: tokenID
        ) == 35 {
            let identity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: tokenID + 1)
            ))
            if identity.kind == .identity
                && compilerBTokenMatchesBytes(
                    source: source,
                    token: identity,
                    expected: "environment"
                )
                && compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: tokenID + 2
                ) == 123 {
                let closingTokenID: Int(compilerBParserMatchingRightBrace(
                    source: source,
                    tokens: tokens,
                    openingTokenID: tokenID + 2,
                    limitTokenID: afterLastTokenID
                ))
                if closingTokenID < 0 { return 0 - 1 }
                let marker: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: tokenID)
                ))
                if syntaxNodeStoreAppend(
                    store: nodes,
                    id: marker.start,
                    firstTokenID: tokenID,
                    afterLastTokenID: closingTokenID + 1
                ) != 0 { return 0 - 1 }
                if process.environments.syntaxIDs.append(element: marker.start) != 0 {
                    return 0 - 1
                }
                if process.environments.ownerDeclarationSyntaxIDs.append(
                    element: ownerSyntaxID
                ) != 0 { return 0 - 1 }
                tokenID: closingTokenID
            }
        }
        tokenID: tokenID + 1
    }
    return 0
}

function compilerBEnvironmentSyntaxIDContainingToken(
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let tokenID: Int
): Int {
    state result: Int(0 - 1)
    state row: Int(0)
    while row < process.environments.syntaxIDs.count {
        if process.environments.ownerDeclarationSyntaxIDs.element(index: row)
            == ownerSyntaxID {
            let environmentSyntaxID: Int(process.environments.syntaxIDs.element(index: row))
            let nodeRow: Int(nodes.ids.firstIndex(value: environmentSyntaxID))
            if nodeRow >= 0
                && tokenID >= nodes.firstTokenIDs.element(index: nodeRow)
                && tokenID < nodes.afterLastTokenIDs.element(index: nodeRow) {
                result: environmentSyntaxID
            }
        }
        row: row + 1
    }
    return result
}

function compilerBParseEmittedMacroBlockFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let blocks: CompilerBBlockFacetStore,
    let macroApplications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int
): Int {
    state environmentRow: Int(0)
    while environmentRow < process.environments.syntaxIDs.count {
        if process.environments.ownerDeclarationSyntaxIDs.element(index: environmentRow)
            == ownerSyntaxID {
            let environmentSyntaxID: Int(
                process.environments.syntaxIDs.element(index: environmentRow)
            )
            let environmentNodeRow: Int(nodes.ids.firstIndex(value: environmentSyntaxID))
            if environmentNodeRow < 0 { return 0 - 1 }
            let environmentOpeningTokenID: Int(
                nodes.firstTokenIDs.element(index: environmentNodeRow) + 2
            )
            let environmentClosingTokenID: Int(
                nodes.afterLastTokenIDs.element(index: environmentNodeRow) - 1
            )
            state tokenID: Int(environmentOpeningTokenID + 1)
            state depth: Int(0)
            while tokenID < environmentClosingTokenID {
                let byte: Int(compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: tokenID
                ))
                if depth == 0 && byte == 64 && tokenID + 2 < environmentClosingTokenID {
                    let identity: CompilerBToken(compilerBRetainedToken(
                        store: tokens,
                        id: TokenID(value: tokenID + 1)
                    ))
                    let afterApplicationTokenID: Int(
                        compilerBParserMacroApplicationAfterLast(
                            source: source,
                            tokens: tokens,
                            applicationTokenID: tokenID
                        )
                    )
                    if identity.kind == .identity
                        && afterApplicationTokenID >= 0
                        && afterApplicationTokenID < environmentClosingTokenID
                        && compilerBParserTokenByte(
                            source: source,
                            tokens: tokens,
                            tokenID: afterApplicationTokenID
                        ) == 123 {
                        let closingTokenID: Int(compilerBParserMatchingRightBrace(
                            source: source,
                            tokens: tokens,
                            openingTokenID: afterApplicationTokenID,
                            limitTokenID: environmentClosingTokenID + 1
                        ))
                        if closingTokenID < 0 { return 0 - 1 }
                        let marker: CompilerBToken(compilerBRetainedToken(
                            store: tokens,
                            id: TokenID(value: tokenID)
                        ))
                        let opening: CompilerBToken(compilerBRetainedToken(
                            store: tokens,
                            id: TokenID(value: afterApplicationTokenID)
                        ))
                        if syntaxNodeStoreAppend(
                            store: nodes,
                            id: marker.start,
                            firstTokenID: tokenID,
                            afterLastTokenID: closingTokenID + 1
                        ) != 0 { return 0 - 1 }
                        if syntaxNodeStoreAppend(
                            store: nodes,
                            id: opening.start,
                            firstTokenID: afterApplicationTokenID,
                            afterLastTokenID: closingTokenID + 1
                        ) != 0 { return 0 - 1 }
                        if compilerBBlockFacetStoreAppend(
                            store: blocks,
                            syntaxID: opening.start,
                            parentSyntaxID: marker.start
                        ) != 0 { return 0 - 1 }
                        state firstArgumentTokenID: Int(tokenID + 2)
                        state afterLastArgumentTokenID: Int(tokenID + 2)
                        if compilerBParserTokenByte(
                            source: source,
                            tokens: tokens,
                            tokenID: tokenID + 2
                        ) == 40 {
                            firstArgumentTokenID: tokenID + 3
                            afterLastArgumentTokenID: afterApplicationTokenID - 1
                        }
                        if compilerBMacroApplicationStoreAppend(
                            store: macroApplications,
                            syntaxID: marker.start,
                            identityTokenID: tokenID + 1,
                            firstArgumentTokenID: firstArgumentTokenID,
                            afterLastArgumentTokenID: afterLastArgumentTokenID,
                            targetSyntaxID: 0 - 1,
                            targetIdentityTokenID: 0 - 1,
                            ordinal: 0
                        ) != 0 { return 0 - 1 }
                        if firstArgumentTokenID < afterLastArgumentTokenID
                            && compilerBParseMacroArgumentFacets(
                                source: source,
                                tokens: tokens,
                                nodes: nodes,
                                process: process,
                                arguments: macroApplications,
                                applicationSyntaxID: marker.start,
                                firstTokenID: firstArgumentTokenID,
                                afterLastTokenID: afterLastArgumentTokenID
                            ) != 0 { return 0 - 1 }
                        if process.emittedMacroBlocks.syntaxIDs.append(
                            element: marker.start
                        ) != 0 { return 0 - 1 }
                        if process.emittedMacroBlocks.identityTokenIDs.append(
                            element: tokenID + 1
                        ) != 0 { return 0 - 1 }
                        if process.emittedMacroBlocks.bodySyntaxIDs.append(
                            element: opening.start
                        ) != 0 { return 0 - 1 }
                        if process.emittedMacroBlocks.openingTokenIDs.append(
                            element: afterApplicationTokenID
                        ) != 0 { return 0 - 1 }
                        if process.emittedMacroBlocks.closingTokenIDs.append(
                            element: closingTokenID
                        ) != 0 { return 0 - 1 }
                        state ordinal: Int(0)
                        state nodeRow: Int(0)
                        while nodeRow < process.environmentNodes.syntaxIDs.count {
                            if process.environmentNodes.environmentSyntaxIDs.element(
                                index: nodeRow
                            ) == environmentSyntaxID
                                && process.environmentNodes.parentSyntaxIDs.element(
                                    index: nodeRow
                                ) == environmentSyntaxID {
                                ordinal: ordinal + 1
                            }
                            nodeRow: nodeRow + 1
                        }
                        if process.environmentNodes.environmentSyntaxIDs.append(
                            element: environmentSyntaxID
                        ) != 0 { return 0 - 1 }
                        if process.environmentNodes.syntaxIDs.append(
                            element: marker.start
                        ) != 0 { return 0 - 1 }
                        if process.environmentNodes.parentSyntaxIDs.append(
                            element: environmentSyntaxID
                        ) != 0 { return 0 - 1 }
                        if process.environmentNodes.ordinals.append(
                            element: ordinal
                        ) != 0 { return 0 - 1 }
                        if compilerBParseProcessFacets(
                            source: source,
                            tokens: tokens,
                            nodes: nodes,
                            process: process,
                            blocks: blocks,
                            macroApplications: macroApplications,
                            members: members,
                            ownerSyntaxID: marker.start,
                            bodySyntaxID: opening.start,
                            openingTokenID: afterApplicationTokenID,
                            afterLastTokenID: closingTokenID + 1
                        ) != 0 { return 0 - 1 }
                        tokenID: closingTokenID + 1
                    } else {
                        tokenID: tokenID + 1
                    }
                } else {
                    if byte == 123 { depth: depth + 1 }
                    if byte == 125 { depth: depth - 1 }
                    tokenID: tokenID + 1
                }
            }
        }
        environmentRow: environmentRow + 1
    }
    return 0
}

function compilerBParseApplicationProvisionFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let macroApplications: CompilerBMacroApplicationStore,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    while tokenID < afterLastTokenID - 1 {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        if token.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: token,
                expected: "extension"
            )
            && tokenID + 1 < afterLastTokenID - 1
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: tokenID + 1
            ) != 58 {
            let firstTargetTokenID: Int(tokenID + 1)
            state compositionOpeningTokenID: Int(firstTargetTokenID)
            while compositionOpeningTokenID < afterLastTokenID - 1
                && compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: compositionOpeningTokenID
                ) != 123 {
                compositionOpeningTokenID: compositionOpeningTokenID + 1
            }
            if compositionOpeningTokenID >= afterLastTokenID - 1 {
                return 0 - 1
            }
            let compositionClosingTokenID: Int(compilerBParserMatchingRightBrace(
                source: source,
                tokens: tokens,
                openingTokenID: compositionOpeningTokenID,
                limitTokenID: afterLastTokenID
            ))
            if compositionClosingTokenID < 0 { return 0 - 1 }
            let environmentSyntaxID: Int(compilerBEnvironmentSyntaxIDContainingToken(
                nodes: nodes,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                tokenID: tokenID
            ))
            if environmentSyntaxID < 0 { return 0 - 1 }
            let extensionSyntaxID: Int(token.start)
            if syntaxNodeStoreAppend(
                store: nodes,
                id: extensionSyntaxID,
                firstTokenID: tokenID,
                afterLastTokenID: compositionClosingTokenID + 1
            ) != 0 { return 0 - 1 }
            let targetApplicationSyntaxID: Int(compilerBParseLeafExpressionFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                firstTokenID: firstTargetTokenID,
                afterLastTokenID: compositionOpeningTokenID
            ))
            if targetApplicationSyntaxID < 0 { return 0 - 1 }
            state targetIdentityTokenID: Int(compositionOpeningTokenID - 1)
            while targetIdentityTokenID >= firstTargetTokenID
                && compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: targetIdentityTokenID)
                ).kind != .identity {
                targetIdentityTokenID: targetIdentityTokenID - 1
            }
            if compilerBApplicationFacetStoreAppend(
                store: process.applications,
                syntaxID: targetApplicationSyntaxID,
                predecessorSyntaxID: 0 - 1,
                identityTokenID: targetIdentityTokenID,
                firstTokenID: firstTargetTokenID,
                afterLastTokenID: compositionOpeningTokenID
            ) != 0 { return 0 - 1 }
            if process.extensions.syntaxIDs.append(element: extensionSyntaxID) != 0 {
                return 0 - 1
            }
            if process.extensions.targetApplicationSyntaxIDs.append(
                element: targetApplicationSyntaxID
            ) != 0 { return 0 - 1 }
            state extensionOrdinal: Int(0)
            state environmentNodeRow: Int(0)
            while environmentNodeRow < process.environmentNodes.syntaxIDs.count {
                if process.environmentNodes.environmentSyntaxIDs.element(
                    index: environmentNodeRow
                ) == environmentSyntaxID
                    && process.environmentNodes.parentSyntaxIDs.element(
                        index: environmentNodeRow
                    ) == environmentSyntaxID {
                    extensionOrdinal: extensionOrdinal + 1
                }
                environmentNodeRow: environmentNodeRow + 1
            }
            if process.environmentNodes.environmentSyntaxIDs.append(
                element: environmentSyntaxID
            ) != 0 { return 0 - 1 }
            if process.environmentNodes.syntaxIDs.append(
                element: extensionSyntaxID
            ) != 0 { return 0 - 1 }
            if process.environmentNodes.parentSyntaxIDs.append(
                element: environmentSyntaxID
            ) != 0 { return 0 - 1 }
            if process.environmentNodes.ordinals.append(
                element: extensionOrdinal
            ) != 0 { return 0 - 1 }
            let firstExtensionMemberRow: Int(members.syntaxIDs.count)
            if compilerBParseOwnedMemberFacets(
                source: source,
                tokens: tokens,
                nodes: nodes,
                applications: macroApplications,
                process: process,
                members: members,
                ownerSyntaxID: extensionSyntaxID,
                openingTokenID: compositionOpeningTokenID,
                afterLastTokenID: compositionClosingTokenID + 1
            ) != 0 { return 0 - 1 }
            state extensionMemberRow: Int(firstExtensionMemberRow)
            while extensionMemberRow < members.syntaxIDs.count {
                if members.ownerSyntaxIDs.element(index: extensionMemberRow)
                    == extensionSyntaxID {
                    state emittedOrdinal: Int(0)
                    environmentNodeRow: 0
                    while environmentNodeRow
                        < process.environmentNodes.syntaxIDs.count {
                        if process.environmentNodes.environmentSyntaxIDs.element(
                            index: environmentNodeRow
                        ) == environmentSyntaxID
                            && process.environmentNodes.parentSyntaxIDs.element(
                                index: environmentNodeRow
                            ) == extensionSyntaxID {
                            emittedOrdinal: emittedOrdinal + 1
                        }
                        environmentNodeRow: environmentNodeRow + 1
                    }
                    if process.environmentNodes.environmentSyntaxIDs.append(
                        element: environmentSyntaxID
                    ) != 0 { return 0 - 1 }
                    if process.environmentNodes.syntaxIDs.append(
                        element: members.syntaxIDs.element(index: extensionMemberRow)
                    ) != 0 { return 0 - 1 }
                    if process.environmentNodes.parentSyntaxIDs.append(
                        element: extensionSyntaxID
                    ) != 0 { return 0 - 1 }
                    if process.environmentNodes.ordinals.append(
                        element: emittedOrdinal
                    ) != 0 { return 0 - 1 }
                }
                extensionMemberRow: extensionMemberRow + 1
            }
            state spliceTokenID: Int(compositionOpeningTokenID + 1)
            while spliceTokenID < compositionClosingTokenID {
                if compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: spliceTokenID
                ) == 35 && spliceTokenID + 1 < compositionClosingTokenID {
                    let sourceIdentity: CompilerBToken(compilerBRetainedToken(
                        store: tokens,
                        id: TokenID(value: spliceTokenID + 1)
                    ))
                    if sourceIdentity.kind == .identity {
                        let sourceSyntaxID: Int(compilerBLocalSyntaxIDForIdentity(
                            source: source,
                            tokens: tokens,
                            process: process,
                            ownerSyntaxID: ownerSyntaxID,
                            identityTokenID: spliceTokenID + 1
                        ))
                        if sourceSyntaxID < 0 { return 0 - 1 }
                        let splice: CompilerBToken(compilerBRetainedToken(
                            store: tokens,
                            id: TokenID(value: spliceTokenID)
                        ))
                        if process.applicationProvisions.syntaxIDs.append(
                            element: splice.start
                        ) != 0 { return 0 - 1 }
                        if process.applicationProvisions.ownerSyntaxIDs.append(
                            element: ownerSyntaxID
                        ) != 0 { return 0 - 1 }
                        if process.applicationProvisions.environmentSyntaxIDs.append(
                            element: environmentSyntaxID
                        ) != 0 { return 0 - 1 }
                        if process.applicationProvisions.extensionSyntaxIDs.append(
                            element: extensionSyntaxID
                        ) != 0 { return 0 - 1 }
                        if process.applicationProvisions.targetApplicationSyntaxIDs.append(
                            element: targetApplicationSyntaxID
                        ) != 0 { return 0 - 1 }
                        if process.applicationProvisions.sourceSyntaxIDs.append(
                            element: sourceSyntaxID
                        ) != 0 { return 0 - 1 }
                    }
                } else {
                    let emittedIdentity: CompilerBToken(compilerBRetainedToken(
                        store: tokens,
                        id: TokenID(value: spliceTokenID)
                    ))
                    if emittedIdentity.kind == .identity
                        && spliceTokenID + 1 < compositionClosingTokenID
                        && compilerBParserTokenByte(
                            source: source,
                            tokens: tokens,
                            tokenID: spliceTokenID + 1
                        ) == 40 {
                        let emittedClosingTokenID: Int(compilerBParserMatchingRightParenthesis(
                            source: source,
                            tokens: tokens,
                            openingTokenID: spliceTokenID + 1
                        ))
                        if emittedClosingTokenID < 0
                            || emittedClosingTokenID >= compositionClosingTokenID {
                            return 0 - 1
                        }
                        state belongsToMember: Bool(false)
                        extensionMemberRow: firstExtensionMemberRow
                        while extensionMemberRow < members.syntaxIDs.count {
                            if members.ownerSyntaxIDs.element(index: extensionMemberRow)
                                == extensionSyntaxID {
                                let memberNodeRow: Int(nodes.ids.firstIndex(value: members.syntaxIDs.element(
                                        index: extensionMemberRow
                                    )))
                                if memberNodeRow >= 0
                                    && spliceTokenID >= nodes.firstTokenIDs.element(
                                        index: memberNodeRow
                                    )
                                    && spliceTokenID < nodes.afterLastTokenIDs.element(
                                        index: memberNodeRow
                                    ) {
                                    belongsToMember: true
                                }
                            }
                            extensionMemberRow: extensionMemberRow + 1
                        }
                        if !belongsToMember {
                            let emittedSyntaxID: Int(compilerBParseExpressionFacet(
                                source: source,
                                tokens: tokens,
                                nodes: nodes,
                                process: process,
                                firstTokenID: spliceTokenID,
                                afterLastTokenID: emittedClosingTokenID + 1
                            ))
                            if emittedSyntaxID < 0 { return 0 - 1 }
                            state emittedOrdinal: Int(0)
                            environmentNodeRow: 0
                            while environmentNodeRow
                                < process.environmentNodes.syntaxIDs.count {
                                if process.environmentNodes.environmentSyntaxIDs.element(
                                    index: environmentNodeRow
                                ) == environmentSyntaxID
                                    && process.environmentNodes.parentSyntaxIDs.element(
                                        index: environmentNodeRow
                                    ) == extensionSyntaxID {
                                    emittedOrdinal: emittedOrdinal + 1
                                }
                                environmentNodeRow: environmentNodeRow + 1
                            }
                            if process.environmentNodes.environmentSyntaxIDs.append(
                                element: environmentSyntaxID
                            ) != 0 { return 0 - 1 }
                            if process.environmentNodes.syntaxIDs.append(
                                element: emittedSyntaxID
                            ) != 0 { return 0 - 1 }
                            if process.environmentNodes.parentSyntaxIDs.append(
                                element: extensionSyntaxID
                            ) != 0 { return 0 - 1 }
                            if process.environmentNodes.ordinals.append(
                                element: emittedOrdinal
                            ) != 0 { return 0 - 1 }
                        }
                        spliceTokenID: emittedClosingTokenID
                    }
                }
                spliceTokenID: spliceTokenID + 1
            }
            tokenID: compositionClosingTokenID + 1
        } else {
            tokenID: tokenID + 1
        }
    }
    return 0
}

function compilerBParseFreestandingMacroApplicationsInBlock(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let applications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let closingTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    state depth: Int(0)
    while tokenID < closingTokenID {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: tokenID
        ))
        if depth == 0 && byte == 64 {
            let afterApplicationTokenID: Int(compilerBParseLocalMacroApplication(
                source: source,
                tokens: tokens,
                nodes: nodes,
                applications: applications,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                applicationTokenID: tokenID,
                limitTokenID: closingTokenID
            ))
            if afterApplicationTokenID < 0 { return afterApplicationTokenID }
            tokenID: afterApplicationTokenID
        } else {
            if byte == 123 { depth: depth + 1 }
            if byte == 125 { depth: depth - 1 }
            tokenID: tokenID + 1
        }
    }
    return 0
}

function compilerBParseSwitchCaseConditionExpression(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let caseSyntaxID: Int,
    let subjectExpressionSyntaxID: Int,
    let firstPatternTokenID: Int,
    let afterLastPatternTokenID: Int
): Int {
    if firstPatternTokenID < 0 || firstPatternTokenID >= afterLastPatternTokenID {
        return 0 - 1
    }
    state operatorCode: Int(compilerBComparisonOperatorEqualCode())
    state firstRightTokenID: Int(firstPatternTokenID)
    state rightExpressionSyntaxID: Int(0 - 1)
    if afterLastPatternTokenID == firstPatternTokenID + 2
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: firstPatternTokenID
        ) == 46 {
        let absence: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: firstPatternTokenID + 1)
        ))
        if absence.kind != .identity || !compilerBTokenMatchesBytes(
            source: source,
            token: absence,
            expected: "none"
        ) { return 0 - 1 }
        operatorCode: compilerBComparisonOperatorNoneCode()
    } else {
        let firstByte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: firstPatternTokenID
        ))
        if firstByte == 60 || firstByte == 62 {
            firstRightTokenID: firstPatternTokenID + 1
            state inclusive: Bool(false)
            if firstRightTokenID < afterLastPatternTokenID
                && compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: firstRightTokenID
                ) == 61 {
                inclusive: true
                firstRightTokenID: firstRightTokenID + 1
            }
            if firstByte == 60 {
                operatorCode: compilerBComparisonOperatorLessCode()
                if inclusive {
                    operatorCode: compilerBComparisonOperatorLessOrEqualCode()
                }
            } else {
                operatorCode: compilerBComparisonOperatorGreaterCode()
                if inclusive {
                    operatorCode: compilerBComparisonOperatorGreaterOrEqualCode()
                }
            }
        }
        rightExpressionSyntaxID: compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: firstRightTokenID,
            afterLastTokenID: afterLastPatternTokenID
        )
        if rightExpressionSyntaxID < 0 { return rightExpressionSyntaxID }
    }
    if compilerBExpressionFacetStoreAppend(
        store: process.expressions,
        syntaxID: caseSyntaxID,
        kindCode: compilerBParserExpressionKindComparisonCode(),
        firstTokenID: firstPatternTokenID,
        afterLastTokenID: afterLastPatternTokenID,
        integerValue: 0
    ) != 0 { return 0 - 1 }
    if process.comparisons.syntaxIDs.append(element: caseSyntaxID) != 0 {
        return 0 - 1
    }
    if process.comparisons.leftExpressionSyntaxIDs.append(
        element: subjectExpressionSyntaxID
    ) != 0 { return 0 - 1 }
    if process.comparisons.rightExpressionSyntaxIDs.append(
        element: rightExpressionSyntaxID
    ) != 0 { return 0 - 1 }
    if process.comparisons.operatorCodes.append(element: operatorCode) != 0 {
        return 0 - 1
    }
    return caseSyntaxID
}

function compilerBParseSwitchProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let macroApplications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let switchTokenID: Int,
    let limitTokenID: Int
): Int {
    state openingTokenID: Int(switchTokenID + 1)
    while openingTokenID < limitTokenID
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: openingTokenID
        ) != 123 {
        openingTokenID: openingTokenID + 1
    }
    if openingTokenID >= limitTokenID || openingTokenID == switchTokenID + 1 {
        return 0 - 1
    }
    let closingTokenID: Int(compilerBParserMatchingRightBrace(
        source: source,
        tokens: tokens,
        openingTokenID: openingTokenID,
        limitTokenID: limitTokenID
    ))
    if closingTokenID < 0 { return 0 - 1 }
    let keyword: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: switchTokenID)
    ))
    let subjectSyntaxID: Int(compilerBParseExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: switchTokenID + 1,
        afterLastTokenID: openingTokenID
    ))
    if subjectSyntaxID < 0 { return 0 - 1 }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: keyword.start,
        firstTokenID: switchTokenID,
        afterLastTokenID: closingTokenID + 1
    ) != 0 { return 0 - 1 }
    if process.switches.syntaxIDs.append(element: keyword.start) != 0 { return 0 - 1 }
    if process.switches.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
        return 0 - 1
    }
    if process.switches.subjectExpressionSyntaxIDs.append(
        element: subjectSyntaxID
    ) != 0 { return 0 - 1 }

    state tokenID: Int(openingTokenID + 1)
    state ordinal: Int(0)
    while tokenID < closingTokenID {
        let candidate: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        state isCase: Bool(false)
        state isDefault: Bool(false)
        if candidate.kind == .identity {
            isCase: compilerBTokenMatchesBytes(
                source: source,
                token: candidate,
                expected: "case"
            )
            isDefault: compilerBTokenMatchesBytes(
                source: source,
                token: candidate,
                expected: "default"
            )
        }
        if !isCase && !isDefault { return 0 - 1 }
        state colonTokenID: Int(tokenID + 1)
        if isCase {
            while colonTokenID < closingTokenID
                && compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: colonTokenID
                ) != 58 {
                colonTokenID: colonTokenID + 1
            }
        }
        if colonTokenID >= closingTokenID
            || compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: colonTokenID
            ) != 58 { return 0 - 1 }
        let firstBodyTokenID: Int(colonTokenID + 1)
        state afterLastBodyTokenID: Int(firstBodyTokenID)
        state depth: Int(0)
        state foundNextCase: Bool(false)
        while afterLastBodyTokenID < closingTokenID && !foundNextCase {
            let bodyToken: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: afterLastBodyTokenID)
            ))
            state bodyStartsCase: Bool(false)
            if depth == 0 && bodyToken.kind == .identity {
                bodyStartsCase: compilerBTokenMatchesBytes(
                    source: source,
                    token: bodyToken,
                    expected: "case"
                ) || compilerBTokenMatchesBytes(
                    source: source,
                    token: bodyToken,
                    expected: "default"
                )
            }
            if bodyStartsCase {
                foundNextCase: true
            } else {
                let byte: Int(compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: afterLastBodyTokenID
                ))
                if byte == 123 { depth: depth + 1 }
                if byte == 125 { depth: depth - 1 }
                afterLastBodyTokenID: afterLastBodyTokenID + 1
            }
        }
        if syntaxNodeStoreAppend(
            store: nodes,
            id: candidate.start,
            firstTokenID: tokenID,
            afterLastTokenID: afterLastBodyTokenID
        ) != 0 { return 0 - 1 }
        if process.switchCases.syntaxIDs.append(element: candidate.start) != 0 {
            return 0 - 1
        }
        if process.switchCases.switchSyntaxIDs.append(element: keyword.start) != 0 {
            return 0 - 1
        }
        state conditionExpressionSyntaxID: Int(0 - 1)
        if isCase {
            conditionExpressionSyntaxID: compilerBParseSwitchCaseConditionExpression(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                caseSyntaxID: candidate.start,
                subjectExpressionSyntaxID: subjectSyntaxID,
                firstPatternTokenID: tokenID + 1,
                afterLastPatternTokenID: colonTokenID
            )
            if conditionExpressionSyntaxID < 0 { return conditionExpressionSyntaxID }
        }
        if process.switchCases.conditionExpressionSyntaxIDs.append(
            element: conditionExpressionSyntaxID
        ) != 0 { return 0 - 1 }
        if process.switchCases.firstBodyTokenIDs.append(
            element: firstBodyTokenID
        ) != 0 { return 0 - 1 }
        if process.switchCases.afterLastBodyTokenIDs.append(
            element: afterLastBodyTokenID
        ) != 0 { return 0 - 1 }
        if process.switchCases.ordinals.append(element: ordinal) != 0 {
            return 0 - 1
        }
        if compilerBParseFreestandingMacroApplicationsInBlock(
            source: source,
            tokens: tokens,
            nodes: nodes,
            applications: macroApplications,
            process: process,
            ownerSyntaxID: candidate.start,
            openingTokenID: colonTokenID,
            closingTokenID: afterLastBodyTokenID
        ) != 0 { return 0 - 1 }
        ordinal: ordinal + 1
        tokenID: afterLastBodyTokenID
    }
    return closingTokenID + 1
}

function compilerBConditionalAlternativeNone(): Int { return 0 }
function compilerBConditionalAlternativeConditional(): Int { return 1 }
function compilerBConditionalAlternativeBlock(): Int { return 2 }

function compilerBParseConditionalProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let blocks: CompilerBBlockFacetStore,
    let macroApplications: CompilerBMacroApplicationStore,
    let process: CompilerBProcessGraph,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let ifTokenID: Int,
    let limitTokenID: Int
): Int {
    let keyword: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: ifTokenID)
    ))
    state openingTokenID: Int(ifTokenID + 1)
    while openingTokenID < limitTokenID
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: openingTokenID
        ) != 123 {
        openingTokenID: openingTokenID + 1
    }
    if openingTokenID >= limitTokenID { return 0 - 41 }
    let closingTokenID: Int(compilerBParserMatchingRightBrace(
        source: source,
        tokens: tokens,
        openingTokenID: openingTokenID,
        limitTokenID: limitTokenID
    ))
    if closingTokenID < 0 { return 0 - 42 }
    let opening: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: openingTokenID)
    ))
    if syntaxNodeStoreAppend(
        store: nodes,
        id: keyword.start,
        firstTokenID: ifTokenID,
        afterLastTokenID: closingTokenID + 1
    ) != 0 { return 0 - 43 }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: opening.start,
        firstTokenID: openingTokenID,
        afterLastTokenID: closingTokenID + 1
    ) != 0 { return 0 - 43 }
    if compilerBBlockFacetStoreAppend(
        store: blocks,
        syntaxID: opening.start,
        parentSyntaxID: keyword.start
    ) != 0 { return 0 - 44 }
    let conditionExpressionSyntaxID: Int(compilerBParseExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: ifTokenID + 1,
        afterLastTokenID: openingTokenID
    ))
    if conditionExpressionSyntaxID < 0 { return conditionExpressionSyntaxID }
    let conditionalRow: Int(process.conditionals.syntaxIDs.count)
    if process.conditionals.syntaxIDs.append(element: keyword.start) != 0 { return 0 - 46 }
    if process.conditionals.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
        return 0 - 46
    }
    if process.conditionals.firstConditionTokenIDs.append(element: ifTokenID + 1) != 0 {
        return 0 - 46
    }
    if process.conditionals.afterLastConditionTokenIDs.append(element: openingTokenID) != 0 {
        return 0 - 46
    }
    if process.conditionals.conditionExpressionSyntaxIDs.append(
        element: conditionExpressionSyntaxID
    ) != 0 { return 0 - 46 }
    if process.conditionals.bodySyntaxIDs.append(element: opening.start) != 0 {
        return 0 - 46
    }
    if process.conditionals.alternativeSyntaxIDs.append(element: 0 - 1) != 0 {
        return 0 - 46
    }
    if process.conditionals.alternativeKindCodes.append(
        element: compilerBConditionalAlternativeNone()
    ) != 0 { return 0 - 46 }
    let bodyStatus: Int(compilerBParseProcessFacets(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        blocks: blocks,
        macroApplications: macroApplications,
        members: members,
        ownerSyntaxID: opening.start,
        bodySyntaxID: opening.start,
        openingTokenID: openingTokenID,
        afterLastTokenID: closingTokenID + 1
    ))
    if bodyStatus != 0 { return bodyStatus }
    state afterConditionalTokenID: Int(closingTokenID + 1)
    if afterConditionalTokenID < limitTokenID {
        let alternative: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: afterConditionalTokenID)
        ))
        if alternative.kind == .identity && compilerBTokenMatchesBytes(
            source: source,
            token: alternative,
            expected: "else"
        ) {
            if afterConditionalTokenID + 1 >= limitTokenID { return 0 - 48 }
            let nextTokenID: Int(afterConditionalTokenID + 1)
            let next: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: nextTokenID)
            ))
            if next.kind == .identity && compilerBTokenMatchesBytes(
                source: source,
                token: next,
                expected: "if"
            ) {
                if process.conditionals.alternativeSyntaxIDs.update(
                    element: next.start,
                    index: conditionalRow
                ) != 0 || process.conditionals.alternativeKindCodes.update(
                    element: compilerBConditionalAlternativeConditional(),
                    index: conditionalRow
                ) != 0 { return 0 - 49 }
                afterConditionalTokenID: compilerBParseConditionalProcessFacet(
                    source: source,
                    tokens: tokens,
                    nodes: nodes,
                    blocks: blocks,
                    macroApplications: macroApplications,
                    process: process,
                    members: members,
                    ownerSyntaxID: keyword.start,
                    ifTokenID: nextTokenID,
                    limitTokenID: limitTokenID
                )
                if afterConditionalTokenID < 0 { return afterConditionalTokenID }
            } else if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: nextTokenID
            ) == 123 {
                let alternativeClosingTokenID: Int(compilerBParserMatchingRightBrace(
                    source: source,
                    tokens: tokens,
                    openingTokenID: nextTokenID,
                    limitTokenID: limitTokenID
                ))
                if alternativeClosingTokenID < 0 { return 0 - 50 }
                if syntaxNodeStoreAppend(
                    store: nodes,
                    id: next.start,
                    firstTokenID: nextTokenID,
                    afterLastTokenID: alternativeClosingTokenID + 1
                ) != 0 || compilerBBlockFacetStoreAppend(
                    store: blocks,
                    syntaxID: next.start,
                    parentSyntaxID: keyword.start
                ) != 0 { return 0 - 51 }
                if process.conditionals.alternativeSyntaxIDs.update(
                    element: next.start,
                    index: conditionalRow
                ) != 0 || process.conditionals.alternativeKindCodes.update(
                    element: compilerBConditionalAlternativeBlock(),
                    index: conditionalRow
                ) != 0 { return 0 - 52 }
                let alternativeStatus: Int(compilerBParseProcessFacets(
                    source: source,
                    tokens: tokens,
                    nodes: nodes,
                    process: process,
                    blocks: blocks,
                    macroApplications: macroApplications,
                    members: members,
                    ownerSyntaxID: next.start,
                    bodySyntaxID: next.start,
                    openingTokenID: nextTokenID,
                    afterLastTokenID: alternativeClosingTokenID + 1
                ))
                if alternativeStatus != 0 { return alternativeStatus }
                afterConditionalTokenID: alternativeClosingTokenID + 1
            } else {
                return 0 - 54
            }
        }
    }
    return afterConditionalTokenID
}

function compilerBParseExecutionProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let ownerSyntaxID: Int,
    let firstTokenID: Int,
    let limitTokenID: Int
): Int {
    state openingTokenID: Int(firstTokenID)
    while openingTokenID < limitTokenID
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: openingTokenID
        ) != 40 {
        openingTokenID: openingTokenID + 1
    }
    if openingTokenID >= limitTokenID { return 0 - 1 }
    state closingTokenID: Int(compilerBParserMatchingRightParenthesis(
        source: source,
        tokens: tokens,
        openingTokenID: openingTokenID
    ))
    if closingTokenID < 0 || closingTokenID >= limitTokenID { return 0 - 1 }
    state afterLastApplicationTokenID: Int(closingTokenID + 1)
    state chaining: Bool(true)
    while chaining && afterLastApplicationTokenID + 2 < limitTokenID {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: afterLastApplicationTokenID
        ) == 46
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: afterLastApplicationTokenID + 2
            ) == 40 {
            closingTokenID: compilerBParserMatchingRightParenthesis(
                source: source,
                tokens: tokens,
                openingTokenID: afterLastApplicationTokenID + 2
            )
            if closingTokenID < 0 || closingTokenID >= limitTokenID {
                return 0 - 1
            }
            afterLastApplicationTokenID: closingTokenID + 1
        } else {
            chaining: false
        }
    }
    let expressionSyntaxID: Int(compilerBParseExpressionFacet(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        firstTokenID: firstTokenID,
        afterLastTokenID: afterLastApplicationTokenID
    ))
    if expressionSyntaxID < 0 { return expressionSyntaxID }
    state ordinal: Int(0)
    state row: Int(0)
    while row < process.executions.ownerSyntaxIDs.count {
        if process.executions.ownerSyntaxIDs.element(index: row) == ownerSyntaxID {
            ordinal: ordinal + 1
        }
        row: row + 1
    }
    if process.executions.syntaxIDs.append(element: expressionSyntaxID) != 0 {
        return 0 - 1
    }
    if process.executions.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
        return 0 - 1
    }
    if process.executions.expressionSyntaxIDs.append(element: expressionSyntaxID) != 0 {
        return 0 - 1
    }
    if process.executions.ordinals.append(element: ordinal) != 0 { return 0 - 1 }
    return afterLastApplicationTokenID
}

// A value followed by a body is retained as one ordinary Application. The
// declaration selected by its @syntax plan gives the body semantic meaning;
// this parser path does not identify conditions, selections, or source names.
function compilerBParseBraceApplicationProcessFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let blocks: CompilerBBlockFacetStore,
    let ownerSyntaxID: Int,
    let firstTokenID: Int,
    let limitTokenID: Int
): Int {
    if firstTokenID + 1 >= limitTokenID { return 0 - 1 }
    let target: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: firstTokenID)
    ))
    if target.kind != .identity || compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: firstTokenID + 1
    ) != 123 { return 0 - 1 }
    let closingTokenID: Int(compilerBParserMatchingRightBrace(
        source: source,
        tokens: tokens,
        openingTokenID: firstTokenID + 1,
        limitTokenID: limitTokenID
    ))
    if closingTokenID < 0 { return 0 - 1 }
    let syntaxID: Int(target.start)
    let body: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: firstTokenID + 1)
    ))
    if syntaxNodeStoreAppend(
        store: nodes,
        id: syntaxID,
        firstTokenID: firstTokenID,
        afterLastTokenID: closingTokenID + 1
    ) != 0 || compilerBExpressionFacetStoreAppend(
        store: process.expressions,
        syntaxID: syntaxID,
        kindCode: compilerBParserExpressionKindCallCode(),
        firstTokenID: firstTokenID,
        afterLastTokenID: closingTokenID + 1,
        integerValue: 0
    ) != 0 || compilerBApplicationFacetStoreAppend(
        store: process.applications,
        syntaxID: syntaxID,
        predecessorSyntaxID: 0 - 1,
        identityTokenID: firstTokenID,
        firstTokenID: firstTokenID,
        afterLastTokenID: closingTokenID + 1
    ) != 0 || syntaxNodeStoreAppend(
        store: nodes,
        id: body.start,
        firstTokenID: firstTokenID + 1,
        afterLastTokenID: closingTokenID + 1
    ) != 0 || compilerBBlockFacetStoreAppend(
        store: blocks,
        syntaxID: body.start,
        parentSyntaxID: syntaxID
    ) != 0 { return 0 - 1 }
    state ordinal: Int(0)
    state row: Int(0)
    while row < process.executions.ownerSyntaxIDs.count {
        if process.executions.ownerSyntaxIDs.element(index: row) == ownerSyntaxID {
            ordinal: ordinal + 1
        }
        row: row + 1
    }
    if process.executions.syntaxIDs.append(element: syntaxID) != 0
        || process.executions.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0
        || process.executions.expressionSyntaxIDs.append(element: syntaxID) != 0
        || process.executions.ordinals.append(element: ordinal) != 0 {
        return 0 - 1
    }
    return closingTokenID + 1
}

function compilerBParserCallStartsAt(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let firstTokenID: Int,
    let limitTokenID: Int
): Bool {
    if firstTokenID + 1 < limitTokenID
        && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: firstTokenID + 1
        ) == 40 {
        return true
    }
    if firstTokenID + 3 >= limitTokenID { return false }
    state cursor: Int(firstTokenID + 1)
    state hasAttachment: Bool(false)
    while cursor < limitTokenID {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: cursor
        ))
        if byte == 40 { return hasAttachment }
        if byte != 46 || cursor + 1 >= limitTokenID { return false }
        let segment: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: cursor + 1)
        ))
        if segment.kind != .identity { return false }
        hasAttachment: true
        cursor: cursor + 2
    }
    return false
}

function compilerBProcessTerminalExpressionSyntaxID(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let openingTokenID: Int,
    let afterLastTokenID: Int
): Int {
    let bodyAfterLastTokenID: Int(afterLastTokenID - 1)
    state lineFirstTokenID: Int(openingTokenID + 1)
    state terminalFirstTokenID: Int(0 - 1)
    state terminalAfterLastTokenID: Int(0 - 1)
    while lineFirstTokenID < bodyAfterLastTokenID {
        terminalFirstTokenID: lineFirstTokenID
        terminalAfterLastTokenID: compilerBParserLineAfterLastTokenID(
            source: source,
            tokens: tokens,
            firstTokenID: lineFirstTokenID,
            limitTokenID: bodyAfterLastTokenID
        )
        if terminalAfterLastTokenID <= lineFirstTokenID { return 0 - 2 }
        lineFirstTokenID: terminalAfterLastTokenID
    }
    if terminalFirstTokenID < 0 { return 0 - 1 }

    let terminalFirst: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: terminalFirstTokenID)
    ))
    if terminalFirst.kind == .identity && compilerBTokenMatchesBytes(
        source: source,
        token: terminalFirst,
        expected: "return"
    ) {
        terminalFirstTokenID: terminalFirstTokenID + 1
    }
    if terminalFirstTokenID >= terminalAfterLastTokenID { return 0 - 1 }

    let expressionFirst: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: terminalFirstTokenID)
    ))
    if expressionFirst.kind == .identity
        && (compilerBTokenMatchesBytes(
                source: source,
                token: expressionFirst,
                expected: "let"
            )
            || compilerBTokenMatchesBytes(
                source: source,
                token: expressionFirst,
                expected: "state"
            )
            || compilerBTokenMatchesBytes(
                source: source,
                token: expressionFirst,
                expected: "if"
            )
            || compilerBTokenMatchesBytes(
                source: source,
                token: expressionFirst,
                expected: "switch"
            )
            || compilerBTokenMatchesBytes(
                source: source,
                token: expressionFirst,
                expected: "while"
            )) {
        return 0 - 1
    }
    if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: terminalFirstTokenID
    ) == 64 { return 0 - 1 }

    state delimiterDepth: Int(0)
    state cursor: Int(terminalFirstTokenID)
    while cursor < terminalAfterLastTokenID {
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: cursor
        ))
        if byte == 123 && delimiterDepth == 0 { return 0 - 1 }
        if byte == 40 || byte == 91 { delimiterDepth: delimiterDepth + 1 }
        if byte == 41 || byte == 93 { delimiterDepth: delimiterDepth - 1 }
        cursor: cursor + 1
    }

    state expressionRow: Int(0)
    while expressionRow < process.expressions.syntaxIDs.count {
        if process.expressions.firstTokenIDs.element(index: expressionRow)
                == terminalFirstTokenID
            && process.expressions.afterLastTokenIDs.element(index: expressionRow)
                == terminalAfterLastTokenID {
            return process.expressions.syntaxIDs.element(index: expressionRow)
        }
        expressionRow: expressionRow + 1
    }
    state expressionSyntaxID: Int(0 - 1)
    if compilerBParserCallStartsAt(
        source: source,
        tokens: tokens,
        firstTokenID: terminalFirstTokenID,
        limitTokenID: terminalAfterLastTokenID
    ) {
        expressionSyntaxID: compilerBParseExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: terminalFirstTokenID,
            afterLastTokenID: terminalAfterLastTokenID
        )
    } else {
        expressionSyntaxID: compilerBParseLeafExpressionFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            firstTokenID: terminalFirstTokenID,
            afterLastTokenID: terminalAfterLastTokenID
        )
    }
    if expressionSyntaxID < 0 { return 0 - 2 }
    return expressionSyntaxID
}

function compilerBParseProcessFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let process: CompilerBProcessGraph,
    let blocks: CompilerBBlockFacetStore,
    let macroApplications: CompilerBMacroApplicationStore,
    let members: CompilerBMemberFacetStore,
    let ownerSyntaxID: Int,
    let bodySyntaxID: Int,
    let openingTokenID: Int,
    let afterLastTokenID: Int
): Int {
    state operationTokenID: Int(openingTokenID + 1)
    state operationDepth: Int(0)
    while operationTokenID < afterLastTokenID - 1 {
        let operation: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: operationTokenID)
        ))
        let operationByte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: operationTokenID
        ))
        if operationDepth == 0 && operationByte == 64 {
            let targetTokenID: Int(compilerBParseLocalMacroApplication(
                source: source,
                tokens: tokens,
                nodes: nodes,
                applications: macroApplications,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                applicationTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if targetTokenID < 0 { return 0 - 21 }
            operationTokenID: targetTokenID
        } else if operationDepth == 0 && operation.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: operation,
                expected: "function"
            ) {
            let declarationNodeRow: Int(syntaxNodeRowForID(
                store: nodes,
                syntaxID: operation.start
            ))
            if declarationNodeRow < 0 { return 0 - 22 }
            operationTokenID: nodes.afterLastTokenIDs.element(
                index: declarationNodeRow
            )
        } else if operationDepth == 0 && operation.kind == .identity
            && (compilerBTokenMatchesBytes(source: source, token: operation, expected: "let")
                || compilerBTokenMatchesBytes(
                    source: source,
                    token: operation,
                    expected: "state"
                )) {
            let afterLocal: Int(compilerBParseLocalBindingProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                memberTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterLocal < 0 { return 0 - 23 }
            operationTokenID: afterLocal
        } else if operationDepth == 0 && operation.kind == .identity
            && operationTokenID + 1 < afterLastTokenID - 1
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: operationTokenID + 1
            ) == 123 {
            let afterApplication: Int(compilerBParseBraceApplicationProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                blocks: blocks,
                ownerSyntaxID: ownerSyntaxID,
                firstTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterApplication < 0 { return 0 - 24 }
            operationTokenID: afterApplication
        } else if operationDepth == 0 && operation.kind == .identity
            && compilerBTokenMatchesBytes(source: source, token: operation, expected: "if") {
            let afterConditional: Int(compilerBParseConditionalProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                blocks: blocks,
                macroApplications: macroApplications,
                process: process,
                members: members,
                ownerSyntaxID: ownerSyntaxID,
                ifTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterConditional < 0 { return afterConditional }
            operationTokenID: afterConditional
        } else if operationDepth == 0 && operation.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: operation,
                expected: "switch"
            ) {
            let afterSwitch: Int(compilerBParseSwitchProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                macroApplications: macroApplications,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                switchTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterSwitch < 0 { return 0 - 25 }
            operationTokenID: afterSwitch
        } else if operationDepth == 0 && operation.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: operation,
                expected: "return"
            ) {
            let afterReturn: Int(compilerBParseReturnProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                returnTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterReturn < 0 { return 0 - 26 }
            operationTokenID: afterReturn
        } else if operationDepth == 0 && operation.kind == .identity
            && operationTokenID + 1 < afterLastTokenID - 1
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: operationTokenID + 1
            ) == 58 {
            let afterAssignment: Int(compilerBParseAssignmentProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                identityTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterAssignment < 0 { return 0 - 27 }
            operationTokenID: afterAssignment
        } else if operationDepth == 0 && operation.kind == .identity
            && compilerBParserCallStartsAt(
                source: source,
                tokens: tokens,
                firstTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ) {
            let afterExecution: Int(compilerBParseExecutionProcessFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                ownerSyntaxID: ownerSyntaxID,
                firstTokenID: operationTokenID,
                limitTokenID: afterLastTokenID - 1
            ))
            if afterExecution < 0 { return 0 - 28 }
            operationTokenID: afterExecution
        } else {
            if operationByte == 40 || operationByte == 91 || operationByte == 123 {
                operationDepth: operationDepth + 1
            }
            if operationByte == 41 || operationByte == 93 || operationByte == 125 {
                operationDepth: operationDepth - 1
            }
            operationTokenID: operationTokenID + 1
        }
    }
    if compilerBParseApplicationProvisionFacets(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        macroApplications: macroApplications,
        members: members,
        ownerSyntaxID: ownerSyntaxID,
        openingTokenID: openingTokenID,
        afterLastTokenID: afterLastTokenID
    ) != 0 { return 0 - 29 }
    let resultExpressionSyntaxID: Int(compilerBProcessTerminalExpressionSyntaxID(
        source: source,
        tokens: tokens,
        nodes: nodes,
        process: process,
        openingTokenID: openingTokenID,
        afterLastTokenID: afterLastTokenID
    ))
    if resultExpressionSyntaxID < 0 && resultExpressionSyntaxID != 0 - 1 {
        return 0 - 30
    }
    if process.processes.ownerSyntaxIDs.append(element: ownerSyntaxID) != 0 {
        return 0 - 31
    }
    if process.processes.bodySyntaxIDs.append(element: bodySyntaxID) != 0 {
        return 0 - 32
    }
    if process.processes.resultExpressionSyntaxIDs.append(
        element: resultExpressionSyntaxID
    ) != 0 { return 0 - 33 }
    return 0
}

function compilerBParseFunctionParameterFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let parameters: CompilerBFunctionParameterFacetStore,
    let functionSyntaxID: Int,
    let openingTokenID: Int,
    let closingTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    state ordinal: Int(0)
    while tokenID < closingTokenID {
        if tokenID + 3 >= closingTokenID { return 0 - 1 }
        let keyword: CompilerBToken(compilerBRetainedToken(
            store: tokens, id: TokenID(value: tokenID)
        ))
        if !compilerBTokenMatchesBytes(source: source, token: keyword, expected: "let")
            && !compilerBTokenMatchesBytes(source: source, token: keyword, expected: "binding") {
            return 0 - 1
        }
        let identityTokenID: Int(tokenID + 1)
        let colonTokenID: Int(tokenID + 2)
        let identity: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: identityTokenID)
        ))
        if identity.kind != .identity || compilerBTokenMatchesBytes(
            source: source, token: identity, expected: "_"
        ) { return 0 - 1 }
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: colonTokenID
        ) != 58 { return 0 - 1 }
        state typeTokenID: Int(colonTokenID + 1)
        if typeTokenID < closingTokenID && compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: typeTokenID
        ) == 64 {
            typeTokenID: typeTokenID + 1
        }
        if typeTokenID >= closingTokenID { return 0 - 1 }
        if parameters.functionSyntaxIDs.append(element: functionSyntaxID) != 0 {
            return 0 - 1
        }
        if parameters.keywordTokenIDs.append(element: tokenID) != 0 { return 0 - 1 }
        if parameters.identityTokenIDs.append(element: identityTokenID) != 0 {
            return 0 - 1
        }
        if parameters.typeTokenIDs.append(element: typeTokenID) != 0 { return 0 - 1 }
        if parameters.ordinals.append(element: ordinal) != 0 { return 0 - 1 }
        let end: Int(compilerBParserArgumentAfterLast(
            source: source, tokens: tokens, firstTokenID: typeTokenID,
            limitTokenID: closingTokenID
        ))
        if end <= typeTokenID { return 0 - 1 }
        tokenID: end + 1
        ordinal: ordinal + 1
    }
    return 0
}

function compilerBParseFunctionFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let signatures: CompilerBFunctionSignatureFacetStore,
    let parameters: CompilerBFunctionParameterFacetStore,
    let functionSyntaxID: Int,
    let nameTokenID: Int,
    let openingTokenID: Int,
    let afterLastTokenID: Int
): Int {
    let signatureToken: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: nameTokenID + 1)
    ))
    if syntaxNodeStoreAppend(
        store: nodes,
        id: signatureToken.start,
        firstTokenID: nameTokenID + 1,
        afterLastTokenID: openingTokenID
    ) != 0 { return 0 - 1 }
    state separatorTokenID: Int(nameTokenID + 1)
    if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: separatorTokenID) == 60 {
        separatorTokenID: compilerBParserMatchingRightAngle(
            source: source, tokens: tokens, openingTokenID: separatorTokenID, limitTokenID: openingTokenID
        )
        if separatorTokenID < 0 { return 0 - 1 }
        separatorTokenID: separatorTokenID + 1
    }
    if separatorTokenID < openingTokenID {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: separatorTokenID
        ) == 40 {
            let parameterClosingTokenID: Int(compilerBParserMatchingRightParenthesis(
                source: source,
                tokens: tokens,
                openingTokenID: separatorTokenID
            ))
            if parameterClosingTokenID < 0 || parameterClosingTokenID >= openingTokenID {
                return 0 - 1
            }
            if compilerBParseFunctionParameterFacets(
                source: source,
                tokens: tokens,
                parameters: parameters,
                functionSyntaxID: functionSyntaxID,
                openingTokenID: separatorTokenID,
                closingTokenID: parameterClosingTokenID
            ) != 0 { return 0 - 1 }
            separatorTokenID: parameterClosingTokenID + 1
        }
    }
    while separatorTokenID < openingTokenID
        && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: separatorTokenID) != 58 {
        separatorTokenID: separatorTokenID + 1
    }
    state returnTypeTokenID: Int(0 - 1)
    state inputCardinalityTokenID: Int(0 - 1)
    state outputCardinalityTokenID: Int(0 - 1)
    if separatorTokenID + 1 < openingTokenID {
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: separatorTokenID + 1
        ) == 64 {
            if separatorTokenID + 2 >= openingTokenID { return 0 - 1 }
            state arrowTokenID: Int(separatorTokenID + 3)
            state cardinalityArrowTokenID: Int(0 - 1)
            while arrowTokenID + 2 < openingTokenID
                && cardinalityArrowTokenID < 0 {
                if compilerBParserTokenByte(
                    source: source,
                    tokens: tokens,
                    tokenID: arrowTokenID
                ) == 45 {
                    if compilerBParserTokenByte(
                        source: source,
                        tokens: tokens,
                        tokenID: arrowTokenID + 1
                    ) == 62 {
                        if compilerBParserTokenByte(
                            source: source,
                            tokens: tokens,
                            tokenID: arrowTokenID + 2
                        ) == 64 {
                            cardinalityArrowTokenID: arrowTokenID
                        }
                    }
                }
                arrowTokenID: arrowTokenID + 1
            }
            if cardinalityArrowTokenID >= 0 {
                inputCardinalityTokenID: separatorTokenID + 2
                outputCardinalityTokenID: cardinalityArrowTokenID + 3
            } else {
                returnTypeTokenID: separatorTokenID + 2
            }
        } else {
            returnTypeTokenID: separatorTokenID + 1
        }
    }
    return compilerBFunctionSignatureFacetStoreAppend(
        store: signatures,
        functionSyntaxID: functionSyntaxID,
        syntaxID: signatureToken.start,
        returnTypeTokenID: returnTypeTokenID,
        inputCardinalityTokenID: inputCardinalityTokenID,
        outputCardinalityTokenID: outputCardinalityTokenID
    )
}

function compilerBParseMacroSignatureFacet(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let signatures: CompilerBMacroSignatureFacetStore,
    let macroSyntaxID: Int,
    let nameTokenID: Int,
    let openingTokenID: Int
): Int {
    state parameterOpeningTokenID: Int(nameTokenID + 1)
    if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: parameterOpeningTokenID) == 60 {
        parameterOpeningTokenID: compilerBParserMatchingRightAngle(
            source: source, tokens: tokens, openingTokenID: parameterOpeningTokenID, limitTokenID: openingTokenID
        )
        if parameterOpeningTokenID < 0 { return 0 - 1 }
        parameterOpeningTokenID: parameterOpeningTokenID + 1
    }
    if parameterOpeningTokenID >= openingTokenID || compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: parameterOpeningTokenID
    ) != 40 { return 0 - 1 }
    let parameterClosingTokenID: Int(compilerBParserMatchingRightParenthesis(
        source: source,
        tokens: tokens,
        openingTokenID: parameterOpeningTokenID
    ))
    if parameterClosingTokenID < 0 || parameterClosingTokenID >= openingTokenID {
        return 0 - 1
    }
    let signatureToken: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: parameterOpeningTokenID)
    ))
    if syntaxNodeStoreAppend(
        store: nodes,
        id: signatureToken.start,
        firstTokenID: parameterOpeningTokenID,
        afterLastTokenID: openingTokenID
    ) != 0 { return 0 - 1 }
    state parameterTokenID: Int(parameterOpeningTokenID + 1)
    state parameterOrdinal: Int(0)
    while parameterTokenID < parameterClosingTokenID {
        if parameterTokenID + 3 >= parameterClosingTokenID { return 0 - 1 }
        let keywordTokenID: Int(parameterTokenID)
        let keyword: CompilerBToken(compilerBRetainedToken(
            store: tokens, id: TokenID(value: keywordTokenID)
        ))
        if !compilerBTokenMatchesBytes(source: source, token: keyword, expected: "let")
            && !compilerBTokenMatchesBytes(source: source, token: keyword, expected: "binding") {
            return 0 - 1
        }
        let identityTokenID: Int(parameterTokenID + 1)
        let parameterColonTokenID: Int(parameterTokenID + 2)
        let identity: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: identityTokenID)
        ))
        if identity.kind != .identity || compilerBTokenMatchesBytes(
            source: source, token: identity, expected: "_"
        ) { return 0 - 1 }
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: parameterColonTokenID
        ) != 58 { return 0 - 1 }
        state typeTokenID: Int(parameterColonTokenID + 1)
        if typeTokenID >= parameterClosingTokenID { return 0 - 1 }
        state cardinality: Bool(false)
        if compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: typeTokenID
        ) == 64 {
            cardinality: true
            typeTokenID: typeTokenID + 1
        }
        let type: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: typeTokenID)
        ))
        if type.kind != .identity { return 0 - 1 }
        parameterTokenID: typeTokenID + 1
        if parameterTokenID < parameterClosingTokenID && compilerBParserTokenByte(
            source: source, tokens: tokens, tokenID: parameterTokenID
        ) == 60 {
            parameterTokenID: compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: parameterTokenID,
                limitTokenID: parameterClosingTokenID
            )
            if parameterTokenID < 0 { return 0 - 1 }
            parameterTokenID: parameterTokenID + 1
        }
        state optional: Bool(false)
        if parameterTokenID < parameterClosingTokenID
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: parameterTokenID
            ) == 63 {
            optional: true
            parameterTokenID: parameterTokenID + 1
        }
        state defaultFirstTokenID: Int(0 - 1)
        state defaultAfterLastTokenID: Int(0 - 1)
        if parameterTokenID < parameterClosingTokenID
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: parameterTokenID
            ) == 40 {
            let defaultClosingTokenID: Int(compilerBParserMatchingRightParenthesis(
                source: source,
                tokens: tokens,
                openingTokenID: parameterTokenID
            ))
            if defaultClosingTokenID < 0 || defaultClosingTokenID > parameterClosingTokenID {
                return 0 - 1
            }
            defaultFirstTokenID: parameterTokenID + 1
            defaultAfterLastTokenID: defaultClosingTokenID
            parameterTokenID: defaultClosingTokenID + 1
        }
        if parameterTokenID < parameterClosingTokenID
            && compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: parameterTokenID
            ) == 58 {
            return 0 - 1
        }
        if compilerBMacroParameterFacetStoreAppend(
            store: signatures,
            macroSyntaxID: macroSyntaxID,
            keywordTokenID: keywordTokenID,
            identityTokenID: identityTokenID,
            typeTokenID: typeTokenID,
            cardinality: cardinality,
            optional: optional,
            defaultFirstTokenID: defaultFirstTokenID,
            defaultAfterLastTokenID: defaultAfterLastTokenID,
            ordinal: parameterOrdinal
        ) != 0 { return 0 - 1 }
        parameterOrdinal: parameterOrdinal + 1
        if parameterTokenID < parameterClosingTokenID {
            if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: parameterTokenID
            ) != 44 { return 0 - 1 }
            parameterTokenID: parameterTokenID + 1
        }
    }
    state colonTokenID: Int(parameterClosingTokenID + 1)
    while colonTokenID < openingTokenID
        && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: colonTokenID) != 58 {
        colonTokenID: colonTokenID + 1
    }
    if colonTokenID + 1 >= openingTokenID { return 0 - 1 }
    state targetTypeTokenID: Int(colonTokenID + 1)
    state afterLastTargetTypeTokenID: Int(openingTokenID)
    state inputCardinalityTokenID: Int(0 - 1)
    if compilerBParserTokenByte(
        source: source,
        tokens: tokens,
        tokenID: targetTypeTokenID
    ) == 64 {
        inputCardinalityTokenID: targetTypeTokenID + 1
        targetTypeTokenID: 0 - 1
        afterLastTargetTypeTokenID: 0 - 1
    }
    state arrowTokenID: Int(colonTokenID + 2)
    state resultTypeTokenID: Int(0 - 1)
    state outputCardinalityTokenID: Int(0 - 1)
    while arrowTokenID + 2 < openingTokenID && resultTypeTokenID < 0 {
        if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: arrowTokenID) == 45
            && compilerBParserTokenByte(source: source, tokens: tokens, tokenID: arrowTokenID + 1) == 62 {
            if targetTypeTokenID >= 0 { afterLastTargetTypeTokenID: arrowTokenID }
            if compilerBParserTokenByte(
                source: source,
                tokens: tokens,
                tokenID: arrowTokenID + 2
            ) == 64 {
                outputCardinalityTokenID: arrowTokenID + 3
            } else {
                resultTypeTokenID: arrowTokenID + 2
            }
        }
        arrowTokenID: arrowTokenID + 1
    }
    return compilerBMacroSignatureFacetStoreAppend(
        store: signatures,
        macroSyntaxID: macroSyntaxID,
        syntaxID: signatureToken.start,
        targetTypeTokenID: targetTypeTokenID,
        afterLastTargetTypeTokenID: afterLastTargetTypeTokenID,
        resultTypeTokenID: resultTypeTokenID,
        inputCardinalityTokenID: inputCardinalityTokenID,
        outputCardinalityTokenID: outputCardinalityTokenID
    )
}

function compilerBParseEnumCaseFacets(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let cases: CompilerBEnumCaseFacetStore,
    let enumSyntaxID: Int,
    let openingTokenID: Int,
    let closingTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    state ordinal: Int(0)
    while tokenID < closingTokenID {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        if token.kind == .identity
            && compilerBTokenMatchesBytes(source: source, token: token, expected: "case") {
            if tokenID + 1 >= closingTokenID { return 0 - 1 }
            let identity: CompilerBToken(compilerBRetainedToken(
                store: tokens,
                id: TokenID(value: tokenID + 1)
            ))
            if identity.kind != .identity { return 0 - 1 }
            state afterLastTokenID: Int(tokenID + 2)
            state depth: Int(0)
            state searching: Bool(true)
            while afterLastTokenID < closingTokenID && searching {
                let candidate: CompilerBToken(compilerBRetainedToken(
                    store: tokens,
                    id: TokenID(value: afterLastTokenID)
                ))
                if depth == 0 && candidate.kind == .identity
                    && compilerBTokenMatchesBytes(
                        source: source,
                        token: candidate,
                        expected: "case"
                    ) {
                    searching: false
                } else {
                    let byte: Int(compilerBParserTokenByte(
                        source: source,
                        tokens: tokens,
                        tokenID: afterLastTokenID
                    ))
                    if byte == 40 || byte == 91 || byte == 123 { depth: depth + 1 }
                    if byte == 41 || byte == 93 || byte == 125 { depth: depth - 1 }
                    afterLastTokenID: afterLastTokenID + 1
                }
            }
            if syntaxNodeStoreAppend(
                store: nodes,
                id: token.start,
                firstTokenID: tokenID,
                afterLastTokenID: afterLastTokenID
            ) != 0 { return 0 - 1 }
            if compilerBEnumCaseFacetStoreAppend(
                store: cases,
                syntaxID: token.start,
                ownerSyntaxID: enumSyntaxID,
                identityTokenID: tokenID + 1,
                ordinal: ordinal
            ) != 0 { return 0 - 1 }
            ordinal: ordinal + 1
            tokenID: afterLastTokenID
        } else {
            tokenID: tokenID + 1
        }
    }
    return 0
}

function compilerBParseDeclarationTokens(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let declarations: CompilerBDeclarationFacetStore,
    let ownedFunctions: CompilerBDeclarationFacetStore,
    let blocks: CompilerBBlockFacetStore,
    let functionSignatures: CompilerBFunctionSignatureFacetStore,
    let functionParameters: CompilerBFunctionParameterFacetStore,
    let process: CompilerBProcessGraph,
    let macroSignatures: CompilerBMacroSignatureFacetStore,
    let macroApplications: CompilerBMacroApplicationStore,
    let members: CompilerBMemberFacetStore,
    let enumCases: CompilerBEnumCaseFacetStore,
    let isFunction: Bool,
    let isMacro: Bool,
    let isEnum: Bool,
    let isMemberOwner: Bool,
    let ownerSyntaxID: Int,
    let declarationIndex: Int
): Int {
    if declarationIndex + 1 >= tokens.kinds.count { return 0 - 1 }
    let declaration: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: declarationIndex)
    ))
    let name: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: declarationIndex + 1)
    ))
    if name.kind != .identity { return 0 - 1 }
    state afterGenerics: Int(declarationIndex + 2)
    if afterGenerics < tokens.kinds.count && compilerBParserTokenByte(
        source: source, tokens: tokens, tokenID: afterGenerics
    ) == 60 {
        if isEnum { return 0 - 18 }
        let closing: Int(compilerBParserMatchingRightAngle(
            source: source, tokens: tokens, openingTokenID: afterGenerics,
            limitTokenID: tokens.kinds.count
        ))
        if closing < 0 { return 0 - 18 }
        if compilerBParseGenericMemberFacets(
            source: source, tokens: tokens, nodes: nodes, process: process,
            members: declarations.generics, ownerSyntaxID: declaration.start,
            openingTokenID: afterGenerics, closingTokenID: closing
        ) != 0 { return 0 - 18 }
        afterGenerics: closing + 1
    }

    let declarationLineAfterLastTokenID: Int(compilerBParserLineAfterLastTokenID(
        source: source,
        tokens: tokens,
        firstTokenID: declarationIndex,
        limitTokenID: tokens.kinds.count
    ))
    state openingIndex: Int(afterGenerics)
    state foundOpening: Bool(false)
    while openingIndex < declarationLineAfterLastTokenID && !foundOpening {
        if source.byte(index: compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: openingIndex)
        ).start) == 123 {
            foundOpening: true
        } else {
            openingIndex: openingIndex + 1
        }
    }
    state headerLimit: Int(openingIndex)
    if !foundOpening { headerLimit: declarationLineAfterLastTokenID }
    state headerCursor: Int(afterGenerics)
    while headerCursor < headerLimit {
        if compilerBParserStartsGenericArguments(
            source: source, tokens: tokens, openingTokenID: headerCursor
        ) {
            let closing: Int(compilerBParserMatchingRightAngle(
                source: source, tokens: tokens, openingTokenID: headerCursor, limitTokenID: headerLimit
            ))
            if closing < 0 { return 0 - 18 }
            if compilerBParseSpecializedExpressionFacet(
                source: source, tokens: tokens, nodes: nodes, process: process,
                firstTokenID: headerCursor - 1, afterLastTokenID: closing + 1
            ) < 0 { return 0 - 18 }
            headerCursor: closing
        }
        headerCursor: headerCursor + 1
    }
    if !foundOpening {
        if !isFunction { return 0 - 2 }
        if syntaxNodeStoreAppend(
            store: nodes,
            id: declaration.start,
            firstTokenID: declarationIndex,
            afterLastTokenID: declarationLineAfterLastTokenID
        ) != 0 { return 0 - 3 }
        if compilerBDeclarationFacetStoreAppend(
            store: declarations,
            syntaxID: declaration.start,
            identityTokenID: declarationIndex + 1,
            bodySyntaxID: 0 - 1
        ) != 0 { return 0 - 5 }
        if compilerBParseFunctionFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            signatures: functionSignatures,
            parameters: functionParameters,
            functionSyntaxID: declaration.start,
            nameTokenID: declarationIndex + 1,
            openingTokenID: declarationLineAfterLastTokenID,
            afterLastTokenID: declarationLineAfterLastTokenID
        ) != 0 { return 0 - 8 }
        if process.functionOwners.functionSyntaxIDs.append(
            element: declaration.start
        ) != 0 { return 0 - 8 }
        if process.functionOwners.ownerSyntaxIDs.append(
            element: ownerSyntaxID
        ) != 0 { return 0 - 8 }
        return declarationLineAfterLastTokenID
    }
    let opening: CompilerBToken(compilerBRetainedToken(
        store: tokens,
        id: TokenID(value: openingIndex)
    ))

    state bodyDepth: Int(1)
    state closingIndex: Int(openingIndex + 1)
    while closingIndex < tokens.kinds.count && bodyDepth != 0 {
        let bodyToken: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: closingIndex)
        ))
        let bodyByte: Int(source.byte(index: bodyToken.start))
        if bodyByte == 123 { bodyDepth: bodyDepth + 1 }
        if bodyByte == 125 { bodyDepth: bodyDepth - 1 }
        closingIndex: closingIndex + 1
    }
    if bodyDepth != 0 { return 0 - 2 }

    if syntaxNodeStoreAppend(
        store: nodes,
        id: declaration.start,
        firstTokenID: declarationIndex,
        afterLastTokenID: closingIndex
    ) != 0 { return 0 - 3 }
    if syntaxNodeStoreAppend(
        store: nodes,
        id: opening.start,
        firstTokenID: openingIndex,
        afterLastTokenID: closingIndex
    ) != 0 { return 0 - 4 }
    if compilerBDeclarationFacetStoreAppend(
        store: declarations,
        syntaxID: declaration.start,
        identityTokenID: declarationIndex + 1,
        bodySyntaxID: opening.start
    ) != 0 { return 0 - 5 }
    if compilerBBlockFacetStoreAppend(
        store: blocks,
        syntaxID: opening.start,
        parentSyntaxID: declaration.start
    ) != 0 { return 0 - 6 }
    if isFunction {
        if compilerBParseFunctionFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            signatures: functionSignatures,
            parameters: functionParameters,
            functionSyntaxID: declaration.start,
            nameTokenID: declarationIndex + 1,
            openingTokenID: openingIndex,
            afterLastTokenID: closingIndex
        ) != 0 { return 0 - 8 }
        if process.functionOwners.functionSyntaxIDs.append(
            element: declaration.start
        ) != 0 { return 0 - 8 }
        if process.functionOwners.ownerSyntaxIDs.append(
            element: ownerSyntaxID
        ) != 0 { return 0 - 8 }
    }
    if isMacro {
        if compilerBParseMacroSignatureFacet(
            source: source,
            tokens: tokens,
            nodes: nodes,
            signatures: macroSignatures,
            macroSyntaxID: declaration.start,
            nameTokenID: declarationIndex + 1,
            openingTokenID: openingIndex
        ) != 0 { return 0 - 11 }
    }
    if isEnum {
        if compilerBParseEnumCaseFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            cases: enumCases,
            enumSyntaxID: declaration.start,
            openingTokenID: openingIndex,
            closingTokenID: closingIndex - 1
        ) != 0 { return 0 - 15 }
    }
    if isMacro {
        if compilerBCollectMacroEnvironmentFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            ownerSyntaxID: declaration.start,
            openingTokenID: openingIndex,
            afterLastTokenID: closingIndex
        ) != 0 { return 0 - 14 }
        if compilerBParseEmittedMacroBlockFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            blocks: blocks,
            macroApplications: macroApplications,
            process: process,
            members: members,
            ownerSyntaxID: declaration.start
        ) != 0 { return 0 - 16 }
        if compilerBParseOwnedFunctionDeclarations(
            source: source,
            tokens: tokens,
            nodes: nodes,
            functions: ownedFunctions,
            blocks: blocks,
            functionSignatures: functionSignatures,
            functionParameters: functionParameters,
            process: process,
            macroSignatures: macroSignatures,
            macroApplications: macroApplications,
            members: members,
            enumCases: enumCases,
            ownerSyntaxID: declaration.start,
            openingTokenID: openingIndex,
            closingTokenID: closingIndex - 1
        ) != 0 { return 0 - 17 }
    }
    if isFunction || isMacro {
        let processStatus: Int(compilerBParseProcessFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            process: process,
            blocks: blocks,
            macroApplications: macroApplications,
            members: members,
            ownerSyntaxID: declaration.start,
            bodySyntaxID: opening.start,
            openingTokenID: openingIndex,
            afterLastTokenID: closingIndex
        ))
        if processStatus != 0 { return processStatus }
    }
    if isMemberOwner {
        let memberStatus: Int(compilerBParseOwnedMemberFacets(
            source: source,
            tokens: tokens,
            nodes: nodes,
            applications: macroApplications,
            process: process,
            members: members,
            ownerSyntaxID: declaration.start,
            openingTokenID: openingIndex,
            afterLastTokenID: closingIndex
        ))
        if memberStatus == 0 - 14 { return 0 - 14 }
        if memberStatus != 0 { return 0 - 12 }
    }
    return closingIndex
}

function compilerBParseOwnedFunctionDeclarations(
    let source: String,
    let tokens: CompilerBRetainedTokenStore,
    let nodes: SyntaxNodeStore,
    let functions: CompilerBDeclarationFacetStore,
    let blocks: CompilerBBlockFacetStore,
    let functionSignatures: CompilerBFunctionSignatureFacetStore,
    let functionParameters: CompilerBFunctionParameterFacetStore,
    let process: CompilerBProcessGraph,
    let macroSignatures: CompilerBMacroSignatureFacetStore,
    let macroApplications: CompilerBMacroApplicationStore,
    let members: CompilerBMemberFacetStore,
    let enumCases: CompilerBEnumCaseFacetStore,
    let ownerSyntaxID: Int,
    let openingTokenID: Int,
    let closingTokenID: Int
): Int {
    state tokenID: Int(openingTokenID + 1)
    state depth: Int(0)
    while tokenID < closingTokenID {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenID)
        ))
        let byte: Int(compilerBParserTokenByte(
            source: source,
            tokens: tokens,
            tokenID: tokenID
        ))
        if depth == 0 && token.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: token,
                expected: "function"
            ) {
            let afterFunction: Int(compilerBParseDeclarationTokens(
                source: source,
                tokens: tokens,
                nodes: nodes,
                declarations: functions,
                ownedFunctions: functions,
                blocks: blocks,
                functionSignatures: functionSignatures,
                functionParameters: functionParameters,
                process: process,
                macroSignatures: macroSignatures,
                macroApplications: macroApplications,
                members: members,
                enumCases: enumCases,
                isFunction: true,
                isMacro: false,
                isEnum: false,
                isMemberOwner: false,
                ownerSyntaxID: ownerSyntaxID,
                declarationIndex: tokenID
            ))
            if afterFunction < 0 { return afterFunction }
            tokenID: afterFunction
        } else {
            if byte == 123 { depth: depth + 1 }
            if byte == 125 { depth: depth - 1 }
            tokenID: tokenID + 1
        }
    }
    return 0
}

// Parsing can retain generic syntax before semantic specialization is ready.
function compilerBRevisionHasGenericSyntax(let revision: SyntaxRevision): Bool {
    return revision.constructDeclarations.generics.syntaxIDs.count > 0
        || revision.functionDeclarations.generics.syntaxIDs.count > 0
        || revision.macroDeclarations.generics.syntaxIDs.count > 0
        || revision.process.genericArguments.applicationSyntaxIDs.count > 0
}

function compilerBParseSyntaxRevisionWithResolution(
    let source: String,
    let resolveRelationships: Bool
): SyntaxRevision {
    let tokens: CompilerBRetainedTokenStore(compilerBLexRetained(source: source))
    let nodes: SyntaxNodeStore(syntaxNodeStoreCreate(
        capacity: source.count
    ))
    let functions: CompilerBDeclarationFacetStore(compilerBDeclarationFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let constructs: CompilerBDeclarationFacetStore(compilerBDeclarationFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let enums: CompilerBDeclarationFacetStore(compilerBDeclarationFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let macros: CompilerBDeclarationFacetStore(compilerBDeclarationFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let blocks: CompilerBBlockFacetStore(compilerBBlockFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let macroApplications: CompilerBMacroApplicationStore(compilerBMacroApplicationStoreCreate(
        capacity: tokens.kinds.count
    ))
    let functionSignatures: CompilerBFunctionSignatureFacetStore(compilerBFunctionSignatureFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let functionParameters: CompilerBFunctionParameterFacetStore(
        compilerBFunctionParameterFacetStoreCreate(capacity: tokens.kinds.count)
    )
    let process: CompilerBProcessGraph(compilerBProcessGraphCreate(
        capacity: tokens.kinds.count
    ))
    let macroSignatures: CompilerBMacroSignatureFacetStore(
        compilerBMacroSignatureFacetStoreCreate(capacity: tokens.kinds.count)
    )
    let members: CompilerBMemberFacetStore(compilerBMemberFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    let enumCases: CompilerBEnumCaseFacetStore(compilerBEnumCaseFacetStoreCreate(
        capacity: tokens.kinds.count
    ))
    state depth: Int(0)
    state valid: Bool(true)
    state diagnostic: String("")
    state diagnosticStart: Int(0 - 1)
    state tokenIndex: Int(0)

    while tokenIndex < tokens.kinds.count && valid {
        let token: CompilerBToken(compilerBRetainedToken(
            store: tokens,
            id: TokenID(value: tokenIndex)
        ))
        state nextIndex: Int(0 - 7)
        if compilerBParserTokenByte(source: source, tokens: tokens, tokenID: tokenIndex) == 64 {
            nextIndex: compilerBParseMacroApplicationChain(
                source: source,
                tokens: tokens,
                nodes: nodes,
                applications: macroApplications,
                process: process,
                chainStart: tokenIndex
            )
        } else if token.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: token,
                expected: "function"
            ) {
            nextIndex: compilerBParseDeclarationTokens(
                source: source,
                tokens: tokens,
                nodes: nodes,
                declarations: functions,
                ownedFunctions: functions,
                blocks: blocks,
                functionSignatures: functionSignatures,
                functionParameters: functionParameters,
                process: process,
                macroSignatures: macroSignatures,
                macroApplications: macroApplications,
                members: members,
                enumCases: enumCases,
                isFunction: true,
                isMacro: false,
                isEnum: false,
                isMemberOwner: false,
                ownerSyntaxID: 0 - 1,
                declarationIndex: tokenIndex
            )
        } else if token.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: token,
                expected: "construct"
            ) {
            nextIndex: compilerBParseDeclarationTokens(
                source: source,
                tokens: tokens,
                nodes: nodes,
                declarations: constructs,
                ownedFunctions: functions,
                blocks: blocks,
                functionSignatures: functionSignatures,
                functionParameters: functionParameters,
                process: process,
                macroSignatures: macroSignatures,
                macroApplications: macroApplications,
                members: members,
                enumCases: enumCases,
                isFunction: false,
                isMacro: false,
                isEnum: false,
                isMemberOwner: true,
                ownerSyntaxID: 0 - 1,
                declarationIndex: tokenIndex
            )
        } else if token.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: token,
                expected: "enum"
            ) {
            nextIndex: compilerBParseDeclarationTokens(
                source: source,
                tokens: tokens,
                nodes: nodes,
                declarations: enums,
                ownedFunctions: functions,
                blocks: blocks,
                functionSignatures: functionSignatures,
                functionParameters: functionParameters,
                process: process,
                macroSignatures: macroSignatures,
                macroApplications: macroApplications,
                members: members,
                enumCases: enumCases,
                isFunction: false,
                isMacro: false,
                isEnum: true,
                isMemberOwner: false,
                ownerSyntaxID: 0 - 1,
                declarationIndex: tokenIndex
            )
        } else if token.kind == .identity
            && compilerBTokenMatchesBytes(
                source: source,
                token: token,
                expected: "macro"
            ) {
            nextIndex: compilerBParseDeclarationTokens(
                source: source,
                tokens: tokens,
                nodes: nodes,
                declarations: macros,
                ownedFunctions: functions,
                blocks: blocks,
                functionSignatures: functionSignatures,
                functionParameters: functionParameters,
                process: process,
                macroSignatures: macroSignatures,
                macroApplications: macroApplications,
                members: members,
                enumCases: enumCases,
                isFunction: false,
                isMacro: true,
                isEnum: false,
                isMemberOwner: false,
                ownerSyntaxID: 0 - 1,
                declarationIndex: tokenIndex
            )
        } else if compilerBParserIsMemberKeyword(source: source, token: token) {
            nextIndex: compilerBParseMemberFacet(
                source: source,
                tokens: tokens,
                nodes: nodes,
                process: process,
                members: members,
                ownerSyntaxID: 0 - 1,
                memberIndex: tokenIndex
            )
        }

        if nextIndex != 0 - 7 {
            if nextIndex == 0 - 1 {
                valid: false
                diagnostic: "declaration is missing an identity"
                diagnosticStart: token.start
            } else if nextIndex == 0 - 2 {
                valid: false
                diagnostic: "missing declaration body or unbalanced macro arguments"
                diagnosticStart: token.start
            } else if nextIndex == 0 - 14 {
                valid: false
                diagnostic: "unable to parse macro environment relationships"
                diagnosticStart: token.start
            } else if nextIndex == 0 - 18 {
                valid: false
                diagnostic: "invalid generic syntax: members require explicit let and arguments require named labels"
                diagnosticStart: token.start
            } else if nextIndex < 0 {
                valid: false
                diagnostic: "unable to store syntax node (parser status \(nextIndex))"
                diagnosticStart: token.start
            } else {
                tokenIndex: nextIndex
            }
        } else {
            if token.kind == .leftDelimiter {
                depth: depth + 1
            } else if token.kind == .rightDelimiter {
                if depth == 0 {
                    valid: false
                    diagnostic: "unexpected closing delimiter"
                    diagnosticStart: token.start
                } else {
                    depth: depth - 1
                }
            }
            tokenIndex: tokenIndex + 1
        }
    }

    if depth != 0 && diagnostic == String("") {
        valid: false
        diagnostic: "unbalanced delimiters"
    }
    if valid && resolveRelationships && (
        constructs.generics.syntaxIDs.count > 0 || functions.generics.syntaxIDs.count > 0
        || macros.generics.syntaxIDs.count > 0 || process.genericArguments.applicationSyntaxIDs.count > 0
    ) {
        valid: false
        diagnostic: "generic specialization resolution is not implemented"
        diagnosticStart: 0
    }
    if valid && resolveRelationships {
        let resolutionStatus: Int(compilerBResolveMacroApplications(
            source: source,
            tokens: tokens,
            applications: macroApplications,
            macros: macros,
            signatures: macroSignatures,
            members: members,
            nodes: nodes,
            process: process
        ))
        if resolutionStatus == 0 - 2 {
            valid: false
            diagnostic: "macro application resolves to multiple declarations"
        } else if resolutionStatus == 0 - 3 {
            valid: false
            diagnostic: "macro application does not match any declaration"
        } else if resolutionStatus != 0 {
            valid: false
            diagnostic: "unable to resolve macro application relationships"
        }
    }
    if valid && resolveRelationships {
        let applicationResolutionStatus: Int(compilerBResolveApplications(
            source: source,
            tokens: tokens,
            applications: process.applications,
            constructs: constructs,
            functions: functions,
            macros: macros
        ))
        if applicationResolutionStatus == 0 - 2 {
            valid: false
            diagnostic: "application resolves to multiple declarations"
        } else if applicationResolutionStatus != 0 {
            valid: false
            diagnostic: "unable to resolve application relationships"
        }
    }
    if valid && resolveRelationships {
        let collectionResolutionStatus: Int(compilerBResolveCollectionProductions(
            source: source,
            tokens: tokens,
            process: process,
            members: members,
            macros: macros,
            constructs: constructs
        ))
        if collectionResolutionStatus != 0 {
            valid: false
            diagnostic: "unable to resolve collection production relationships"
        }
    }
    return SyntaxRevision(
        source: source,
        tokens: tokens,
        nodes: nodes,
        functionDeclarations: functions,
        constructDeclarations: constructs,
        enumDeclarations: enums,
        enumCases: enumCases,
        macroDeclarations: macros,
        blocks: blocks,
        macroApplications: macroApplications,
        functionSignatures: functionSignatures,
        functionParameters: functionParameters,
        process: process,
        macroSignatures: macroSignatures,
        members: members,
        environmentSlotsSealed: valid && resolveRelationships,
        valid: valid,
        diagnostic: diagnostic,
        diagnosticStart: diagnosticStart
    )
}

function compilerBParseSyntaxRevision(let source: String): SyntaxRevision {
    return compilerBParseSyntaxRevisionWithResolution(
        source: source,
        resolveRelationships: true
    )
}

function syntaxRevisionDestroy(let revision: SyntaxRevision): Int {
    state status: Int(0)
    if compilerBProcessGraphDestroy(graph: revision.process) != 0 {
        status: 0 - 1
    }
    if compilerBMemberFacetStoreDestroy(store: revision.members) != 0 {
        status: 0 - 1
    }
    if compilerBEnumCaseFacetStoreDestroy(store: revision.enumCases) != 0 {
        status: 0 - 1
    }
    if compilerBMacroSignatureFacetStoreDestroy(
        store: revision.macroSignatures
    ) != 0 { status: 0 - 1 }
    if compilerBFunctionSignatureFacetStoreDestroy(store: revision.functionSignatures) != 0 {
        status: 0 - 1
    }
    if compilerBFunctionParameterFacetStoreDestroy(store: revision.functionParameters) != 0 {
        status: 0 - 1
    }
    if compilerBMacroApplicationStoreDestroy(store: revision.macroApplications) != 0 {
        status: 0 - 1
    }
    if compilerBBlockFacetStoreDestroy(store: revision.blocks) != 0 {
        status: 0 - 1
    }
    if compilerBDeclarationFacetStoreDestroy(
        store: revision.constructDeclarations
    ) != 0 { status: 0 - 1 }
    if compilerBDeclarationFacetStoreDestroy(
        store: revision.enumDeclarations
    ) != 0 { status: 0 - 1 }
    if compilerBDeclarationFacetStoreDestroy(
        store: revision.macroDeclarations
    ) != 0 { status: 0 - 1 }
    if compilerBDeclarationFacetStoreDestroy(
        store: revision.functionDeclarations
    ) != 0 { status: 0 - 1 }
    if syntaxNodeStoreDestroy(store: revision.nodes) != 0 {
        status: 0 - 1
    }
    if compilerBRetainedTokenStoreDestroy(store: revision.tokens) != 0 {
        status: 0 - 1
    }
    return status
}

function syntaxNodeStart(let revision: SyntaxRevision, let row: Int): Int {
    return compilerBRetainedToken(
        store: revision.tokens,
        id: TokenID(value: revision.nodes.firstTokenIDs.element(index: row))
    ).start
}

function syntaxNodeEnd(let revision: SyntaxRevision, let row: Int): Int {
    return compilerBRetainedToken(
        store: revision.tokens,
        id: TokenID(value: revision.nodes.afterLastTokenIDs.element(index: row) - 1)
    ).end
}

function compilerBDeclarationName(
    let revision: SyntaxRevision,
    let declarations: CompilerBDeclarationFacetStore,
    let row: Int
): String {
    let token: CompilerBToken(compilerBRetainedToken(
        store: revision.tokens,
        id: TokenID(value: declarations.identityTokenIDs.element(index: row))
    ))
    return revision.source.substring(start: token.start, end: token.end)
}

function compilerBRenderSyntaxRevision(let revision: SyntaxRevision): String {
    state index: Int(0)
    state output: String("")
    while index < revision.nodes.ids.count {
        let id: Int(revision.nodes.ids.element(index: index))
        let start: Int(syntaxNodeStart(revision: revision, row: index))
        let end: Int(syntaxNodeEnd(revision: revision, row: index))
        let functionRow: Int(revision.functionDeclarations.syntaxIDs.firstIndex(value: id))
        let constructRow: Int(revision.constructDeclarations.syntaxIDs.firstIndex(value: id))
        let enumRow: Int(revision.enumDeclarations.syntaxIDs.firstIndex(value: id))
        let enumCaseRow: Int(revision.enumCases.syntaxIDs.firstIndex(value: id))
        let macroRow: Int(revision.macroDeclarations.syntaxIDs.firstIndex(value: id))
        let blockRow: Int(revision.blocks.syntaxIDs.firstIndex(value: id))
        let memberRow: Int(revision.members.syntaxIDs.firstIndex(value: id))
        if functionRow >= 0 {
            let ownerRow: Int(revision.process.functionOwners.functionSyntaxIDs.firstIndex(value: id))
            state functionOwnerSyntaxID: Int(0 - 1)
            if ownerRow >= 0 {
                functionOwnerSyntaxID: revision.process.functionOwners.ownerSyntaxIDs.element(
                    index: ownerRow
                )
            }
            output.append(text: "syntaxNode\tmarker=function\tid=\(id)\tparent=\(functionOwnerSyntaxID)\tname=\(compilerBDeclarationName(revision: revision, declarations: revision.functionDeclarations, row: functionRow))\tstart=\(start)\tend=\(end)\n")
        } else if constructRow >= 0 {
            output.append(text: "syntaxNode\tmarker=construct\tid=\(id)\tparent=-1\tname=\(compilerBDeclarationName(revision: revision, declarations: revision.constructDeclarations, row: constructRow))\tstart=\(start)\tend=\(end)\n")
        } else if enumRow >= 0 {
            output.append(text: "syntaxNode\tmarker=enum\tid=\(id)\tparent=-1\tname=\(compilerBDeclarationName(revision: revision, declarations: revision.enumDeclarations, row: enumRow))\tstart=\(start)\tend=\(end)\n")
        } else if enumCaseRow >= 0 {
            let identity: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: revision.enumCases.identityTokenIDs.element(
                    index: enumCaseRow
                ))
            ))
            output.append(text: "syntaxNode\tmarker=enumCase\tid=\(id)\tparent=\(revision.enumCases.ownerSyntaxIDs.element(index: enumCaseRow))\tname=\(revision.source.substring(start: identity.start, end: identity.end))\tordinal=\(revision.enumCases.ordinals.element(index: enumCaseRow))\tstart=\(start)\tend=\(end)\n")
        } else if macroRow >= 0 {
            let macroResult: CompilerBDeclarationQueryResult(compilerBMacroDeclarationQueryExecute(
                revision: revision,
                query: CompilerBMacroDeclarationQuery(identity: SyntaxNodeID(value: id))
            ))
            let signatureRow: Int(revision.macroSignatures.macroSyntaxIDs.firstIndex(value: id))
            state targetType: String("")
            state resultType: String("")
            if signatureRow >= 0 {
                let targetTypeTokenID: Int(
                    revision.macroSignatures.targetTypeTokenIDs.element(index: signatureRow)
                )
                if targetTypeTokenID >= 0 {
                    let targetTypeToken: CompilerBToken(compilerBRetainedToken(
                        store: revision.tokens,
                        id: TokenID(value: targetTypeTokenID)
                    ))
                    let afterLastTargetTypeTokenID: Int(
                        revision.macroSignatures.afterLastTargetTypeTokenIDs.element(
                            index: signatureRow
                        )
                    )
                    let lastTargetTypeToken: CompilerBToken(compilerBRetainedToken(
                        store: revision.tokens,
                        id: TokenID(value: afterLastTargetTypeTokenID - 1)
                    ))
                    targetType: revision.source.substring(
                        start: targetTypeToken.start,
                        end: lastTargetTypeToken.end
                    )
                } else {
                    let inputCardinalityTokenID: Int(
                        revision.macroSignatures.inputCardinalityTokenIDs.element(
                            index: signatureRow
                        )
                    )
                    if inputCardinalityTokenID >= 0 {
                        let inputCardinality: CompilerBToken(compilerBRetainedToken(
                            store: revision.tokens,
                            id: TokenID(value: inputCardinalityTokenID)
                        ))
                        targetType: "@\(revision.source.substring(start: inputCardinality.start, end: inputCardinality.end))"
                    }
                }
                let resultTypeTokenID: Int(
                    revision.macroSignatures.resultTypeTokenIDs.element(index: signatureRow)
                )
                if resultTypeTokenID >= 0 {
                    let resultTypeToken: CompilerBToken(compilerBRetainedToken(
                        store: revision.tokens,
                        id: TokenID(value: resultTypeTokenID)
                    ))
                    resultType: revision.source.substring(
                        start: resultTypeToken.start,
                        end: resultTypeToken.end
                    )
                } else {
                    let outputCardinalityTokenID: Int(
                        revision.macroSignatures.outputCardinalityTokenIDs.element(
                            index: signatureRow
                        )
                    )
                    if outputCardinalityTokenID >= 0 {
                        let outputCardinality: CompilerBToken(compilerBRetainedToken(
                            store: revision.tokens,
                            id: TokenID(value: outputCardinalityTokenID)
                        ))
                        resultType: "@\(revision.source.substring(start: outputCardinality.start, end: outputCardinality.end))"
                    }
                }
            }
            output.append(text: "syntaxNode\tmarker=macro\tid=\(id)\tparent=-1\tname=\(macroResult.name)\ttarget=\(targetType)\tresult=\(resultType)\tstart=\(start)\tend=\(end)\n")
        } else if memberRow >= 0 {
            let member: CompilerBMemberQueryResult(compilerBMemberQueryExecute(
                revision: revision,
                query: CompilerBMemberQuery(identity: SyntaxNodeID(value: id))
            ))
            let keywordToken: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: member.keyword
            ))
            state typeName: String("")
            if member.type.value >= 0 {
                let typeToken: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: member.type
                ))
                typeName: revision.source.substring(
                    start: typeToken.start,
                    end: typeToken.end
                )
            }
            output.append(text: "syntaxNode\tmarker=member\tid=\(id)\tparent=\(member.owner.value)\tkind=\(revision.source.substring(start: keywordToken.start, end: keywordToken.end))\tname=\(member.name)\ttype=\(typeName)\tinitializer=\(member.initializerExpression.value)\tbody=\(member.body.value)\tinitializerStart=\(member.initializer.first.value)\tinitializerEnd=\(member.initializer.afterLast.value)\tstart=\(start)\tend=\(end)\n")
        } else if blockRow >= 0 {
            output.append(text: "syntaxNode\tmarker=block\tid=\(id)\tparent=\(revision.blocks.parentSyntaxIDs.element(index: blockRow))\tstart=\(start)\tend=\(end)\n")
        }
        index: index + 1
    }
    state functionParameterRow: Int(0)
    while functionParameterRow < revision.functionParameters.functionSyntaxIDs.count {
        let identity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.functionParameters.identityTokenIDs.element(
                index: functionParameterRow
            ))
        ))
        let type: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.functionParameters.typeTokenIDs.element(
                index: functionParameterRow
            ))
        ))
        output.append(text: "functionParameter\tfunction=\(revision.functionParameters.functionSyntaxIDs.element(index: functionParameterRow))\tlabel=\(revision.source.substring(start: identity.start, end: identity.end))\tname=\(revision.source.substring(start: identity.start, end: identity.end))\ttype=\(revision.source.substring(start: type.start, end: type.end))\tordinal=\(revision.functionParameters.ordinals.element(index: functionParameterRow))\n")
        functionParameterRow: functionParameterRow + 1
    }
    state macroSignatureRow: Int(0)
    while macroSignatureRow < revision.macroSignatures.macroSyntaxIDs.count {
        let macroSyntaxID: Int(
            revision.macroSignatures.macroSyntaxIDs.element(index: macroSignatureRow)
        )
        let cardinality: CompilerBFunctionCardinalityQueryResult(
            compilerBMacroCardinalityQueryExecute(
                revision: revision,
                macroSyntaxID: macroSyntaxID
            )
        )
        if cardinality.found {
            let input: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: cardinality.input
            ))
            let outputCardinality: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: cardinality.output
            ))
            output.append(text: "macroCardinality\tmacro=\(macroSyntaxID)\tinput=\(revision.source.substring(start: input.start, end: input.end))\toutput=\(revision.source.substring(start: outputCardinality.start, end: outputCardinality.end))\n")
        }
        macroSignatureRow: macroSignatureRow + 1
    }
    state applicationRow: Int(0)
    while applicationRow < revision.process.applications.syntaxIDs.count {
        let applicationSyntaxID: Int(
            revision.process.applications.syntaxIDs.element(index: applicationRow)
        )
        let declarationSyntaxID: Int(
            revision.process.applications.declarationSyntaxIDs.element(index: applicationRow)
        )
        let identity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.applications.identityTokenIDs.element(
                index: applicationRow
            ))
        ))
        state declarationKind: String("")
        state inputCardinality: String("")
        state outputCardinality: String("")
        let macroDeclarationRow: Int(revision.macroDeclarations.syntaxIDs.firstIndex(value: declarationSyntaxID))
        if macroDeclarationRow >= 0 {
            declarationKind: "macro"
            let cardinality: CompilerBFunctionCardinalityQueryResult(
                compilerBMacroCardinalityQueryExecute(
                    revision: revision,
                    macroSyntaxID: declarationSyntaxID
                )
            )
            if cardinality.found {
                let input: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: cardinality.input
                ))
                let cardinalityOutput: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: cardinality.output
                ))
                inputCardinality: revision.source.substring(
                    start: input.start,
                    end: input.end
                )
                outputCardinality: revision.source.substring(
                    start: cardinalityOutput.start,
                    end: cardinalityOutput.end
                )
            }
        } else if revision.functionDeclarations.syntaxIDs.firstIndex(value: declarationSyntaxID) >= 0 {
            declarationKind: "function"
        }
        output.append(text: "application\tid=\(applicationSyntaxID)\tpredecessor=\(revision.process.applications.predecessorSyntaxIDs.element(index: applicationRow))\tdeclaration=\(declarationSyntaxID)\tkind=\(declarationKind)\tname=\(revision.source.substring(start: identity.start, end: identity.end))\tinput=\(inputCardinality)\toutput=\(outputCardinality)\n")
        applicationRow: applicationRow + 1
    }
    state componentRow: Int(0)
    while componentRow < revision.process.components.ownerApplicationSyntaxIDs.count {
        let labelTokenID: Int(
            revision.process.components.labelTokenIDs.element(index: componentRow)
        )
        state label: String("_")
        if labelTokenID >= 0 {
            let labelToken: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: labelTokenID)
            ))
            label: revision.source.substring(
                start: labelToken.start,
                end: labelToken.end
            )
        }
        let firstValue: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.components.firstValueTokenIDs.element(
                index: componentRow
            ))
        ))
        let lastValue: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.components.afterLastValueTokenIDs.element(
                index: componentRow
            ) - 1)
        ))
        output.append(text: "applicationComponent\towner=\(revision.process.components.ownerApplicationSyntaxIDs.element(index: componentRow))\tordinal=\(revision.process.components.ordinals.element(index: componentRow))\tlabel=\(label)\tvalue=\(revision.source.substring(start: firstValue.start, end: lastValue.end))\n")
        componentRow: componentRow + 1
    }
    state processRow: Int(0)
    while processRow < revision.process.processes.ownerSyntaxIDs.count {
        let resultExpressionSyntaxID: Int(
            revision.process.processes.resultExpressionSyntaxIDs.element(
                index: processRow
            )
        )
        if resultExpressionSyntaxID >= 0 {
            let resultExpressionRow: Int(revision.process.expressions.syntaxIDs.firstIndex(value: resultExpressionSyntaxID))
            if resultExpressionRow >= 0 {
                state kind: String("unknown")
                let kindCode: Int(revision.process.expressions.kindCodes.element(
                    index: resultExpressionRow
                ))
                if kindCode == compilerBParserExpressionKindIntegerCode() {
                    kind: "integer"
                }
                if kindCode == compilerBParserExpressionKindBooleanCode() {
                    kind: "boolean"
                }
                if kindCode == compilerBParserExpressionKindStringCode() {
                    kind: "string"
                }
                if kindCode == compilerBParserExpressionKindComparisonCode() {
                    kind: "comparison"
                }
                if kindCode == compilerBParserExpressionKindLogicalCode() {
                    kind: "logical"
                }
                let first: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(value: revision.process.expressions.firstTokenIDs.element(
                        index: resultExpressionRow
                    ))
                ))
                let last: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(
                        value: revision.process.expressions.afterLastTokenIDs.element(
                            index: resultExpressionRow
                        ) - 1
                    )
                ))
                output.append(text: "processResult\towner=\(revision.process.processes.ownerSyntaxIDs.element(index: processRow))\texpression=\(resultExpressionSyntaxID)\tkind=\(kind)\tvalue=\(revision.source.substring(start: first.start, end: last.end))\n")
            }
        }
        processRow: processRow + 1
    }
    state localRow: Int(0)
    while localRow < revision.process.locals.syntaxIDs.count {
        let identity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.locals.identityTokenIDs.element(index: localRow))
        ))
        let expressionSyntaxID: Int(
            revision.process.locals.valueExpressionSyntaxIDs.element(index: localRow)
        )
        let fallbackRow: Int(revision.process.noValueFallbacks.expressionSyntaxIDs.firstIndex(value: expressionSyntaxID))
        state hasNoValueFallback: Bool(false)
        state rhsKind: String("")
        if fallbackRow >= 0 {
            hasNoValueFallback: true
            let rhs: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: revision.process.noValueFallbacks.firstRHSTokenIDs.element(
                    index: fallbackRow
                ))
            ))
            rhsKind: compilerBTokenKindName(kind: rhs.kind)
        }
        output.append(text: "processLocal\tid=\(revision.process.locals.syntaxIDs.element(index: localRow))\towner=\(revision.process.locals.ownerSyntaxIDs.element(index: localRow))\tkind=\(revision.process.locals.kindCodes.element(index: localRow))\tname=\(revision.source.substring(start: identity.start, end: identity.end))\tvalue=\(expressionSyntaxID)\tnoValueFallback=\(hasNoValueFallback)\trhsKind=\(rhsKind)\n")
        localRow: localRow + 1
    }
    state assignmentRow: Int(0)
    while assignmentRow < revision.process.assignments.syntaxIDs.count {
        let identity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.assignments.identityTokenIDs.element(
                index: assignmentRow
            ))
        ))
        output.append(text: "processAssignment\tid=\(revision.process.assignments.syntaxIDs.element(index: assignmentRow))\towner=\(revision.process.assignments.ownerSyntaxIDs.element(index: assignmentRow))\tname=\(revision.source.substring(start: identity.start, end: identity.end))\tvalue=\(revision.process.assignments.valueExpressionSyntaxIDs.element(index: assignmentRow))\tordinal=\(revision.process.assignments.ordinals.element(index: assignmentRow))\n")
        assignmentRow: assignmentRow + 1
    }
    state returnRow: Int(0)
    while returnRow < revision.process.returns.syntaxIDs.count {
        output.append(text: "processReturn\tid=\(revision.process.returns.syntaxIDs.element(index: returnRow))\towner=\(revision.process.returns.ownerSyntaxIDs.element(index: returnRow))\tvalue=\(revision.process.returns.valueExpressionSyntaxIDs.element(index: returnRow))\tordinal=\(revision.process.returns.ordinals.element(index: returnRow))\n")
        returnRow: returnRow + 1
    }
    state conditionalRow: Int(0)
    while conditionalRow < revision.process.conditionals.syntaxIDs.count {
        output.append(text: "processIf\tid=\(revision.process.conditionals.syntaxIDs.element(index: conditionalRow))\towner=\(revision.process.conditionals.ownerSyntaxIDs.element(index: conditionalRow))\tcondition=\(revision.process.conditionals.conditionExpressionSyntaxIDs.element(index: conditionalRow))\tbody=\(revision.process.conditionals.bodySyntaxIDs.element(index: conditionalRow))\talternative=\(revision.process.conditionals.alternativeSyntaxIDs.element(index: conditionalRow))\talternativeKind=\(revision.process.conditionals.alternativeKindCodes.element(index: conditionalRow))\n")
        conditionalRow: conditionalRow + 1
    }
    state switchRow: Int(0)
    while switchRow < revision.process.switches.syntaxIDs.count {
        output.append(text: "processSwitch\tid=\(revision.process.switches.syntaxIDs.element(index: switchRow))\towner=\(revision.process.switches.ownerSyntaxIDs.element(index: switchRow))\tsubject=\(revision.process.switches.subjectExpressionSyntaxIDs.element(index: switchRow))\n")
        switchRow: switchRow + 1
    }
    state comparisonRow: Int(0)
    while comparisonRow < revision.process.comparisons.syntaxIDs.count {
        output.append(text: "processComparison\tid=\(revision.process.comparisons.syntaxIDs.element(index: comparisonRow))\tleft=\(revision.process.comparisons.leftExpressionSyntaxIDs.element(index: comparisonRow))\tright=\(revision.process.comparisons.rightExpressionSyntaxIDs.element(index: comparisonRow))\toperator=\(compilerBComparisonOperatorName(operatorCode: revision.process.comparisons.operatorCodes.element(index: comparisonRow)))\n")
        comparisonRow: comparisonRow + 1
    }
    state logicalRow: Int(0)
    while logicalRow < revision.process.logicals.syntaxIDs.count {
        output.append(text: "processLogical\tid=\(revision.process.logicals.syntaxIDs.element(index: logicalRow))\tleft=\(revision.process.logicals.leftExpressionSyntaxIDs.element(index: logicalRow))\tright=\(revision.process.logicals.rightExpressionSyntaxIDs.element(index: logicalRow))\toperator=\(compilerBLogicalOperatorName(operatorCode: revision.process.logicals.operatorCodes.element(index: logicalRow)))\n")
        logicalRow: logicalRow + 1
    }
    state switchCaseRow: Int(0)
    while switchCaseRow < revision.process.switchCases.syntaxIDs.count {
        output.append(text: "processSwitchCase\tid=\(revision.process.switchCases.syntaxIDs.element(index: switchCaseRow))\tswitch=\(revision.process.switchCases.switchSyntaxIDs.element(index: switchCaseRow))\tcondition=\(revision.process.switchCases.conditionExpressionSyntaxIDs.element(index: switchCaseRow))\tbodyStart=\(revision.process.switchCases.firstBodyTokenIDs.element(index: switchCaseRow))\tbodyEnd=\(revision.process.switchCases.afterLastBodyTokenIDs.element(index: switchCaseRow))\tordinal=\(revision.process.switchCases.ordinals.element(index: switchCaseRow))\n")
        switchCaseRow: switchCaseRow + 1
    }
    state provisionRow: Int(0)
    while provisionRow < revision.process.applicationProvisions.syntaxIDs.count {
        let sourceSyntaxID: Int(
            revision.process.applicationProvisions.sourceSyntaxIDs.element(
                index: provisionRow
            )
        )
        let sourceRow: Int(revision.process.locals.syntaxIDs.firstIndex(value: sourceSyntaxID))
        let sourceIdentity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.locals.identityTokenIDs.element(index: sourceRow))
        ))
        output.append(text: "applicationProvision\tid=\(revision.process.applicationProvisions.syntaxIDs.element(index: provisionRow))\towner=\(revision.process.applicationProvisions.ownerSyntaxIDs.element(index: provisionRow))\tenvironment=\(revision.process.applicationProvisions.environmentSyntaxIDs.element(index: provisionRow))\textension=\(revision.process.applicationProvisions.extensionSyntaxIDs.element(index: provisionRow))\ttarget=\(revision.process.applicationProvisions.targetApplicationSyntaxIDs.element(index: provisionRow))\tsourceIdentity=\(sourceSyntaxID)\tsource=\(revision.source.substring(start: sourceIdentity.start, end: sourceIdentity.end))\n")
        provisionRow: provisionRow + 1
    }
    state environmentRow: Int(0)
    while environmentRow < revision.process.environments.syntaxIDs.count {
        output.append(text: "environment\tid=\(revision.process.environments.syntaxIDs.element(index: environmentRow))\towner=\(revision.process.environments.ownerDeclarationSyntaxIDs.element(index: environmentRow))\n")
        environmentRow: environmentRow + 1
    }
    state environmentNodeRow: Int(0)
    while environmentNodeRow < revision.process.environmentNodes.syntaxIDs.count {
        let containedSyntaxID: Int(
            revision.process.environmentNodes.syntaxIDs.element(index: environmentNodeRow)
        )
        state containedKind: String("extension")
        state containedValue: String("")
        let containedMacroBlockRow: Int(revision.process.emittedMacroBlocks.syntaxIDs.firstIndex(value: containedSyntaxID))
        if containedMacroBlockRow >= 0 {
            containedKind: "macroBlock"
            let identity: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: revision.process.emittedMacroBlocks.identityTokenIDs.element(
                    index: containedMacroBlockRow
                ))
            ))
            containedValue: revision.source.substring(start: identity.start, end: identity.end)
        } else {
            let containedApplicationRow: Int(revision.process.applications.syntaxIDs.firstIndex(value: containedSyntaxID))
            if containedApplicationRow >= 0 {
                containedKind: "application"
                let identity: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(value: revision.process.applications.identityTokenIDs.element(
                        index: containedApplicationRow
                    ))
                ))
                containedValue: revision.source.substring(start: identity.start, end: identity.end)
            } else {
                let containedMemberRow: Int(revision.members.syntaxIDs.firstIndex(value: containedSyntaxID))
                if containedMemberRow >= 0 {
                    containedKind: "member"
                    let identity: CompilerBToken(compilerBRetainedToken(
                        store: revision.tokens,
                        id: TokenID(value: revision.members.identityTokenIDs.element(
                            index: containedMemberRow
                        ))
                    ))
                    containedValue: revision.source.substring(
                        start: identity.start,
                        end: identity.end
                    )
                }
            }
        }
        output.append(text: "environmentNode\tenvironment=\(revision.process.environmentNodes.environmentSyntaxIDs.element(index: environmentNodeRow))\tid=\(containedSyntaxID)\tparent=\(revision.process.environmentNodes.parentSyntaxIDs.element(index: environmentNodeRow))\tordinal=\(revision.process.environmentNodes.ordinals.element(index: environmentNodeRow))\tkind=\(containedKind)\tvalue=\(containedValue)\n")
        environmentNodeRow: environmentNodeRow + 1
    }
    state collectionRow: Int(0)
    while collectionRow < revision.process.collections.syntaxIDs.count {
        let selector: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.collections.selectorIdentityTokenIDs.element(
                index: collectionRow
            ))
        ))
        output.append(text: "collection\tid=\(revision.process.collections.syntaxIDs.element(index: collectionRow))\tselector=\(revision.process.collections.selectorMacroSyntaxIDs.element(index: collectionRow))\tmarker=\(revision.source.substring(start: selector.start, end: selector.end))\n")
        collectionRow: collectionRow + 1
    }
    state materializationApplicationRow: Int(0)
    while materializationApplicationRow < revision.macroApplications.syntaxIDs.count {
        let targetSyntaxID: Int(revision.macroApplications.targetSyntaxIDs.element(
            index: materializationApplicationRow
        ))
        let targetCollectionRow: Int(revision.process.collections.syntaxIDs.firstIndex(value: targetSyntaxID))
        let targetMemberRow: Int(revision.members.syntaxIDs.firstIndex(value: targetSyntaxID))
        if targetCollectionRow >= 0 && targetMemberRow >= 0 {
            let kindToken: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: revision.members.keywordTokenIDs.element(
                    index: targetMemberRow
                ))
            ))
            output.append(text: "collectionMaterialization\tapplication=\(revision.macroApplications.syntaxIDs.element(index: materializationApplicationRow))\tdeclaration=\(revision.macroApplications.macroDeclarationSyntaxIDs.element(index: materializationApplicationRow))\ttarget=\(targetSyntaxID)\towner=\(revision.members.ownerSyntaxIDs.element(index: targetMemberRow))\tmemberKind=\(revision.source.substring(start: kindToken.start, end: kindToken.end))\tadmission=\(revision.process.collections.selectorMacroSyntaxIDs.element(index: targetCollectionRow))\tstorage=derived\n")
        }
        materializationApplicationRow: materializationApplicationRow + 1
    }
    state productionRow: Int(0)
    while productionRow < revision.process.collectionProductions.syntaxIDs.count {
        let outputIdentity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.process.collectionProductions.outputIdentityTokenIDs.element(
                index: productionRow
            ))
        ))
        output.append(text: "collectionProduction\tid=\(revision.process.collectionProductions.syntaxIDs.element(index: productionRow))\tsource=\(revision.process.collectionProductions.sourceCollectionSyntaxIDs.element(index: productionRow))\toutput=\(revision.process.collectionProductions.outputDeclarationSyntaxIDs.element(index: productionRow))\toutputName=\(revision.source.substring(start: outputIdentity.start, end: outputIdentity.end))\n")
        productionRow: productionRow + 1
    }
    state executionRow: Int(0)
    while executionRow < revision.process.executions.syntaxIDs.count {
        let expressionSyntaxID: Int(
            revision.process.executions.expressionSyntaxIDs.element(index: executionRow)
        )
        state callee: String("")
        let applicationRow: Int(revision.process.applications.syntaxIDs.firstIndex(value: expressionSyntaxID))
        if applicationRow >= 0 {
            let predecessorSyntaxID: Int(
                revision.process.applications.predecessorSyntaxIDs.element(
                    index: applicationRow
                )
            )
            let predecessorRow: Int(revision.process.applications.syntaxIDs.firstIndex(value: predecessorSyntaxID))
            if predecessorRow >= 0 {
                let predecessorIdentity: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(value: revision.process.applications.identityTokenIDs.element(
                        index: predecessorRow
                    ))
                ))
                callee.append(text: revision.source.substring(
                    start: predecessorIdentity.start,
                    end: predecessorIdentity.end
                ))
                callee.append(text: ".")
            }
            let applicationIdentity: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: revision.process.applications.identityTokenIDs.element(
                    index: applicationRow
                ))
            ))
            callee.append(text: revision.source.substring(
                start: applicationIdentity.start,
                end: applicationIdentity.end
            ))
        } else {
            let macroApplicationRow: Int(revision.macroApplications.syntaxIDs.firstIndex(value: expressionSyntaxID))
            if macroApplicationRow >= 0 {
                let macroIdentity: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(value: revision.macroApplications.identityTokenIDs.element(
                        index: macroApplicationRow
                    ))
                ))
                callee.append(text: "@")
                callee.append(text: revision.source.substring(
                    start: macroIdentity.start,
                    end: macroIdentity.end
                ))
            }
        }
        output.append(text: "processExecution\tid=\(revision.process.executions.syntaxIDs.element(index: executionRow))\towner=\(revision.process.executions.ownerSyntaxIDs.element(index: executionRow))\texpression=\(expressionSyntaxID)\tordinal=\(revision.process.executions.ordinals.element(index: executionRow))\tfunction=\(callee)\n")
        executionRow: executionRow + 1
    }
    state parameterRow: Int(0)
    while parameterRow < revision.macroSignatures.parameterMacroSyntaxIDs.count {
        let identity: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.macroSignatures.parameterIdentityTokenIDs.element(
                index: parameterRow
            ))
        ))
        let type: CompilerBToken(compilerBRetainedToken(
            store: revision.tokens,
            id: TokenID(value: revision.macroSignatures.parameterTypeTokenIDs.element(index: parameterRow))
        ))
        state typeName: String("")
        if revision.macroSignatures.parameterCardinalityValues.element(
            index: parameterRow
        ) != 0 { typeName.append(text: "@") }
        typeName.append(text: revision.source.substring(start: type.start, end: type.end))
        state defaultValue: String("")
        let defaultFirstTokenID: Int(
            revision.macroSignatures.parameterDefaultFirstTokenIDs.element(
                index: parameterRow
            )
        )
        if defaultFirstTokenID >= 0 {
            let defaultFirst: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: defaultFirstTokenID)
            ))
            let defaultAfterLast: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: revision.macroSignatures.parameterDefaultAfterLastTokenIDs.element(
                    index: parameterRow
                ) - 1)
            ))
            defaultValue: revision.source.substring(
                start: defaultFirst.start,
                end: defaultAfterLast.end
            )
        }
        output.append(text: "macroParameter\tmacro=\(revision.macroSignatures.parameterMacroSyntaxIDs.element(index: parameterRow))\tname=\(revision.source.substring(start: identity.start, end: identity.end))\ttype=\(typeName)\toptional=\(revision.macroSignatures.parameterOptionalValues.element(index: parameterRow) != 0)\tdefault=\(defaultValue)\tordinal=\(revision.macroSignatures.parameterOrdinals.element(index: parameterRow))\n")
        parameterRow: parameterRow + 1
    }
    state argumentRow: Int(0)
    while argumentRow < revision.macroApplications.argumentApplicationSyntaxIDs.count {
        let labelTokenID: Int(
            revision.macroApplications.argumentLabelTokenIDs.element(index: argumentRow)
        )
        state label: String("_")
        if labelTokenID >= 0 {
            let labelToken: CompilerBToken(compilerBRetainedToken(
                store: revision.tokens,
                id: TokenID(value: labelTokenID)
            ))
            label: revision.source.substring(start: labelToken.start, end: labelToken.end)
        }
        let expressionRow: Int(revision.process.expressions.syntaxIDs.firstIndex(value: revision.macroApplications.argumentValueExpressionSyntaxIDs.element(index: argumentRow)))
        state kind: String("unknown")
        if expressionRow >= 0 {
            let kindCode: Int(revision.process.expressions.kindCodes.element(index: expressionRow))
            if kindCode == compilerBParserExpressionKindIntegerCode() { kind: "integer" }
            if kindCode == compilerBParserExpressionKindBooleanCode() { kind: "boolean" }
            if kindCode == compilerBParserExpressionKindStringCode() { kind: "string" }
        }
        output.append(text: "macroArgument\tapplication=\(revision.macroApplications.argumentApplicationSyntaxIDs.element(index: argumentRow))\tlabel=\(label)\tvalue=\(revision.macroApplications.argumentValueExpressionSyntaxIDs.element(index: argumentRow))\tkind=\(kind)\tordinal=\(revision.macroApplications.argumentOrdinals.element(index: argumentRow))\n")
        argumentRow: argumentRow + 1
    }
    state relationshipRow: Int(0)
    while relationshipRow < revision.macroApplications.syntaxIDs.count {
        let declarationSyntaxID: Int(
            revision.macroApplications.macroDeclarationSyntaxIDs.element(index: relationshipRow)
        )
        if declarationSyntaxID >= 0 {
            output.append(text: "macroRelationship\tapplication=\(revision.macroApplications.syntaxIDs.element(index: relationshipRow))\tdeclaration=\(declarationSyntaxID)\ttarget=\(revision.macroApplications.targetSyntaxIDs.element(index: relationshipRow))\n")
        }
        relationshipRow: relationshipRow + 1
    }
    state applicationEnvironmentRow: Int(0)
    while applicationEnvironmentRow
        < revision.macroApplications.environmentApplicationSyntaxIDs.count {
        output.append(text: "macroEnvironment\tapplication=\(revision.macroApplications.environmentApplicationSyntaxIDs.element(index: applicationEnvironmentRow))\tenvironment=\(revision.macroApplications.environmentSyntaxIDs.element(index: applicationEnvironmentRow))\tordinal=\(revision.macroApplications.environmentOrdinals.element(index: applicationEnvironmentRow))\n")
        applicationEnvironmentRow: applicationEnvironmentRow + 1
    }
    state expandedMarkerRow: Int(0)
    while expandedMarkerRow < revision.macroDeclarations.syntaxIDs.count {
        let markerDeclarationSyntaxID: Int(
            revision.macroDeclarations.syntaxIDs.element(index: expandedMarkerRow)
        )
        state expandedRelationshipOrdinal: Int(0)
        let expandedRelationshipCount: Int(
            compilerBExpandedMacroRelationshipCount(
                revision: revision,
                markerDeclarationSyntaxID: markerDeclarationSyntaxID
            )
        )
        while expandedRelationshipOrdinal < expandedRelationshipCount {
            let relationship: CompilerBExpandedMacroRelationshipQueryResult(
                compilerBExpandedMacroRelationshipQueryExecute(
                    revision: revision,
                    markerDeclarationSyntaxID: markerDeclarationSyntaxID,
                    ordinal: expandedRelationshipOrdinal
                )
            )
            let targetApplicationRow: Int(revision.process.applications.syntaxIDs.firstIndex(value: relationship.extensionTarget.value))
            state channel: String("")
            if targetApplicationRow >= 0 {
                let channelIdentity: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(value: revision.process.applications.identityTokenIDs.element(
                        index: targetApplicationRow
                    ))
                ))
                channel: revision.source.substring(
                    start: channelIdentity.start,
                    end: channelIdentity.end
                )
            }
            state emissionName: String("")
            let memberRow: Int(revision.members.syntaxIDs.firstIndex(value: relationship.emission.value))
            if memberRow >= 0 {
                let memberIdentity: CompilerBToken(compilerBRetainedToken(
                    store: revision.tokens,
                    id: TokenID(value: revision.members.identityTokenIDs.element(
                        index: memberRow
                    ))
                ))
                emissionName: revision.source.substring(
                    start: memberIdentity.start,
                    end: memberIdentity.end
                )
            }
            if relationship.found {
                output.append(text: "expandedMacroRelationship\tapplication=\(relationship.application.value)\tenvironment=\(relationship.environment.value)\textension=\(relationship.extensionSyntax.value)\tchannel=\(channel)\temission=\(relationship.emission.value)\tname=\(emissionName)\tmarkerApplication=\(relationship.markerApplication.value)\tdeclaration=\(relationship.markerDeclaration.value)\tordinal=\(relationship.ordinal)\n")
            }
            expandedRelationshipOrdinal: expandedRelationshipOrdinal + 1
        }
        expandedMarkerRow: expandedMarkerRow + 1
    }
    state macroIndexRow: Int(0)
    while macroIndexRow < revision.macroDeclarations.syntaxIDs.count {
        let macroSyntaxID: Int(revision.macroDeclarations.syntaxIDs.element(index: macroIndexRow))
        output.append(text: "macroTargetIndex\tdeclaration=\(macroSyntaxID)\ttargets=\(compilerBMacroRelationshipCount(revision: revision, macroDeclarationSyntaxID: macroSyntaxID))\n")
        macroIndexRow: macroIndexRow + 1
    }
    return output
}
