# Deferred Core

These definitions and meaning documents were moved from `Language/Core` while
active Core design focuses on `Int` and `@integer`. Their directory structure
and contents are preserved for reference and deliberate reintroduction.

This directory is excluded from automatic Core source discovery. The bounded
compiler evaluator tests explicitly use the deferred Byte, Bool, and String
declarations as fixture inputs; this does not establish their active Core status.
