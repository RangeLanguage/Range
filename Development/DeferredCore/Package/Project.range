macro project(): Construct -> Void {
    let targets: #environment.target.macros.filter(for: @target)

    if targets.isEmpty {
        @diagnostic("Project must declare at least one @target")
    }
}

construct Project {
    let identity: String
    let root: URL
    let toolchainRoot: URL
}
