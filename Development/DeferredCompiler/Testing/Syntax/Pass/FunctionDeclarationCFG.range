compilerTypedSyntaxCFGSnapshot

function graphFlow(let input: Int): Int {
    state value: Int(input + 2)
    while value > 0 {
        if value == 1 {
            value: value - 1
        } else {
            value: value - 2
        }
    }
    return value
}
